# Open Findings — verified, unfixed, as of 2026-08-13

> ## STATUS RECONCILIATION — 2026-08-18, lane `register-reconcile`, at HEAD `5909552`
>
> **Read this before the work claim below it.** Ninety rows were re-checked against the tree at
> `5909552` and each now carries a dated `RECONCILED 2026-08-18` clause appended to its own text,
> with the command or `file:line` that produced the verdict. Nothing was deleted and nothing was
> renumbered: a falsified sentence stays visible with the correction beside it, per this file's own
> convention.
>
> **Why the pass was run, and it is this file's own recorded prediction coming true.** On 2026-08-18
> four lane briefs were compiled from a session task list instead of from this register, and
> **six of twelve dispatched findings did not exist** — three of them (60, 62, 63) had been recorded
> CLOSED here on 2026-08-17. Three lanes spent a run confirming a closure. HEAD's own commit message
> says so: *"The briefs were compiled from a session task list without reading the register that
> supersedes it… logged as finding 103."* **A lane brief is compiled from this file, never from a
> task list.**
>
> **The work claim immediately below is stale and is the specific sentence that cost it.** Its line
> *"Findings **60, 62, 63, 69** are reported done by it"* summarises `b50bd77`, whose own message
> says *"SNAPSHOT, NOT A CLOSE-OUT."* It is left standing, uncorrected in place, because row 103 is
> about it — but **it is not a status line and must not be read as one.** The row's own appended
> clause is the only place a status lives now; read the row, not the header.
>
> **Verdict counts from this pass, over the 90 rows re-checked** — **21 CLOSED · 1 NOT REAL AT
> HEAD · 15 PARTIAL · 53 OPEN**. **37 of 90 rows changed status** (21 + 1 + 15) — the register was
> wrong about two rows in five of those it was asked about.
>
> **A note on the census that motivated this pass.** The brief counted *109 of 114 rows carrying no
> closure marker in their first 400 characters*; scanning the same way here returns **99 of 130**
> row-shaped lines. Neither figure is a count of open findings, and the 400-character window is why:
> rows **81, 82, 83, 61, 65, 127 and 153** all carry a closure, ruling or dated correction **past**
> character 400 and read as unmarked. A head-of-row scan is a triage tool, never a status.
>
> **Fixed by a commit that never named the row — 3, and the method is stated so it can be re-run.**
> For each CLOSED row the fixing commit was located with `git log -S` on a distinctive string from
> the repair, then its full message searched for that row number
> `[obs:2026-08-18 git log --oneline -S<repair string> -- <file> then git log -1 --format=%B <sha>,
> over 12 CLOSED rows, at 5909552]`. Named: 66, 70, 71, 72, 73, 74, 75, 81, 82, 83, 92, 93,
> 100, 101, 150. **Not named: 118** (`5909552` names findings 60/62/63/67/68/79/103/147/155/173),
> **161** (`6316edf`, `cea7400`, `ade5457` — all name skills, none names the row), and **162**
> (`e06073a`, whose body describes the defect in full and whose subject is about a different one).
> This is the same shape the register was already bitten by when `b50bd77` fixed row 62 in silence.
>
> **Three regressions were closed by a later blind run rather than by inspection** — G-R1, G-R2 and
> G-R3 all read PASS in `blind-2026-08-17/`. That is the only evidence class that can close a
> regression row, which is why rows 154–158 and 168–178 all stay OPEN however much carrier work
> landed today.
>
> **Boundary of this pass, stated so nobody reads it as complete.** NOT re-checked: the six PILOT
> rows **121–126** (client-site observations that no repo commit can close — they close on a new
> capture or a client action), row **65** (section A, carries its own 2026-08-17 correction), row
> **76** (section C, superseded), and every row already carrying a closure marker in its own text
> (**60, 62, 63, 67, 69, 77, 78, 79, 80, 86, 87, 88, 91, 96–99, 102, 105–113, 116, 133**, plus A1's
> **B1, B2, B3, F1–F9**). This pass did not open the tree for any of those, so their existing
> closure text stands on its own original evidence and not on anything measured today.
>
> **Working-tree caution.** Parallel lanes held five files under uncommitted edit while this ran —
> `build/geo-content-optimizer/SKILL.md`, `build/geo-content-optimizer/references/geo-score-arithmetic.md`,
> `cross-cutting/domain-authority-auditor/SKILL.md`, `docs/loop/FAILURE-LEDGER.md` and
> `references/action-output-contract.md`. Every verdict touching them (rows **148, 149, 153, 159, 160,
> 163, 164**) was taken from `git show 5909552:<path>`, never from the working tree, and each says so.
>
> **Closing re-check, and it caught two rows moving under the pass.** Re-running `git status` at close found seven
> further files dirty that were not dirty at open, including `docs/loop/eval-baselines/INSTRUMENT-CHANGES.md`
> (+131 lines) and four blind records. Two verdicts above are being falsified in the working tree as this is
> written — **166** (the editor-slot encoding is being normalised corpus-wide) and **167** (an id-remap entry has
> been drafted). Both notes are appended to their own rows, both stay OPEN, and neither closes on uncommitted work.
> Checked and **unaffected**: C2 (the drift is on the e3.5 slot, not on `skill_version_under_test`) and G-M1 / 158
> (the `summary` objects did not move).

> ## ⚠ WORK CLAIM — two sessions are working this list. Read before editing anything.
>
> **A local (Mac Studio) session and this cloud session are both working.** Neither can see the
> other's context, and editing a file another session is mid-way through is ledger **F8** —
> which cost a reviewer its entire pass earlier today. The split below is the whole
> coordination mechanism; there is no other channel.
>
> **CLOUD SESSION HAS CLAIMED (2026-08-11, do not edit these):**
> `optimize/content-refresher/**` · `monitor/alert-manager/**` · `build/geo-content-optimizer/**` ·
> `references/core-eeat-benchmark.md` · `docs/loop/SETTLED-RULINGS.md` ·
> `docs/loop/FAILURE-LEDGER.md` · `docs/loop/OPEN-FINDINGS.md`
> — covering findings **66, 70, 71, 72, 73, 74, 75**, plus the section-A rulings.
>
> **LOCAL SESSION HOLDS:** everything else in section B, the pilot tree
> (`docs/loop/pilot/**`), the R3 supersession candidate, and the two gates. Findings
> **60, 62, 63, 69** are reported done by it.
>
> **If you are the local session**: pull before you start, take from section B outside the
> claimed paths, and add your own claim line here in the same commit that starts the work —
> not the one that finishes it. A claim written afterwards protects nobody.
>
> **MAC STUDIO SESSION CLAIMS (2026-08-18, opening commit — Sani confirmed the cloud session ENDED,
> from its own status line "session end requested", not inferred from push silence):**
> `references/greek-output-mechanics.md` (new) · the Greek-carrier pointer lines only in
> `optimize/content-refresher/SKILL.md`, `optimize/internal-linking-optimizer/SKILL.md`,
> `monitor/performance-reporter/SKILL.md`, `build/schema-markup-generator/SKILL.md`,
> `optimize/on-page-seo-auditor/SKILL.md` · the five tracking files those bumps require
> (`VERSIONS.md`, `.claude-plugin/plugin.json`, `marketplace.json`, `README.md`, `CLAUDE.md`).
> — covering the Greek-mechanics carrier gap, then the 9 rubric repairs named by the
> 2026-08-18 pre-flight audit.
>
> ⚠️ **Three of those five skills sit inside paths the 2026-08-11 cloud claim above still
> names.** That claim is stale — its session has ended — but it is left standing rather than
> deleted, per this file's own convention that a superseded line is corrected in place. This
> claim supersedes it for the five pointer lines ONLY, and touches nothing else in those trees.
>
> **Neither session merges PR #9.** The condition is unmet and the gates are unrun.

**Why this file exists.** These lived only in a session-local task list, which no other session
and no file could read. A Mac Studio session asked which set "the 18 open findings" meant, could
not find them anywhere in the repository, and was right: they had never been written down. That
is the same defect class as ledger **F17** — a thing that governs work, carried nowhere a worker
can reach it. Fixed here.

**The count moved.** "18" was accurate when said and is **20** now; three were opened after.
A count quoted from a running list is a timestamp, not a fact — this file supersedes any number
quoted in conversation.

**Status vocabulary — corrected 2026-08-13, and the correction is the point.** This read
*"every row below is OPEN. Nothing here is applied."* It was falsified four sections down by
`## A0. RULED` and by finding 77's *"RULED, and applied"* — a stale sibling left standing when a
status flipped, which is ledger **F11**'s founding class. **The rule F11 recurrence 5 widened the
guard to is that a recorded flip triggers a re-scan of the whole register**, not just the row that
flipped; this header was the row nobody re-read. What is true now:

- **Section A0** — ruled, and where the ruling could be applied here, applied. Each says which.
- **Section A1** — the merge gate's findings, each marked FIXED or OPEN in its own row.
- **Sections A, B, C** — open. Where a row needs a decision rather than an edit, it says so and
  names who decides.

Rows that have moved between sections are **not** left behind in their old one: a finding
appearing twice is a finding that can be closed once and stay open elsewhere.

---

## A0. RULED 2026-08-13 — decisions made, application named

**77, 79 and 68 were section A rows; 80 was opened straight into this section** and never sat in
A — stated because the previous wording ("These were section A") was false for it, and a reader
checking A for 80's history would have found nothing and doubted the record rather than the
sentence. All four are now decided, and their rows have been **removed from section A** rather
than duplicated there. Where the file to change belongs to the other session, the ruling says
so — **a ruling and its application are different jobs, and the second one belongs to whoever
holds the file.**

### 77 — RULED, and applied to C09. **The heading below was wrong when written — see B1.**

> **Correction, 2026-08-13.** This section originally read *"The benchmark no longer asserts
> engine behaviour."* That was false at the moment it was written. C09 had been fixed; §4 of
> the same file still stated "All engines extract from first paragraph", and sixteen more
> instances stood across eight other files. The merge gate found it. **A closure note is a
> claim, and one written from the instance you just fixed rather than from a sweep is the F9
> shape in prose** — the register said the class was closed on the evidence of one member.
> Finding **B1** below records the sweep and its result; the sentence here is left in place,
> corrected rather than deleted, so the next reader sees what it used to claim.

`references/core-eeat-benchmark.md` C09 read *"Markup is not required for the Pass — **engines
parse the visible Q&A either way**"*. That is an engine-behaviour claim asserted as fact, and it
is the exact mirror of the one R3 amendment 9a retracted this week for having **no primary source
in either direction**. It had also reached client-facing prose in three of five blind
deliverables, in both languages, because the benchmark carried it.

**Ruling: the criterion rests on what the page contains, not on what an engine does with it.**
The Pass is earned by the visible Q&A block — which is what a reader gets and what any consumer
can read without markup. That is true without needing to know how any engine behaves. Applied in
the same commit as this ruling; the correction is dated in place rather than rewritten, so the
next reader sees what it used to say.

**Why not simply source it**: nobody here can. `developers.google.com` and `schema.org` are both
refused by this environment's egress. Sourcing it would be the better fix and stays available to
anyone who can read those pages.

### 68 — RULED. State the anti-correlation; do not re-weight.
Scoring Search Demand from competitor cluster depth forces Competitive Density from the same
evidence, so at 25% and 20% the two largely cancel and five factors behave as three.

**Ruling: warn at the proxy site, and require the report to say so — do not change the weights.**
Re-weighting would make the published-weight and proxy paths score differently for the same gap,
which is a worse defect than the one being fixed, and it would invalidate every attainable-value
set already derived. When cluster depth is the Demand proxy, the report states that Density is
not independent evidence on that row. **A named limitation the reader can see beats a silent
correction they cannot.**

**APPLIED 2026-08-17** in content-gap-analysis 4.2.7, disclosure only — no weight, formula or
band changed. The application also **measured** what "largely cancel" means, which the ruling
asserted without a number: on the weighted Gap Priority the mirrored pair contributes
`0.25·D + 0.20·(6−D) = 1.20 + 0.05·D`, a spread of **0.20** across the full 1→5 swing against
1.80 if the two moved independently — **11.1% of nominal leverage**. In the **Quick Win Score**
the same two factors are *unweighted*, so the pair contributes a constant `D + (6−D) = 6` and the
cancellation is **total, not partial**: the screen collapses to `Relevance + Effort − 6`, a −4..+4
quantity read against bands calibrated for four inputs over −8..+8. Two gaps at opposite ends of
the demand evidence (18-article cluster vs 2-article cluster) score an **identical +2** and land
in the same tier. The ruling's own text — *"at 25% and 20% they largely cancel"* — was true of the
weighted score and understated for the unweighted one, and `gap-analysis-frameworks.md:372` still
asserted *"The screen is a demand test as much as a cost test."* on a path where it is not one at
all. Carried to `SKILL.md:166`, `:271`, `gap-analysis-frameworks.md:283`/`:294`/`:379`,
`score-arithmetic.md:182`, `analysis-templates.md:430`.

### 79 — RULED. The rule exists; make it reachable where prose is written.
Three deliverables in one suite carried prose contradicting their own tables — a superlative
against a figure printed twenty lines below it, and two count errors in one paragraph. The rule
already exists: *where a sentence and a table disagree, the table wins — fix the sentence.*

**Ruling: this is a placement failure, not a missing rule, and it is fixed by placement, not by
adding an expectation per instance.** The rule sits in a derivation reference that a writer
consults while computing, and is breached later while writing prose about the result. It must
appear where the prose is written — in the output-format section, as a pre-send check.
**No ledger increment**: the existing entry is scoped to the loop's own reporting, and stretching
it to cover deliverable prose would make the counter mean two things.

**APPLIED 2026-08-17** in content-gap-analysis 4.2.7 — and the application found the ruling had
named the wrong site. `SKILL.md:224` (the output-format pre-send check the ruling prescribed) was
already correct at HEAD. The prose slots that actually produce the failing sentences are drafted
from `analysis-templates.md`'s Prioritized Report Template — *Key Findings* and *Why prioritize* —
and carried nothing; the rule now sits at `:420`, outside the client fence and labelled an
author's check. Also corrected: `score-arithmetic.md:193` read *"**Both copies** are the same
rule; change them together"*, a two-site bookkeeping line that a third instance would have
falsified — the F11 shape. Both bookkeeping lines now name all three sites.

### 80 — RULED. A ruling ID is a run handle. The reason it carries is not.
`ruling R2`, `ruling R3 + amendment 9a` and cross-file pointers sit inside client report fences,
uncaught by §6's pattern.

**Ruling: the ID is a handle and goes; the substantive statement stays, in the client's own
terms.** "One accurate schema type per page, so the markup describes what the page is" says
everything a client needs. "(ruling R2)" adds nothing they can act on and names a register they
cannot read. This is the same test family 8 already applies to framework item IDs, and the
gloss-on-first-use exemption does not reach it: that exemption covers framework **names**, which
a client may meet again in their own analytics, not internal register IDs.

**The cross-file pointer is the same class** — a path into this repository is meaningless on a
client surface. It moves to the operator block with the handles.

**Carrier**: §6 family 8, extended to name ruling IDs and repo paths alongside item IDs and
slugs. **Application belongs to the session holding `build/seo-content-writer/`** for the rule
text, and to each skill's holder for the residue.

---

### 102 — RULED 2026-08-17. The seventh claim family: one ships, one is declined on measurement, one is routed elsewhere.

Mode A reported that a seventh shape family existed and that **18 of 19** constructed shapes
escaped all six. **Re-derived before ruling: 16 of 19, not 18.** Two of its examples —
*"Definition blocks are what Google rewards"* and *"The reason Perplexity cites tables is their
density"* — are already caught by P1, whose verb list happens to carry `rewards` and `cites`.
The 16 fall in three groups, and they do not get one answer.

**(a) Prepositional / framing adjunct — SHIPS as P7.** *"According to Google, definition blocks
matter most"*, *"To an LLM, an unsourced number is worthless"*. P1–P6 all require the engine to
be an argument of a verb, subject or object; these have no engine verb at all. Narrowed to a
**comma-closed** adjunct after the first draft returned 75 raw hits, almost all *"optimized for
AI citation"* — which names a goal and is the corrected form this library wants. Final
measurement: **raw +2 (both already excused at stage 3), residual +0, catches 6 of 6**.

**(b) Copular / nominal predication — DECLINED, on measurement, not on principle.** *"Schema is
how engines know what your page is about"*, *"Statistics are a strong citation signal for LLMs"*.
Real class members. The drafted regex caught 2 of the 3 genuine escapes and contributed
**+1 to the real-tree residual — a false positive**, `analysis-templates.md:171`, which is
ruling-R2 guidance. A broader form fired on the **definition of SEO** (17 hits), because `engine`
sits inside the term this library is about. True-to-false on the real tree, 0:4. The sweep's own
header states its failure mode is punishing a corrected line, at a recorded cost four times over.
The three shapes are written into the probe's declined block, so the day a real one appears the
pattern is already drafted.

**(c) Greek — ROUTED to `anti-slop-ruleset.md` §6 as FAIL-grade family 9, not added to the sweep.**
«Η Google προτιμά σελίδες με σαφή ορισμό στην αρχή.» is P1's exact defect in the language half
this library's deliverables are written in, and every verb list in the sweep is English-only.
It does **not** follow that the sweep should learn Greek. That sweep scans repo text; measured
the same day, its seven directories hold **7 Greek-carrying files outside `evals/` and zero Greek
engine-claims** — the Greek there is the ruleset's own vocabulary rows, keyword-transliteration
tables and label examples. Greek engine-claims are absent from the repo because Greek
deliverables are absent from it: they are produced at run time, on the surface §6 governs and the
sweep never sees. A Greek branch there would have reported a clean residual for an unscanned
surface — the exact failure that script's header exists to prevent.

**A measured constraint that decided the form of the Greek net.** In this environment (GNU grep
3.11), `grep -P '[\x{0370}-\x{03FF}]'` **exits 2** under C, POSIX *and* `en_US.UTF-8`, working
only under `C.UTF-8`; the POSIX range `[α-ω]` does the exact opposite — matches by byte under
C/POSIX/`en_US.UTF-8`, aborts under `C.UTF-8`. **There is no locale in which both work.** The
first Greek census run for this ruling used the PCRE form and reported **"0 files carry Greek"
when 30 do**, caught only by cross-checking against fixed strings. Family 9 therefore uses fixed
literals and a NET/RANK two-step; measured **6 of 6 constructed defects flagged, 3 of 3
legitimate Greek lines passed**. The `[α-ω]` half of this was already in §6; the PCRE half is new.

**Carriers**: `scripts/engine-claim-sweep.sh` (P7, plus a KNOWN ESCAPES block recording (b) and
(c) with the measurement that declined or routed each); `anti-slop-ruleset.md` §6 family 9 and the
governing locale note; `anti-slop-provenance.md` (family 9 provenance, per F18).

## A1. Merge-gate findings, 2026-08-13 — what the gate returned and what has been done

The merge gate ran both lanes. **Mode A returned BLOCK.** The contrastive lane issued no
verdict, by design, and additionally declared its own lack of independence — same agent, same
tier, same context as Mode A — which is itself a finding about the layer, not about PR #9.

**As of 2026-08-13 every finding Mode A raised has been fixed except the three contrastive ones,
and those cannot be fixed by editing anything.** C1/C2/C4 are about *measurements*, not defects:
the blind records describe skill versions the tree has moved past. Retaking them needs a **fresh,
uncontaminated executor** — an agent that has not read these skills, this file, or the ledger.
This session cannot supply one, and cannot be one: it wrote the fixes. **A re-run by a contaminated
reader is not a blind run**, and recording one as if it were would poison the baseline every later
comparison rests on. So C1 stays open by construction until a clean session runs it.

**Mode A itself has not re-reviewed the fixes.** Its BLOCK was answered, not withdrawn — a verdict
belongs to the reviewer, and the author of a fix cannot clear it. That is a second thing a fresh
session owes this PR.

| ID | Finding | State |
|---|---|---|
| **B2** | **A permission shipped as a recommendation.** Google's words: *"While you can drop this structured data from your site, there's no need to proactively remove it."* Thirteen shipped surfaces across six skills and one command said "Google **advises against** removing it" — advice never given, in text a client reads. `validate-tracking.sh`'s `R3_LEGAL` allowlist had gained the same phrase as a marker, so the guard could not fail the claim: it *was* the pass condition. | **FIXED** 2026-08-13. All 13 surfaces rewritten; allowlist marker replaced with the faithful phrasing; the overstatement is now a hard fail in check (f), narrowed to lines also mentioning FAQ/schema/markup. Guard probed at the shell per F15 — fires on the old wording, passes the corrected tree. |
| **B1** | **The 9a retraction was applied in one direction only.** 9a retracted "FAQPage earns AI citations" because *no primary source establishes it either way*, and the mirror claim stayed asserted throughout — both scoring frameworks' Top-6 tables, both frameworks' per-engine tables, and a numbered list in `serp-feature-taxonomy.md`. The finding named 6 sites. | **SWEPT 2026-08-17, and the closure note is now written from the sweep rather than from the fix.** The three earlier numbers, in order: the finding named **6** sites; the first closure claimed **17 across 9 files**; the Mode A reviewer then found **23+ survivors** of the class it declared closed. The dedicated sweep rewrote a **hand-counted ~37 sites across 12 skills and 1 framework file** — 12, not the 11 first recorded, and the site count is a hand tally with no derivation, so read it as an order of magnitude and not a measurement. All 12 validate 15/15. Not deleted (F19 overshoot): relabelled as this library's prioritisation model, each reason restated as what the item puts on the page. **The method is the finding.** Four verb families were each complete against their own vocabulary and still missed six members — `AI systems prioritize informational answers` matched none of them, because `prioritize` was on no list. A fifth family existed only because the sweeper assumed its own list was incomplete and tested that assumption. **A verb-list sweep is bounded by its verb list**, so this class is closed *against five stated patterns*, never absolutely. **Reproducible basis: `scripts/engine-claim-sweep.sh`** — five families, a named adjudication list with a reason per excused class, and a `--probe` mode that fault-injects a known member. **The first number written here was wrong, and Mode A caught it.** It read *"measured at the shell 2026-08-17 at `dcabd6b`: raw 171 → residual 42"* — taken on a dirty working tree mid-edit and labelled with `git rev-parse HEAD`, which still named the previous commit. Re-run at `dcabd6b` the command returns **91**; `171/42` occurs at **no commit in the range** (F16 → 3, F9 → 7). Restated from a **`git archive` of commit `42a1798`**, so the working tree cannot contaminate it: **raw 173 → adjudicated → residual 46**. Verified twice — once on a clean checkout, once against the archive after the commit was amended and its hash changed — because a number tied to a hash goes stale the moment anyone rewrites that commit, which happened here within the hour. One of those was a genuine class member on Mode A's read and is now `[VERIFY]`-tagged (`ai-citation-patterns.md:148`, asserting how a product sources its answers); the remaining 45 read as **non-**members (connector tokens, R3-correct wording, retraction notes quoting withdrawn text per F11-r6, and SERP-surface facts). **Re-read them rather than trusting that sentence — it has been wrong once already, and the reader who re-read it is the reason this row is right.** The one line the sweep escalated for a decision — `link-quality-rubric.md:75`, hedged but justifying a scoring weight — is tagged `[VERIFY]`, with the weight shown not to rest on it. |
| **B3** | **"Nothing now alerts later than before" is false.** The 5xx Warning band moved `Any occurrence` → `>1/day`, so a single daily 5xx now raises nothing. Third false superlative found in one day. | **FIXED** 2026-08-13 (the claim). The paragraph now states the loss plainly and quotes the old sentence as the example. **The band decision itself is row 8 of the skill's own *Open threshold decisions* table** — it needs a measurement of this site's 5xx floor, which nobody here has. Fixing it surfaced a second defect: that table was headed "seven rows" over six, the seventh having been written up in prose and never carried into the list. Now eight rows, heading counted from the table. |
| **F1–F9** | Nine Mode A FIX items, incl. `Referring domains` compared across two different periods, crawl errors on three ladders, geo's before-table carrying the after-definition, Content Freshness penalising the Statistics rule, a position ladder that still overlaps (25→26 scores 100), and `claims-gate.sh` failing on this file's own line 33. | **ALL NINE FIXED** 2026-08-13. F1/F6 in alert-manager 4.3.2; F2/F3/F4 in geo 4.4.2; F5 in content-refresher 4.3.2; F7/F8 here and in `pre-push-gate.sh`; F9 in `commit-scope-check.sh`. Ledger: **F9 → 5, F11 → 6, F15 → 2**. F15-r2 was caught **in flight** — the first draft of F9's own fix would have passed `71345f3`, the commit it was written to catch, because that message says "gate" (about **G**ATED-ITEMS). |
| **C1** | **The blind records test skill versions that no longer ship.** geo 4.3.1, content-refresher 4.2.1, alert-manager 4.2.1 — only `gap` matched its subject. This was the decisive reason PR #9 could not proceed: a clean result on a superseded version is not evidence about the current one. | **RE-RUN 2026-08-13; two of three CLOSED.** Three blind executors ran from `8fdb629`. Each verified per-file that nothing it read changed mid-run, and all three came back empty. Version alignment checked at the shell, not asserted: **geo run-at 4.4.2 = now 4.4.2, 0 files changed** · **alert-manager run-at 4.3.2 = now 4.3.2, 0 files changed** · **content-refresher run-at 4.3.2, now 4.3.3, 4 files changed** — that skill was swept *after* its run, so its record is one version behind by construction and is being graded in two states. Graders dispatched for all three. |
| **C2** | `alertmanager.json` misstates its own subject version. | **OPEN.** — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the site is now pinned by measurement rather than by memory.** The record is `docs/loop/eval-baselines/blind-2026-08-11/alertmanager.json`: it states `skill_version_under_test: "4.2.0 (SKILL.md frontmatter version and metadata.version, in lockstep)"`, while at its **own** recorded `repo_head_at_open` (`71345f3c`) that file's frontmatter reads **4.2.1** in both fields. The sibling `blind-2026-08-13/alertmanager.json` is correct — it claims 4.3.2 and `a76706dc` carries 4.3.2 `[obs:2026-08-18 python read of both records; git show 71345f3c:monitor/alert-manager/SKILL.md → version 4.2.1; git show a76706dc:… → 4.3.2, at 5909552]`. One record, one field, off by a patch — and it is the field every currency recount keys on. |
| **C4** | An expectation was rewritten between runs in the FAIL→PASS direction, on 3 of 29. | **OPEN.** — **RECONCILED 2026-08-18 at `5909552` — PARTIAL: the class has a carrier, this instance is still uncorrected.** `docs/loop/eval-baselines/INSTRUMENT-CHANGES.md` (127 lines) now holds four entries, each stating its DIRECTION explicitly — the 2026-08-17 content-gap append (*"addition, not rewrite"*), the 2026-08-17 three-suite retraction row, and the 2026-08-18 row recording three repaired expectation texts with *"no denominator moved"* and *"no recorded verdict moves"*. So a reader comparing two numbers can now find the direction. **What is not done**: no entry restates or re-grades the original 3-of-29 rewrite this row records, and the file is still unreachable from the biggest record set — see row 95 `[obs:2026-08-18 grep -n on docs/loop/eval-baselines/INSTRUMENT-CHANGES.md at 5909552 — entry headings at :17, :48, :87]`. |

## A2. From the 2026-08-13 grading wave — three regressions, and a divergence I failed to record

| ID | Finding | State |
|---|---|---|
| **G-R1** | **REGRESSION, do-not-merge class. content-refresher e4.4 PASS at 4.2.1 → FAIL at 4.3.2.** The clause requires a blog page to be scheduled *on the tool-comparisons cadence* ("Every 3-6 months", last updated ~9 months before the export). The page is scheduled and its stale title year is flagged, but the cadence is invoked nowhere — a scan for `3-6`, `cadence`, `tool comparison` and every content-type word returns zero hits for it, and the audit table carries **no content-type column at all** though the CSV supplies one for all eight rows. **Explicitly checked and not attributable to the R3 sweep**: `git diff c3c9ab4 8fdb629 -- optimize/content-refresher/` touches neither the *Update Strategy by Content Type* table nor the SKILL.md line routing to it. A real capability loss between versions. | **OPEN.** Outranks the pass rate; blocks a clean merge claim on this suite. — **RECONCILED 2026-08-18 at `5909552` — CLOSED by a later blind run, not by inspection.** `blind-2026-08-17/content-refresher.json` records **e4.4 = PASS**, at 4.3.7. The carrier is in place too: `refresh-templates.md:227` now states *"**The Type cell is not optional — it is what routes the row.**… print that type's refresh frequency in the Cadence check cell beside the gap since the last update, and **schedule any page past that frequency on it, quoting the frequency**, whatever the traffic trend shows — a cadence nobody quotes is a cadence nobody applied"*, with a `**Content Type**: [type]` field at `:124` `[obs:2026-08-18 python read of docs/loop/eval-baselines/blind-2026-08-17/content-refresher.json → e4.4 PASS; grep -n on optimize/content-refresher/references/refresh-templates.md at 5909552]`. |
| **G-D9** | **The ruling register and the skills now disagree about R3, and I did not write that down.** Option C moved nine files off the unverified 2026 FAQ dates. `SETTLED-RULINGS.md` R3's Statement **still asserts them, unmarked** — "ended FAQ rich results in 2026 … dropped June 2026 … API scheduled for August 2026". The skills are deliberately *narrower* than the ruling, not contradicting it, and silence is not contradiction — but a future run reading R3 would re-assert what the skills now decline to. **I planned to record this when applying Option C and did not.** The grader found it; a `grep` for it in this file returned nothing. That is the same shape as finding 77's closure note: the intention existed, the record did not, and only an outside reader closed the gap. | **OPEN by design, but now recorded.** The register edit is Sani's gate and is not touched here. It resolves with the R3 verdict — see `r3-decision-brief.md`. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged.** `docs/loop/SETTLED-RULINGS.md:92-94` still states, unmarked, *"Google ended FAQ rich results in 2026 — the search appearance, the rich…"* and *"support is **scheduled for August 2026** — but FAQPage generation stays in the library"*, while the skills decline those dates `[obs:2026-08-18 grep -n on docs/loop/SETTLED-RULINGS.md at 5909552 — lines 92, 94]`. Still Sani's gate; resolves with R3. |
| **G-S1** | **CONFIRMED AND CLOSED. A suite expectation quoted skill text that no longer existed.** content-refresher run-start `e1.2` required FAQPage kept *"for AI-engine/GEO parsing only"* and cited that as the skill's own line — but at run start the skill read *"…no primary source establishes an AI-citation benefit either way, so none is claimed"*. Satisfying the expectation literally would have **breached R3 amendment 9a**. `e2.4` and `e3.6` carried it too. Same class as **F9 recurrence 2** — a sweep rewrote skill text and the suite kept quoting the old text. | **FIXED** by `a76706d`, before the grader reached it. Recorded because the ledger counter should move, not because any work is outstanding. |
| **G-R2** | **REGRESSION, do-not-merge class. alert-manager e2.1 PASS at 4.2.0 → FAIL at 4.3.2.** The prior run replaced the dead FID row with `INP degraded | INP above 200 ms | Warning | P2`. This run kills FID correctly and then writes **no INP rule and no 200 ms boundary anywhere** — `INP` appears once as prose context, and a scan for `200` returns zero hits. Checked, not assumed: the guide's own `\| INP \| >200ms \| >500ms \| >1000ms \|` row is **byte-identical** at 4.2.0 and 4.3.2, so nothing in the wave removed the source. | **OPEN.** — **RECONCILED 2026-08-18 at `5909552` — CLOSED by a later blind run.** `blind-2026-08-17/alert-manager.json` records **e2.1 = PASS**. The carrier the row said was missing is present: `monitor/alert-manager/SKILL.md:285` *`\| INP (field, mobile) \| >200ms — "Needs Improvement" \| >500ms — "Poor" \| Weekly \|`* and `references/alert-configuration-templates.md:240` *`\| INP — Needs Improvement \| Field INP (mobile) above **200 ms** \| Warning \| P2 \|`* `[obs:2026-08-18 python read of docs/loop/eval-baselines/blind-2026-08-17/alert-manager.json → e2.1 PASS; grep -rn INP on the two files at 5909552]`. |
| **G-R3** | **REGRESSION + F19 recurrence. alert-manager e2.2 PASS → FAIL, and this one is partly my wave's doing.** LCP is never corrected to 2.5 s; the deliverable says *"confirm the current boundary numbers … when you reconnect the feed"* and the figure appears nowhere. That is F19's exact ledgered pattern — abstention where a settled ruling holds the answer — **on the same values as F19's founding instance** (content-refresher refusing R4's thresholds). **The contributing cause is skill-side and was introduced by the 4.3.x wave under test**: it added a second status-based CWV row and the one-ladder-per-metric precedence rule, which makes a status-only rebuild the *faithful* reading of the surface. **Root cause: `alert-threshold-guide.md` bands LCP by status word and states 2.5 s nowhere.** R4 appears zero times in `monitor/alert-manager/`, so the number is unreachable from inside the skill — an **F17 shape**, a rule with no carrier. | **ROOT CAUSE FIXED** 2026-08-13 (numeric boundaries now sit beside the status words, citing R4). The regression itself stands until a re-run. — **RECONCILED 2026-08-18 at `5909552` — CLOSED by a later blind run.** `blind-2026-08-17/alert-manager.json` records **e2.2 = PASS**, which is the re-run the row said it was waiting for. The numeric boundary is reachable from inside the skill: `alert-threshold-guide.md:300` *`\| LCP (field, mobile) \| above **2.5 s** … \| above **4.0 s** …`* and `:307` *"'Good' is **LCP ≤ 2.5 s · INP ≤ 200 ms · CLS ≤ 0.1** — settled ruling **R4**"* `[obs:2026-08-18 python read of the record → e2.2 PASS; grep -nE '2\.5' monitor/alert-manager/references/alert-threshold-guide.md at 5909552]`. |
| **G-G1** | **The new citation-divergence guard fires as designed `[obs:2026-08-13T11:05:00Z bash scripts/citation-divergence-check.sh at f51ca6e — Results: 1 passed, 1 warnings, 6 failed]`, and every one of its six failures is on R3 — the ruling that caused this whole problem.** `scripts/citation-divergence-check.sh` (fault-injected, not wired into the gate) reports: R3 names source URLs but **declares no `S<n>` source list**, so no claim can point at a source; one reference is **elided** (`.../2026/05/a-new-resource-for-optimizing`) and an ellipsis is not a URL a verifier can open; one is **scheme-less**; and **three load-bearing claims carry no source pointer at all**. It fires on no other ruling, which is the evidence it is not noise. Its WARN is honest about its own reach: 4 rulings name no URL and state no evidence grade, so it cannot verify them and does **not** fail them. **This is precisely the check whose absence let R3 cite two documents while drawing its claims from a third** — and the reason it was never caught is that nothing compared the two. | **OPEN and not fixable here.** Every fix is an edit to `SETTLED-RULINGS.md`, which is Sani's gate. Resolves with the R3 verdict. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the figure is byte-for-byte the one the row recorded.** `[obs:2026-08-18 bash scripts/citation-divergence-check.sh at 5909552 — 'Results: 1 passed, 1 warnings, 6 failed', 'citation-divergence-check FAILED — a ruling's claims and its citations have come apart', with R4 reported as '2 load-bearing claim sentence(s), 0 sources']`. Every fix is still an edit to `SETTLED-RULINGS.md`, which is Sani's gate. |
| **G-M1** | **Two suites are at their measurement ceiling.** geo scored **93.10% at 4.2.2, 4.3.1 and 4.4.2** — identical rate *and* identical composition — while four out-of-set defects were fixed and six new ones found. content-refresher's headline is identical to baseline (25/1/27) with **one expectation fixed and one broken**. A measurement that cannot separate its subjects cannot say what to fix next. Both graders proposed cheap additions that restore discrimination without inventing a bar. | **OPEN.** Suite-improvement work, not a defect. — **RECONCILED 2026-08-18 at `5909552` — NOT REAL AT HEAD: both ceilings broke.** The row's evidence was geo at **93.10% identical across 4.2.2 / 4.3.1 / 4.4.2** and content-refresher identical to baseline at **25/1/27**. At HEAD the newest records read geo **29 passed / 2 failed / 33 total = 0.8788** and content-refresher **24 / 2 / 27 = 0.8889** — geo's denominator moved 29 → 33 and both rates moved off their pinned values, so the graders' proposed additions landed and the suites separate again `[obs:2026-08-18 python read of docs/loop/eval-baselines/blind-2026-08-17/geo-content-optimizer.json and content-refresher.json summary objects at 5909552]`. The standing concern behind it — suite discrimination — is row 144's and row 120's, not this measurement's. |

## A. Needs a ruling, not an edit — coordinator or Sani

**77, 79 and 68 have moved to A0 (ruled).** They are not repeated here. One row is left, and it
is the one nobody in this repository can settle:

| # | Finding | Who decides |
|---|---|---|
| 65 | **COUNT CORRECTED 2026-08-17 — nine listed, and eleven more on no list; proposal delivered to Sani, nothing applied.** Four registers gave four counts and the disagreement is itself resolved: the guide's **9** is correct at HEAD; this row's **8** is stale by one, written against 4.3.1 and omitting the monthly referring-domain row added by 4.3.2 the same day; the 2026-08-13 decisions doc's **7** was correct for its date; `VERSIONS.md:134`'s **5** counted a different population (rows *reported*, not rows *open*). **Both named conflicts were already fixed** by 4.3.0 — the brief described a pre-4.3.0 tree. The DA/DR pair are no longer active thresholds (both files now say "the connected tool's own scale, tool named"); the 5xx ladder resolved to >1/>5/>20 **per day**, and the wrong table was §2's Absolute Value Method at >5/hour, which is 24× the daily Critical. **A full read then found 11 further rows needing a number that are on no list**, including an active SSL conflict (14 days vs <30/<7) and a fourth number on one metric in §7's playbook triggers. Proposal at `docs/loop/decisions/alert-thresholds-proposal-2026-08-17.md`, row-by-row for approval. **Process finding**: `docs/loop/alert-threshold-decisions-2026-08-13.md` put 7 rows to Sani on 2026-08-13 and **no answer is recorded** — the proposal carries them forward rather than restarting. Original text follows. **alert-manager: eight rows need a threshold decision, not a doc fix.** Includes a row banding **DA 70+** where the guide bands **DR 60+** — different vendors' instruments, not interchangeable; a P1 on a trigger that reaches no band; four page-level rows with no comparison period stated, so their band is underivable; the two citation-*rate* rows shipping one priority above their band default on an override that cannot structurally reach them; and (new, 2026-08-13) whether a single daily 5xx should raise a boundary alert. **All eight are carried in the skill itself**, in `alert-threshold-guide.md` → *Open threshold decisions*, which is a better home than this file: an operator configuring alerts reads the guide, not the loop register. This row exists to say the decision is outstanding, not to hold it. | Sani / operator |

## B. Confirmed defects with a known fix

| # | Finding |
|---|---|
| 67 | **FIXED 2026-08-17 in content-gap-analysis 4.2.7 — but the defect still standing at HEAD is not the one written here.** The quoted equivalence sentence no longer exists anywhere in the tree (removed in `b50bd77`); what remained was **its replacement, which reopened the same hole through its own escape hatch**. `SKILL.md:144` gated demand on *"clear the demand floor (>100/month by default) **or carry the named demand proxy this run is using**"* — but carrying a proxy is a property of the **run**, not the keyword, so in the no-tool run (this skill's normal case, the one the proxy path exists for) every keyword satisfied the gate equally and the filter admitted everything. Row 67's original arithmetic then applied verbatim. Worked: a 2-article shallow cluster scores `1+4+4+5−12 = +2` → *Moderate quick win*, Gap Priority `3.30` → P1, and the tier rule (P0/P1 **and** QWS ≥ 2) ships it as **Tier 1 "do now"** — a gap at the floor of the demand scale. The fix makes the gate keyword-testable on both paths: a proxied run must set and print a floor in the proxy's own units. No weight, formula or band changed. Carried to `SKILL.md:144`/`:272`, `gap-analysis-frameworks.md:79`/`:367`/`:379`, `score-arithmetic.md:188`, `analysis-templates.md:106`. |
| 73 | **content-refresher: the displacement fix is half-done.** The criterion no longer instructs fabrication — proven, the blind run invented nothing — but the input column names competitor *coverage* notes as sufficient while every rung is worded as what ranks *above* the page. The commonest real input can never score it. Unscoreable was not the intent. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** `content-decay-signals.md:210`'s input cell now reads *"Competitor *coverage* notes are not a substitute — see 'Coverage notes cannot score displacement' below"*, and `:242-250` states the behaviour the row said was missing: the row is **N/A** in a coverage-notes-only session, the missing input is named, the weights renormalise, and the notes get a scoring home (*Competitive opportunity* plus the Step 4 missing-topics table). Unscoreable is now an authorised, named state rather than a dead end `[obs:2026-08-18 sed -n '242,250p' optimize/content-refresher/references/content-decay-signals.md at 5909552]`. |
| 70 | **geo: two scoring factors that cannot reward correct behaviour.** Source Citations leaves the score unchanged when you remove an unsourceable claim — which the skill *mandates* — so compliance scores zero and an optimiser is pushed to keep the claim. Freshness passes vacuously on a page with no data points at all. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, both halves.** Source Citations: `geo-score-arithmetic.md` §3.1 now counts a properly-recorded disposition as met, and §3 states *"Both count a properly-recorded disposition as met; both fix `asked` at the step-2 inventory; both refuse to reward a quiet deletion"* (`:328-330`), so removing an unsourceable claim no longer scores zero. Content Freshness: the vacuous pass is gone — `:122` *"A factor with nothing to count is N/A, not a 1"* and the §3.2 table row `:233` *"neither \| N/A \| nothing — excluded from sum and divisor, never scored 1"* `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/references/geo-score-arithmetic.md — lines 122, 233, 310-330]`. |
| 74 | **content-refresher: two templates order the family-8 violation they forbid.** A client-read Quick Score column requires framework item IDs; an example prints item IDs on a client surface and derives an 8-dimension score from a 5-item scan, which the handoff carrier explicitly forbids. **Checked against the 2026-08-13 row-label carve-out on 2026-08-17 and NOT mooted by it.** The carve-out admits an item ID as the *row label* of a scored table with the item's plain-language name beside it in the same row; this template's IDs are a bare list inside a cell — `C02, C03, C05 Pass; C01 Partial; C09 Fail` — with no name against any of them, which is the referent form wearing a table's clothes. The shipped fix (IDs moved to their own operator fence) is the right one and was not reopened. The 5-item-scan half of the row is independent of the carve-out either way. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** `optimize/content-refresher/SKILL.md:154` now states *"**The item IDs do not go in that table.** It is client-read…"*; the client Quick Score rows at `:138-145` read `[X]/100 — [P] pts over [n] items` with no ID cell; and the IDs sit in an `<!-- OPERATOR BLOCK … -->` fence at `:159-168` carrying **both** labels required by the Reader Test amendment. The 5-item-scan half is gone with them `[obs:2026-08-18 sed -n '130,172p' optimize/content-refresher/SKILL.md at 5909552]`. **But see row 117**: the suite still grades the pre-fix shape, so this closure is what makes 117 a live conflict. |
| 75 | **alert-manager: 12 template/rule disagreements.** Worst is a **24× conflict inside one guide** — 5xx banded at ">5/hour" in one table, ">5/day" in another; six errors in a day is Critical under one and Warning under the other. Index coverage stated at both −20% and −15%. The skill contradicts itself on `>3` vs `>=3`, its own worked example disagreeing with its own rule. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** One 5xx ladder in one unit: `alert-threshold-guide.md:149` *`Server 5xx errors \| >1/day \| >5/day`*, with the 24× conflict recorded as resolved at `:207`. Index coverage is −15% on both surfaces (`:137`, `:298`). `>=3` throughout; the single `>3` left at `:209` is the correction table quoting the old value beside its fix `[obs:2026-08-18 grep -n on monitor/alert-manager/references/alert-threshold-guide.md at 5909552]`. |
| 78 | **CLOSED 2026-08-17 — already fixed at HEAD, in `3d0b592` (geo-content-optimizer 4.4.3), and the commit never said so.** The row describes the tree at `1a1b55d`. That commit's body says only that it carries 4.4.3 "from agents still running, swept in by a working-tree commit" — a swept-in fix with no finding named is a fix nobody can find, which is why this row read open for four days. e1.5 now carries the placement clause and more. **The class was swept, not the instance**: all 5 evals scanned for marker-bearing expectations — **6 of 6 carry a placement clause, none missing** (e1.5, e2.5, e3.7, e4.5, e5.2, e5.3), carrier `geo-content-optimizer/SKILL.md:309`. Original text follows. **geo suite: e1.5 is the unfixed sibling of an expectation we corrected.** e2.5 gained a placement clause confining the gap marker to report sections; e1.5 did not. Satisfied literally inside published copy it instructs a FAIL-grade violation. Did not fire this run; armed for the next. |
| 66 | **content-refresher: 4 R3-rationale surfaces deferred behind a blind run.** Deliberate — a blind executor was reading that skill and editing mid-run is F8. Apply now that the run is complete. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** The four R3-rationale surfaces are carried: `SKILL.md:308`, `references/refresh-templates.md:276-277`, `references/ai-overview-recovery.md:65-66` and `references/refresh-example.md:180-182` each state the permission-not-advice framing with the full source (Search Central, *Changes to HowTo and FAQ rich results*, 2023-08-08, URL). Applied by `2e00fd5`, whose subject names the class and not this row `[obs:2026-08-18 grep -rn 'permission to leave existing markup alone' optimize/content-refresher/ at 5909552 — 4 files]`. The row named no file list, so this was matched on the class, not on a named set. |
| 62 | **CLOSED 2026-08-17 — NOT REAL AT HEAD; it was fixed on 2026-08-13 and the register never said so.** The finding's own test now inverts: `grep -i handoff` returns **12** hits, not zero — `SKILL.md:318` carries a `## Handoff to the Next Run` section (three routes, the three payload fields this skill has, the omit-and-name rule for the three it does not, the ruled ID and score forms, the in-fence operator label) and `analysis-templates.md:424` carries the block template. Landed in `b50bd77`, whose message says so. The downstream obligation is met too: `references/inter-skill-handoff.md:339` carries the producer row. **The cost of the stale row is recorded as finding 103** — the section-B header claimed this wave "reported done" a set including 62 while leaving 67/68/79 as an unlabelled mid-flight snapshot, so a reader starting from the register would have re-fixed a fixed sentence and started 67 from scratch. |
| 63 | **CLOSED 2026-08-17 as written — the named contradiction was resolved on 2026-08-13** (`SKILL.md:256` now scopes the ban to emitted output and admits the marker in a requested skeleton; all 12 JSON fences in `schema-templates.md` carry `"_SKELETON"` as first member and the HTML array fence carries the comment form). **Three siblings in the same skill were open and are now fixed** (4.2.9): (a) the Implementation Guide's two HTML fences were unlabelled skeletons using `[Your JSON-LD schema here]`, a stand-in shape the same file's `:186` forbids; (b) `:146`, inside the client-read Schema Analysis fence, carried `(R3 + 9a)` — a settled-ruling ID in client prose, and the escape that produced finding 108; (c) the fence-structure defect now recorded as finding 106. |
| 69 | **Generalised 2026-08-17 into finding 106 — this was one instance of a class, not a typo.** | 
| 69b | **`analysis-templates.md`: an unclosed nested fence truncates the template.** A bare fence inside a `markdown` fence ends the outer one early, so the last lines fall outside it — and a model copies the fence, not the prose around it. Same file: no content-type label exists for a definition/glossary page. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and the row does not say which `analysis-templates.md`; four files bear that name.** In `research/content-gap-analysis/references/analysis-templates.md` **both halves are closed**: the outer fences at `:90` and `:519` are four-backtick, and a `Glossary/definitions` content-type row exists at `:201`. Still standing, same class, in files the row may or may not have meant: `research/serp-analysis/references/analysis-templates.md:18` and `:353`, and `optimize/internal-linking-optimizer/references/linking-templates.md:29` `[obs:2026-08-18 bash scripts/fence-nesting-check.sh --bare-inner at 5909552 — 3 sites in 2 files; the gated walk reports 0 problems over 251 files]`. |
| 72 | **`core-eeat-benchmark.md` §5 has no e-commerce category row.** A Greek e-shop category page — the first case this library's stated market hits — has no mapping, so every skill scoring O05 handles it by invention. Two unresolved siblings: whether a nested `ItemList` counts as a second type, and what to do when the correct type *changes* because of the optimisation. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, all three parts.** §5 now carries the e-commerce category row (`core-eeat-benchmark.md:308`, mapped to `ItemList`); the nesting sibling is ruled at `:314` (`CollectionPage` as an existing root is left alone, new markup uses `ItemList`, never both top-level); the type-change sibling is ruled at `:318` (*"the primary type is **replaced** in the same change"*); and `:173` states what a type with no weight column scores on. **One consequence is NOT closed and is recorded under row 84(c)**: the weight table still has no e-commerce column, and `references/inter-skill-handoff.md:47` still requires the content type *"verbatim, from the CORE-EEAT content-type weight table"* `[obs:2026-08-18 grep -n on references/core-eeat-benchmark.md and references/inter-skill-handoff.md at 5909552]`. |
| 71 | **anti-slop §6 still carries measurement history inside rule text**, in the file restructured today to stop exactly that. No suite names or expectations, so not an F18 recurrence — but, in the finding executor's words, "the separation the file claims for itself is not quite the separation it has." — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** The separation is now stated, and it is a **file** boundary. `anti-slop-ruleset.md:163-169` says the section is *"rule text plus the measured reach of the screens it hands you"* and that a hit rate *"tell[s] you how far to trust a clean grep, which is part of using the rule rather than evidence about it"*; `:171-174` states *"What is never here: the run a rule came from. No suite name, no eval or deliverable label, no quoted expectation, no count tied to any of them"*, pointing at `anti-slop-provenance.md`, which carries `[PROVENANCE — not a rule]` on **142** lines `[obs:2026-08-18 sed -n '160,176p' anti-slop-ruleset.md; grep -c 'PROVENANCE — not a rule' anti-slop-provenance.md at 5909552 — 142]`. |
| 86 | **FIXED 2026-08-17** — carrier written: `docs/loop/eval-baselines/INSTRUMENT-CHANGES.md`, which a reader comparing two numbers reaches before the JSON, with the direction of the change stated (a correct response moved FAIL→PASS) because a reader who does not know the direction cannot even subtract safely. **The re-run it names is still owed.** Original finding: **three eval baselines now measure a different instrument, and no register said so.** `dcabd6b` rewrote e3 in `schema-markup-generator`, e2 in `alert-manager` and e5 in `technical-seo-checker` — all three had *required* the framing ruling R3 amendment 9a retracted, so a run that stated the sourced position used to FAIL. Their prior Mode B baselines therefore grade a question no longer asked, and a later reader comparing old score to new will read an instrument change as a regression or an improvement. **This is open finding C4's shape** (an expectation rewritten between runs in the FAIL→PASS direction) at wave scale rather than line scale. Nothing is wrong with the rewrite; what is missing is the note that the yardstick moved. Needs: a line in `eval-baselines/` marking those three suites' pre-`dcabd6b` records as measuring a superseded expectation, and a re-run before any of them is quoted as evidence. |
| 87 | **CORRECTED IN REGISTER 2026-08-17** — the commit itself cannot be edited without rewriting history that **14** later commits now sit on (measured `git log --oneline e76366c..HEAD`), so the correction lives here, which is where a reader looking for repo state arrives first: **`e76366c` is pushed, superseded by `3d0b592`, and nothing in it is outstanding.** Read its subject as historical as of **2026-08-17T05:02:11Z**, `3d0b592`'s authorship time. **Both figures in the first version of this row were invented**: "five commits" matches no reading of the history — and the row it replaced correctly carried no number at all — while `2026-08-13T21:00Z` **predates the commit's own existence by four days** and corresponds to no event here. F11's founding class, mirror image, inside the register the F11 guard exists to keep clean. Original finding: **`e76366c` ships a label that is now false.** Its subject reads *"NOT reviewed, NOT pushable yet"* and it has been pushed and superseded by `3d0b592`. The content is fine and nothing is outstanding in it — the defect is a standing claim in the permanent record that contradicts the record around it, which is F11's founding class. The house form for exactly this is F10's *"historical as of that timestamp"* marker, and a commit message cannot be edited after pushing without rewriting history that other work now sits on. Fix belongs in a register that a reader reaches first, not in the commit. |
| 92 | **The R3 allowlist fix is real but its headline number is false, and the number was measured in a session nobody saved.** The commit claimed *"fault-injected both ways — 6 of 6 violations caught, 0 of 3 legitimate lines wrongly caught"*. Mode A rebuilt the corpus properly — 17 constructed violations, 9 legitimate R3 lines — and measured **old 7/17 → new 12/17, 0/9 false positives**. The fix is real and one-directional; the claim is not. **Five violations still escape**, and one rides `\bretired\b`, a member the diff *did* bound: *"This **retired** template aside, FAQ schema is eligible for FAQ rich results today"*. Word-bounding closes the **substring** channel only — the comment's own opening sentence names the real one (*"a WHOLE-LINE exclusion … exempts any line that merely contains the word"*) and the fix does not address it. **Owed**: check the corpus in as a fixture and give `validate-tracking.sh` a `--probe` for check (f), so the number is re-runnable — its sibling in the same commit ships one and this one does not (F9-r6's rule, same day). — **RECONCILED 2026-08-18 at `5909552` — CLOSED, and the numbers moved past the row.** Both owed items shipped: the corpus is checked in (`scripts/fixtures/r3-allowlist/`, `scripts/fixtures/r3-overstate/overstate-cases.txt`) and check (f) has a `--probe` (`validate-tracking.sh:11,57,60,94`). The escape count is now **17 of 17 caught, 0 MISSED, 0 declared open** on the r3-violations corpus, against the row's *old 7/17 → new 12/17*; the probe prints that ladder itself. `\bretired\b`'s whole-line channel — the row's named survivor — is closed by the proximity binding `[obs:2026-08-18 bash scripts/validate-tracking.sh --probe at 5909552 — 'r3-violations 17 entries \| 17 caught \| 0 MISSED', 'R3, whole-line unbounded (pre 8f69a7a) 7/17', 'word-bounded (8f69a7a) 12/17']`. |
| 93 | **`DEPRECATED_LEGAL`'s bounding is a practical no-op, shipped as "the same hazard … bounded too".** Measured on 8 constructed FID violations: **0/8 caught before, 0/8 after**. All eight ride members that are still unbounded — `no longer`, `deprecat`, `superseded`, `is dead`, `not named here`, `do not teach`. The two that *were* bounded (`dropped`, `teaching`) have essentially no English words containing them, so the edit cannot change behaviour on a realistic line. Worth knowing: **`\bteaching\b` is the sole excuse for 2 of the only 3 FID lines in the tree**, so the bounding was one character from failing the gate. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** `DEPRECATED_LEGAL` is now split into `DEPRECATED_LEGAL_NAMED` / `DEPRECATED_LEGAL_NEAR` and the near-members are bound to the token by **proximity**, not whole-line (`validate-tracking.sh:525-531`, *"…is now bound to the token by proximity"*). Measured effect on the row's own corpus: **5 of 8 caught, 0 MISSED, 3 declared open**, against the row's *0/8 before, 0/8 after* `[obs:2026-08-18 bash scripts/validate-tracking.sh --probe at 5909552 — 'fid-violations 8 entries \| 5 caught \| 0 MISSED \| 3 declared open'; 'FID, whole-line word-bounded (8f69a7a) 0/8']`. |
| 94 | **Check (f) is green by excuse, not by absence — and the fix changed nothing on the real tree.** Measured: 51 lines match `R3_TOKENS`, 0 survive the allowlist; under the OLD allowlist also 0. 3 lines match `DEPRECATED_TOKENS`, 0 survive. So the commit's *"repo still passes check (f)"* distinguishes nothing — it passed identically before. Sole-excuse concentration on the R3 leg: `non-faq` ×6, `does not (support\|test) faqpage` ×3, `no faq rich result` ×2, `\bretired\b` ×1, `\bended\b` ×1. Any of those weakening silently unblocks several lines at once. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a structural statement; every number in it is stale.** Check (f) is still green by excuse: at HEAD **R3 56 lines match the tokens, 0 survive the allowlist; FID 4 match, 0 survive** (row said 51 and 3). The sole-excuse concentration is now recomputed on every probe run rather than measured once — `non-faq` 6, `no faq rich result` 4, `\bretired\b` 4, `2023-08-08` 4, `does not (support\|test) faqpage` 3 — so *"any of those weakening silently unblocks several lines at once"* is now visible per run instead of asserted `[obs:2026-08-18 bash scripts/validate-tracking.sh --probe at 5909552 — 'Live tree, this commit: R3 56 lines match the tokens, 0 survive the allowlist. FID 4 … 0 survive']`. What the row asked for beyond measurement — a decision about the excuses — has not been taken. |
| 95 | **`INSTRUMENT-CHANGES.md` findability is an assumption, not a mechanism.** Its stated rationale is *"a reader comparing two numbers reaches this directory first"* — but **6 of the affected records sit one directory below it**, carry no pointer up, and nothing outside the file points to it. `docs/loop/PIPELINE.md` (the directory's own description) and the skill-reviewer's regression-check instruction both send a grader to a record **by path**, so the parent is never listed. It is also **outside claims-gate scope** (rule 1/3 covers `docs/loop/*.md` + `VERSIONS.md`, and it sits one level down), so its 52 lines of dated claims were never scanned. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and the largest record set is the uncovered one.** Five record directories now carry an `INSTRUMENT-CHANGES` pointer in their README (`blind-2026-08-10`, `-10b`, `-10c`, `-11`, `-13`). **`blind-2026-08-17/` has no README at all** — 20 records, the newest and biggest set, with no pointer up. `docs/loop/PIPELINE.md` still names the file nowhere. It is still **outside claims-gate scope**: `scripts/claims-gate.sh:12-13` excludes `eval-baselines/` explicitly. **A mechanism now exists and its granularity is the gap**: `validate-tracking.sh` check (i) — added against this very row — reports *"PASS: (i) all 54 baseline record(s) sit in a directory that names INSTRUMENT-CHANGES.md"*, but it tests the **directory**, and `blind-2026-08-17/` qualifies because **5 of its 20 records** name the file inside their own JSON. **The other 15 carry no pointer and the check still reports them covered** `[obs:2026-08-18 grep -ln INSTRUMENT-CHANGES docs/loop/eval-baselines/*/README.md → 5 READMEs, none of them 08-17; ls docs/loop/eval-baselines/blind-2026-08-17/README.md → No such file; grep -l INSTRUMENT-CHANGES docs/loop/eval-baselines/blind-2026-08-17/* → 5 of 20; bash scripts/validate-tracking.sh → '[i] … PASS', at 5909552]`. |
| 96 | **REASSIGNED 2026-08-17 — the in-scope half was already fixed; the real defect is in the shared framework files, not in content-refresher.** The buying-guide route exists in both of content-refresher's own content-type tables (`content-decay-signals.md:363`, `:681`), and `:356-359` already answers the finding's objection. The three factual sub-claims are all **true** and all sit outside that skill: the weight table at `references/core-eeat-benchmark.md:162`, §5/§6 at `:257-262`/`:280-288` (no branch of that tree produces "Comparison"), and the verbatim requirement at `references/inter-skill-handoff.md:47`. The collision cannot bite in content-refresher — it references `weight table`/`Weighted Score`/`weight profile` **zero times** and emits no handoff content-type field. **Sibling of findings 72 and 84(c); belongs with them, in a framework-files lane.** In-scope residue fixed at `SKILL.md:187-189`. Original text follows. **content-refresher: content type has no route for a buying guide, and two vocabularies collide.** The CORE-EEAT weight-table columns (Product Review, How-to Guide, Comparison, Landing Page, Blog Post, FAQ Page, Alternative, Best-of, Testimonial) are a different vocabulary from §5/§6's type names (Blog (guides), Best-of, E-commerce category…). **"Comparison" is a weight-table column the §6 decision tree never produces**, and the handoff requires the label *verbatim from the weight table* — so a retailer's buying guide, the commonest page this library's stated market has, fits none cleanly. Sibling of finding 84(c) and of 72: the same missing-row defect, now blocking a content-type route as well as a handoff. |
| 97 | **FIXED 2026-08-17 in content-refresher 4.3.7.** Proving line before: `SKILL.md:171-172` offered `item ID` or `"none"` and nothing else, so a run facing a plausible material connection it could not settle had to assert one or deny one. The third state already had authority in two files the skill cites — `core-eeat-benchmark.md:199` (*"Evidence for a veto item missing or unassessable — No final score is issued at all"*) and `inter-skill-handoff.md` §4.3. Template now carries `CORE-EEAT-T04 unassessable`, with the consequence stated: after an unassessable veto the full audit issues **no final score at all**, not a capped one. Original text follows. **content-refresher: T04's third state has no home in the Step 1 template.** The veto line offers *item ID* or *"none"*. Where a material connection plausibly exists but disclosure is unverifiable — a retailer comparing products it sells, which is this market's default — the answer is neither pass, nor fail, nor "none". The benchmark says unassessable veto evidence means **no final score at all**, and the quick-scan template has no slot for that outcome. A blind run invented "applicability unresolved" and carried it into the handoff. |
| 98 | **FIXED 2026-08-17 in content-refresher 4.3.7.** No rule existed anywhere; the only related line was the per-dimension three-item bar. Now stated at `SKILL.md:186-187`, as a new Input Validation checkbox at `:348`, and in full in `refresh-templates.md` §"Step 1" with five numbered reporting rules and a stop condition. **No invented threshold**: the stop is *"no dimension reaches three checkable items"*, which follows from the bar the skill already had rather than adding a number. Original text follows. **content-refresher: nothing covers "you were given a description of the page, not the page."** The ≥3-items-per-dimension rule is the right instrument and applies literally, but the result — seven of eight dimensions unscored — is an outcome the skill never anticipates or says how to report, and it never says whether a quick scan may proceed at all without the page text. Same family as serp-analysis finding 90: a skill with a no-data rule for its scores and none for its own applicability. |
| 99 | **SPLIT THREE WAYS 2026-08-17; two halves fixed in content-refresher 4.3.7, one was already dead.** (a) The **C09 Pass criterion** describes text that no longer exists — `references/core-eeat-benchmark.md:338` was reworded 2026-08-13 and now contains zero `restrict*` / `2023-08-08` / `government` / `health websites` tokens. (b) The **FAQPage claim** was real and is fixed on all 5 in-scope surfaces, each now carrying the source URL within its own line window — measured 5 of 5, not asserted. (c) **R4 taken on trust** was real and is fixed in `refresh-templates.md`, which now states what R4 does and does not carry (no source URL, no evidence grade) and then binds it: *"That governs attribution, never disclosure"* — because telling an executor the backing is thin is one step from re-founding F19 in the skill that founded it. Original text follows. **Two more unsourced-claim surfaces in content-refresher, both found by a blind run that could act on the marked ones.** The C09 Pass criterion and the FAQPage discussion state what Google restricted and when with **no citation** — the reader is told the claim is sourced but never shown the source. And Core Web Vitals thresholds cite "settled ruling R4", a pointer into a register **the executor is not given**, so "settled" must be taken on trust. The contrast is the finding: the same skill's `[VERIFY]` blocks on the CTR curve and recovery bands worked perfectly — the run declined to use any of them — so the machinery is right and the coverage is not. |
| 100 | **`eval-expectation-sweep.sh` carries a deliberate copy of check (f)'s patterns, and the MECHANISM has now diverged.** Its `F1_TOK/F1_LEG` and `F2_TOK/F2_LEG` are a sanctioned copy with a standing "keep them copied" instruction, and the member sets are still identical — but check (f) now binds its excuses by proximity while that file still excuses **whole-line, over sentences past 800 characters**, where the hole is wider than it ever was in the original. **A copy kept in sync by member list, while the thing that makes the members work diverges**, is F13's shape with a maintenance note attached: the instruction that was supposed to prevent drift specifies the wrong axis. Decide whether the two share a sourced function or the copy is retired. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, and the decision went to sourcing rather than to retirement.** The copy is gone: `scripts/eval-expectation-sweep.sh:58` states the patterns are imported from `validate-tracking.sh` at run time via `--emit-f-patterns`, *"never copied"*; `:235-241` fails hard if the import is empty or short; `:484-488` derives `F1_LEG` from `SRC["DEPRECATED_LEGAL_NAMED"] + SRC["DEPRECATED_LEGAL_NEAR"]`, i.e. from the proximity split itself, so the mechanism cannot diverge from the member list again; `:680` asserts parity. The probe confirms the export is complete `[obs:2026-08-18 bash scripts/validate-tracking.sh --probe at 5909552 — 'Export to eval-expectation-sweep.sh: 13 pattern name(s), complete against this file's own assignments']`. |
| 101 | **22 of 35 allowlist members carry nothing on today's tree** (R3 14 of 25; FID 8 of 10 — only `\bteaching\b` ×2 and `\bretired\b` ×1 hold anything up). Every one is a whole-line or near-window excuse standing over zero real lines, which is the "narrower excuses" direction's natural next step. **But each also protects a future correct rewording**, so this is a deletion decision with a real cost either way, not a cleanup. The probe now recomputes per-member weight on every run, so the list is recomputed rather than frozen as a snapshot. — **RECONCILED 2026-08-18 at `5909552` — OPEN, number refreshed to 23 of 35.** R3 **15 of 25** carry nothing on today's tree (the row said 14) and FID **8 of 10** (unchanged), with `\bretired\b` now holding 4 R3 lines and 2 FID lines and `\bteaching\b` 2. The recompute-per-run mechanism the row records is working and is what produced this figure; the deletion decision it describes has not been taken `[obs:2026-08-18 bash scripts/validate-tracking.sh --probe at 5909552 — per-member weight block, '.' rows counted]`. |
| 88 | **FIXED 2026-08-17** in serp-analysis 4.3.7 — `# SKELETON` label added as the first line **inside** the fence in the fence's own syntax, plus the standing instruction that an outline with no captured SERP behind it is not emitted at all. Sibling checklist slot labelled too. Original: **the Recommended Content Outline fence was an unlabelled skeleton, and it was armed.** `analysis-templates.md`'s Recommendations block ends with a fenced block that is entirely bracket tokens (`Title: [Optimized title]`, `H2: [Section based on PAA/top results]`) with no in-fence `# SKELETON …` label, and the "Content Requirements to Rank" checklist above it (`Word count: [X]+ words`) is the same shape. Under the Value Rule clause 2 a model copies the fence, not the heading — so a run **with** data ships bracket tokens inside a paste-shaped block. The blind run did not trigger it only because it had no SERP to fill the outline from; the trap is live for every run that does. Same class as the nine command fences fixed earlier, recurring in a skill reference. |
| 89 | **`[VERIFY]` has no stated reader rule.** Three serp-analysis references tag items with it and `SKILL.md` says to report them "tagged unverified", but nothing says whether the literal tag may cross onto a client surface. It is a register handle from an internal queue, so the Reader Test says resolve it into plain language — but the Reader Test names slugs, item IDs, ruling IDs and repo paths, not `[VERIFY]`. A blind run read the instruction both ways and had to rule for itself. Needs one line in the Reader Test naming `[VERIFY]` explicitly. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** No reader rule for the literal `[VERIFY]` string exists anywhere: it is absent from the Reader Test in root `CLAUDE.md`, from `anti-slop-ruleset.md` §6 family 8, and from `references/inter-skill-handoff.md` `[obs:2026-08-18 grep -rn for a VERIFY-crosses-to-client rule over references/, build/seo-content-writer/references/ and research/serp-analysis/ at 5909552 — no match]`. **Adjacent and worth noting**: `geo-content-optimizer/SKILL.md:139` states the rule for its own skill (*"A `[VERIFY]` tag does not travel, so the claim it sits on does not travel either"*) — one skill has it and the library-wide rule the row asks for still does not exist. |
| 90 | **serp-analysis: two more no-data holes the difficulty fix does not cover.** (a) Output Validation requires "every recommendation cites specific data points" and "ranking factors identified from actual top 10 analysis", neither satisfiable when nothing arrived, while Data Sources says "proceed with the full analysis using provided data" — the skill has no stated *nothing arrived* mode. (b) The intent breakdown is emphatic that the split is counted and that unclassifiable elements stay in the denominator; N=0 is not addressed. Both were resolved by the blind run inventing a shape the skill neither authorises nor forbids. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL.** **(a) OPEN**: `serp-analysis/SKILL.md:193` (*"Every recommendation cites specific data points"*) and `:196` (*"Ranking factors identified from actual top 10 analysis"*) stand unchanged, and no *nothing arrived* mode exists. **(b) half-closed**: `:199` now requires every share to state its denominator (*"the intent breakdown counts classified SERP elements (`9 of 9`)"*) — but `N = 0` is still unaddressed, which was the specific hole `[obs:2026-08-18 grep -n on research/serp-analysis/SKILL.md at 5909552 — lines 193, 196, 199]`. |
| 91 | **FIXED 2026-08-17** in serp-analysis 4.3.7 — both figures moved **behind** the tag rather than sitting in body prose ahead of it, and each now states what is still usable without the number, so the reader gets the actionable half without the unquotable one. Original: **`skroutz-visibility-factors.md` stated a number and then forbade citing it.** The BoxNow ~300k locker target and the Skoop per-order fee appear as body facts and are immediately `[VERIFY]`'d with "do not cite a specific number to a client". A figure printed in body prose will be read as usable; the tag arrives after the reader has it. Either the figure moves behind the tag or it goes. |
| 81 | **`geo-score-arithmetic.md` §9 states a locale fact that is wrong under this environment's default, and its prescribed fix has its own silent false positive.** §9 asserts as established in-repo fact that a Greek range `[α-ω]` "fails with `Invalid collation character` and exit status 2". Measured by a blind executor 2026-08-17: the shell default here is **POSIX** (`LANG` empty, `LC_CTYPE=POSIX`), where `[α-ω]` did **not** abort — exit **0**, matching by byte instead. It aborts only under `LC_ALL=C.UTF-8`. So the file's rule is right about the abort and wrong about when, and the dangerous case is the one it does not describe: **a screen that exits 0 having silently matched the wrong thing.** Worse, the prescribed replacement — explicit two-character brackets, `ιδανικ[ηήοό]` — returned **3** of 3 lines under POSIX including a false positive on «ιδανικά», which shares a UTF-8 lead byte, against **2** under `C.UTF-8` and `rg`. §9's *"a screen that exits non-zero has not run"* needs its converse beside it. **Same family, and it feeds a threshold**: POSIX `wc -w` reported **204** words for a Greek deliverable where `LC_ALL=C.UTF-8 wc -w` reported **1116** and a Python token count **1077** — a citation-density threshold read off POSIX `wc` is wrong by roughly 5×. **FIXED 2026-08-17** in geo 4.4.6 and seo-content-writer 4.5.8, after the three measurements were re-taken independently rather than carried from the report. Both files now state the locale dependence, the quiet-failure direction, the workaround's own false positive, and the `wc -w` divergence; the rule is `LC_ALL=C.UTF-8` or `rg`, with the locale recorded beside the result. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, verified rather than taken from the fix note.** `geo-score-arithmetic.md:453-458` prints the locale table (default/`POSIX` → 3 matched by byte, exit 0; `C.UTF-8` → `Invalid collation character`, exit 2), `:465-467` the `ιδανικ[ηήοό]` false positive on «ιδανικά», `:470-471` the `wc -w` divergence `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/references/geo-score-arithmetic.md]`. Note the row's closure marker sits past its 400th character, so a census scanning row heads reads it as unmarked. |
| 82 | **`geo-score-arithmetic.md`: `asked` is undefined for exactly the two factors an optimization run changes.** §3 fixes `asked` once at step 2 "from the content and the brief" and §8 item 5 requires both columns to use it unchanged — but *Clear definitions* derives `asked` from "key terms the content actually uses" and *Quotable statements* from "main sections", and optimizing the page changes both. Read literally, the after-column denominator is capped at the inbound page's vocabulary. The two factors the skill exists to move are the two whose denominator it never says how to fix. **FIX SHIPPED BUT INSUFFICIENT — REOPENED 2026-08-17 after Mode A.** geo 4.4.7 sets `asked` from the **planned** structure, which lets the *scored run itself* choose the denominator. Two consequences the fix note did not name. (a) `asked` can now legitimately **shrink**, and at `asked = 0` the N/A rule drops the factor from both sum and divisor — precisely the "worse hole" §3.1 refused to open when it ruled that widening `met` must never shrink `asked`. (b) The bigger lever is the **lift**, not the ratio: `Lift % = (after − before) ÷ before × 100`, so planning more terms lowers `before` and inflates the headline. Worked by the reviewer on one page: **72% versus 96%, +24 points of headline lift chosen by the run being scored**. The stated guard — "a shrinking `asked` between two runs of the same page is a finding" — needs a prior run and is empty on a first run, which is the normal case. **RULED 2026-08-17, in geo 4.4.9**: `asked = max(inbound, planned)` so the denominator is monotone and cannot be walked to 0 through the N/A rule; and — **corrected the same day, after the exclusion turned out to create the defect the file already forbids** — the two plan-denominated factors **disclose** their denominator beside the lift rather than being excluded from it. §5 rules that a shorter factor set over the same content produces a second baseline and *"the lift then depends on which one the reader happens to read"*; excluding two factors introduced exactly that, six lines above the file's own worked example. One factor set, one baseline, one lift, with `met→met of asked` printed beside it for the two the run chose. **Found by grepping my own closure**: the commit claiming the exclusion was "carried to all of them" had reached two of six surfaces. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, verified.** `asked = max(inbound, planned)` is carried at `geo-score-arithmetic.md:61`, in both factor rows `:65-66`, and as the §3 floor rule at `:93` (*"`asked` has a floor: the count the inbound page already uses"*); the monotonicity reason is at `:113` `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/references/geo-score-arithmetic.md — lines 61,65,66,93,113]`. Closure marker sits past character 400, as with 81. |
| 83 | **Content-freshness scope is undecidable on a listing page, and both readings satisfy §8 rule 10.** Whether a product grid counts as "≥1 time-sensitive figure" decides between `asked = 1` (10/10, +0) and N/A, and §3.2's "already in scope" argument only reaches figures in a supplied data block. Related: Input Validation wants a date "known — or **confirmed** unavailable", and a blind or client-less run can confirm nothing; there is no branch for "not supplied and cannot be asked". **FIX SHIPPED BUT RELOCATES THE DEFECT — REOPENED 2026-08-17 after Mode A.** geo 4.4.7 ruled a time-sensitive figure is one the page **states as a claim**, not one a reader could count off the layout. Rule and rationale disagree on the commonest e-commerce page there is: a **feed-rendered price** is stated in text *and* changes with no edit — in scope by the rule, out by the rationale — and §3.2 lists prices first among time-sensitive figures, pulling a third way. Sharpest: the ruling's own example «12 μοντέλα διαθέσιμα» is generated from the **same inventory query** as the grid it excludes, so the line is drawn at *typography* while the justification appeals to *mutability*. Two honest runs still reach different `asked`. **RULED 2026-08-17, in geo 4.4.9**: the rule now follows the rationale and typography drops out — *a figure is time-sensitive if it would become wrong through the passage of time unless someone edits the page*. Ask it as a question: would this go wrong if nobody touched the page? Hand-written «12 μοντέλα διαθέσιμα», yes; the same sentence auto-generated from the inventory query, no — and so the grid and a feed-rendered price fall out for **one** reason instead of two. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, verified.** `geo-score-arithmetic.md:247-250` carries the mutability rule: *"A figure is time-sensitive if it would become wrong through the passage of time unless [someone edits the page]"*, with the test stated as a question. Typography is out `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/references/geo-score-arithmetic.md — lines 247-250]`. |
| 84 | **Three carrier gaps the blind run hit in one deliverable.** (a) Input Validation accepts a **URL** as content, but *Source citations* needs a claim inventory "taken from the page alone" and the only exit is `asked = 0 → N/A`, written for a different condition — "the page makes no claim" is not "the page could not be read". (b) Step 1's `[VERIFY]`-never-in-the-indicative rule and step 5's structured-data row pull opposite ways: the row asks for "the type the page needs and why", which is the indicative statement, on a client surface. (c) The handoff payload requires the content type **verbatim from the CORE-EEAT weight table**, and that table has **no e-commerce-category column** though §5 and §6 both define the type — so the correct type for a product-listing page cannot be expressed in the payload's required vocabulary, and finding 72's sibling is now blocking a handoff, not just a score. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, split by leg.** **(b) CLOSED**: `geo/SKILL.md:139` now rules *"A `[VERIFY]` tag does not travel, so the claim it sits on does not travel either… never reaches client copy in the indicative"*, and `:147` reframes the step-5 row as three answers with the uncertain call **routed** rather than asserted. **(a) OPEN**: Input Validation `:301` still reads *"Content source identified (URL, full text, or content draft)"* with no branch for a URL that could not be read; the only exit is still `asked = 0 → N/A`. **(c) PARTIAL**: the scoring half is handled (`core-eeat-benchmark.md:173` — unweighted Total Score plus a stated absence), but the **handoff is still blocked** — `references/inter-skill-handoff.md:47` requires the type *"verbatim, from the CORE-EEAT content-type weight table"* and that table has no e-commerce column at HEAD `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/SKILL.md lines 139/147/301; references/inter-skill-handoff.md:47; references/core-eeat-benchmark.md:165-173]`. |
| 85 | **Target queries: required, absent, and derivation forbidden.** Data Sources asks the user for target queries; a brief may give a target *term*. Step 4 then requires "the queries the user named, **not a generic set**", with no third branch. The blind run derived five, labelled them, and asked for confirmation — against the letter of that sentence. Same shape as 84(a): a required input with no stated behaviour when it is absent. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** `geo/SKILL.md:245` still reads *"One row per target query from step 1 — the queries the user named, not a generic set"*. No third branch for *the user named none* exists `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/SKILL.md — line 245]`. |
| 61 | Both auditors' report templates violate their own Output Validation. **Partially settled 2026-08-17, and deliberately not closed.** The ID-column half is settled: the 2026-08-13 carve-out admits the scored per-item table's ID column, both auditors' tables are `\| ID \| Check Item \| Score \| Notes \|` with a plain-language name beside every ID, and both checkboxes now state the carve-out. **The operator-block half was checked and is not settled.** `content-quality-auditor` shows the labelled block inline in its own `SKILL.md` (:302); `domain-authority-auditor` shows none and points to `references/example-report.md` (:122) for the shape. An executor there must traverse to satisfy a checkbox its own template gives nowhere to land — the same root shape as the two regressions diagnosed 2026-08-13, in the safer direction. **Operator-block half FIXED 2026-08-17** in domain-authority-auditor 4.3.6 — the labelled fence now ships inline in its own `SKILL.md`, matching its sibling, with a note recording why it moved out of the reference. **The row stays open** for the reason it was opened: both closures so far were reached from a carrier fix rather than from a full sweep of both auditors' full templates, and closing on that basis is the F9 shape that has been the finding three days running. What is owed is one pass reading both templates in full against their own Output Validation, not another spot fix. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the sweep it asks for still has not been run.** Both closures recorded in the row remain true at HEAD and neither is the full-template pass the row is held open for. No commit in today's range reads both auditors' templates end-to-end against their own Output Validation; the two auditors were touched by `39d56f1`, `5266d39`, `ade5457` and `5909552`, each on a named defect `[obs:2026-08-18 git log --oneline -- cross-cutting/content-quality-auditor/SKILL.md cross-cutting/domain-authority-auditor/SKILL.md at 5909552]`. **A third instance of the class was found and fixed since** (row 113, entity-optimizer), which is evidence for the row's own argument that spot fixes are not a sweep. |
| 60 | **CLOSED 2026-08-17 — not real, and the row named a file that does not exist.** There is no `references/report-templates.md`; the only file of that name is `monitor/performance-reporter/references/report-templates.md`, and its §3 fence has carried the label since `5220e59` (2026-08-13). All three of its fences (`:30`, `:116`, `:319`) carry it, and they are the only fences in the file. **Left open by this closure — see finding 112**: the label they carry is `ILLUSTRATIVE FILL`, a third label vocabulary that neither ruled table names. |
| 64 | Mode A advisories A3 and A4 — provenance reachable by a correctly-scoped executor grep; the editor slot leaves `passed + failed ≠ total` unexplained on the `failed` side. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL: A3 CLOSED, A4 instrumented but unsettled.** **A3 is closed by a file split**: provenance now lives in `build/seo-content-writer/references/anti-slop-provenance.md`, and **142** of its lines carry a `[PROVENANCE — not a rule]` marker so an accidental grep hit identifies itself with no filename and no heading in view; `anti-slop-ruleset.md:171-174` states the boundary and root `CLAUDE.md` encodes it for executors. **A4 is measured, not resolved**: `scripts/eval-corpus-report.sh` now refuses to print one pooled figure — *"MIXED EDITOR-SLOT CONVENTION"*, **32 suites leave the slot uncounted, 15 count it**, giving 1259/1400 = 0.8993 as recorded against 1291/1400 = 0.9221 slot-passed — and individual records still show `passed + failed ≠ total` (e.g. internal-linking-optimizer 28 + 1 ≠ 30) `[obs:2026-08-18 grep -c 'PROVENANCE — not a rule' anti-slop-provenance.md → 142; bash scripts/eval-corpus-report.sh at 5909552]`. |

| 103 | **A register header claimed work was done that was explicitly a mid-flight snapshot, and the cost was a re-fix.** Section B's work-claim header listed findings "60, 62, 63, 69 … reported done"; `b50bd77`'s own commit message says the opposite — *"SNAPSHOT, NOT A CLOSE-OUT … its references/ and evals/ passes have not landed."* 62 was genuinely done; 67, 68 and 79 were not, and the register said nothing. A reader working from the register would have started 67 from scratch and re-fixed an already-deleted sentence — which is what the executing lane began doing before checking the tree. **This is F11's class** (a status line outliving its subject), differing from earlier instances in direction: the claim was not falsified by later work, it was **never true of part of its own scope**. Fix: a register row that summarises a commit quotes that commit's own scope caveat, or does not summarise it. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and its cost recurred today at wave scale.** The rule it proposes **is** carried: `docs/loop/FAILURE-LEDGER.md:2121` — *"a register row summarising a commit **quotes that commit's own scope caveat, or …"***. The **instance is not fixed**: this file's own work-claim header still reads *"Findings **60, 62, 63, 69** are reported done by it"* with no scope caveat and no dated correction. HEAD's own commit message records the recurrence — *"THREE OF THE TWELVE DISPATCHED FINDINGS DID NOT EXIST… Items 60, 63 and 62 were all already recorded CLOSED in OPEN-FINDINGS.md on 2026-08-17… logged as finding 103"* — three lanes spent a run confirming a closure `[obs:2026-08-18 git log -1 --format=%B 5909552; grep -n 'reported done by it' docs/loop/OPEN-FINDINGS.md]`. A dated correction is now appended to that header by this pass; the register-vs-task-list rule itself is carried only in a commit message, which is an F17 shape. |
| 104 | **content-gap: the proxy path has no worked report at full length, only a worked row.** `example-report.md` demonstrates the volume path and is arithmetically clean (all four score rows recompute; `1,200+890+720+1,800 = 4,610` matches its summary). The proxy path — the no-tool default, and the one findings 67 and 68 both sit on — now has a worked *pair* in `gap-analysis-frameworks.md` §4 but no worked *report*. A writer following the proxy path has no example of the finished shape, which is the condition under which the two defects above were introduced in the first place. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** `research/content-gap-analysis/references/example-report.md` is 102 lines and the string `proxy` occurs in it **zero** times: it still demonstrates the volume path only, and the proxy path — the no-tool default — still has no worked report at full length `[obs:2026-08-18 wc -l and grep -c proxy on research/content-gap-analysis/references/example-report.md at 5909552 — 102 lines, 0 hits]`. |
| 105 | **FIXED 2026-08-17 — four expectations appended, suite 30 → 34.** e1.7/e3.8 grade the demand floor on the proxy path (English/Greek), e1.8/e3.9 the Density non-independence disclosure. Every pre-existing expectation is **byte-identical and in position**, checked programmatically — so per-expectation regression comparison against all three prior records stays valid while raw pass rates across the boundary do not; recorded in `docs/loop/eval-baselines/INSTRUMENT-CHANGES.md`. Two disciplines worth keeping: the floor expectations are scoped to **Tier 1 / Quick-Wins membership, not to the gap list**, because without that scoping they would have contradicted e1.1 and e3.2, which require the shallow planted gaps to be *reported*; and the carrier check caught the lane inventing a Greek phrase (`≥ N άρθρα`) the skill states nowhere, which was removed — a rule the skill never states is a test the skill was never told about. Original text follows. **content-gap: the floor and cancellation behaviour just fixed is graded by no eval expectation.** e4's three handoff expectations are now carried, but nothing in `evals.json` grades the demand floor on the proxy path or the disclosure of the Density anti-correlation. A run could regress on either invisibly, and the next blind wave would score 100% while shipping a Tier 1 "do now" for a 2-article cluster. This is the F13 shape stated forward: the fix landed in the skill, and the instrument that would catch its loss does not mention it. |

| 106 | **FIXED 2026-08-17, and the class was invisible to every gate. Finding 69 was filed as a typo; a sweep of all 230 markdown files found the same defect standing in two SKILL.md files.** `build/schema-markup-generator/SKILL.md` had 3 sites where a `\`\`\`html` fence sat inside a `\`\`\`markdown` fence at equal length — the Implementation Guide template cut at its first inner close, and the Output Validation checklist, Validation Checkpoints and Example rendered as literal preformatted text. `build/meta-tags-optimizer/SKILL.md` had an **unclosed fence from line 344 to EOF**, swallowing `## Tips for Success`, `## Reference Materials` and `## Related Skills` whole. Both files passed `validate-skill.sh` at 15/15 and every other gate leg, because every existing check reads the file as text and this defect exists only in the render. **The mechanism, stated because both authors kept tripping on it** (CommonMark 4.5): a fence closes on a line of N-or-more backticks with **no info string**, so `\`\`\`html` inside an open block is *content*, not a closer — the outer block then runs to the next bare fence, which is usually the inner block's closer, and every fence after that is off by one. To nest, the **outer** must be longer. Fixed by promoting outer fences to four backticks. **New gate leg**: `scripts/fence-nesting-check.sh`, wired into `pre-push-gate.sh` immediately rather than run advisory — unlike the engine-claim sweep it contains no judgment (a fence is closed by the rule or it is not), it ships with a probe carrying a negative control and both mechanisms fault-injected, and it returns 0 across all 230 files now that the two real defects are fixed. |
| 107 | **RULED 2026-08-17 — the row-label carve-out covers a column a reader can skip, not an ID prefixed to prose.** The implementing lane found `**[ID] [Name]** — [suggestion]` heading both auditors' Top-5 priority entries and stopped, correctly: the 2026-08-13 carve-out says the name must sit beside the ID, which it does, but the carve-out is written for *"the row label of a scored per-item table"* and a numbered list is not one. **Ruling: the ID comes out.** The distinguishing test is what the client's eye must pass through — a dedicated ID column is beside the reading path and ignorable, an ID prefixed to a prose lead is in it. The forcing reasons differ too: the table's column was kept because Output Validation requires **every** one of 80 (or 40) items scored and the ID is the row's stable identity across an instrument the client bought, while nothing whatever requires an ID in a five-item improvement list and removing it costs the client nothing. Checked before applying: no eval expectation in either suite requires the ID there — they grade the Finding/Evidence/Impact/Fix structure and the confidence labels. Applied to both templates and both worked examples (8 entries); operator blocks keep their handles. Carried to `anti-slop-ruleset.md` family 8 and `CLAUDE.md` Reader Test clause 3. |
| 108 | **FIXED 2026-08-17 — family 8's own net could not match an abbreviated ruling ID, and the escape was standing in a client fence.** Both finding-80 patterns (`ruling R[0-9]`, `amendment [0-9]`) require the literal word, so `(R3 + 9a)` matched neither — and that string sat inside `schema-markup-generator`'s client-read Schema Analysis fence. Found by reading, not by grep. The bare form `\bR[0-9]\b` is added **as a screen, explicitly**: measured over the shipped skill tree it returns **105** hits, and most are legitimate, because skill text, reference files and operator notes are exactly where a ruling handle belongs. Same shape as the `\b[ACEIORT][0-9]{2}\b` false-positive class and handled the same way — every hit goes to the reader test, and the only question is whether the surface is client-read. |
| 109 | **FIXED 2026-08-17 in content-quality-auditor 4.4.5 and domain-authority-auditor 4.3.8 — at zero line cost.** The blocker was real (`content-quality-auditor` sat at 349 of the 350-line cap) and the answer was not to add a checkbox but to extend the one already there: the operator-block checkbox now opens with the report fence's own label requirement — `<!-- SKELETON … -->` while slots are unfilled, `<!-- ILLUSTRATIVE FILL … -->` once demo numbers are in — before going on to the follow-up-run fence. One rule, one line, both halves enforced. Original text kept below for the record. **Neither auditor's Output Validation has a checkbox for the report-fence skeleton label.** The operator block is enforced by that list; the skeleton label — added to both templates on 2026-08-17 — is not. An unenforced label is the condition under which both templates lost it in the first place. Not added yet for a real reason: `content-quality-auditor/SKILL.md` is at **349 of the 350-line cap**, so the checkbox costs a structural decision (which line goes, or does it move to a reference), not just a line. |
| 110 | **FIXED 2026-08-17 on all four surfaces.** Both report headings now name the job in the client's words — `## Content Quality Audit`, `## Domain Authority Audit` — with the framework glossed on first use: for the content auditor as an Overview row (*Scored against: CORE-EEAT — our 80-item content-quality benchmark: how clearly the page answers, how it is organised, how reliable and current it is, and the experience, expertise, authority and trust it shows*), for the domain auditor as a standalone line. **The gloss was folded into the Overview list rather than written as its own paragraph for a measurable reason**: the paragraph form put `content-quality-auditor` at **351** lines against a 350 cap, and the list row costs one line instead of three. Original text kept below for the record. **`## CORE-EEAT Audit Report` and `## CITE Domain Authority Report` are unglossed framework names on a client surface**, in both auditors' templates and both worked examples. Family 8's exemption is conditional — a framework name *"may appear on a client surface **if it is glossed on first use**"* — and no template glosses. A template that orders an ungloss is the fix-the-template case, the same shape as finding 74. Reported rather than fixed because it sits outside finding 61's Output Validation scope and both files are near the line cap. |
| 111 | **RULED AND FIXED 2026-08-17 in schema-markup-generator 4.2.10 — it is an operator lookup, and the file says so two lines above the fence.** The step-1 prose introduces it as *"the table below is that boundary on this skill's supported types"*: it is read to pick the primary type, and nothing in it is emitted. The emitted block is the next fence down, and that one already carries its `<!-- SKELETON … -->` label. The risk the lane correctly flagged is that both are fenced ```markdown, so they look identical to a model — which is the founding reason for the in-fence rule. Now labelled `<!-- OPERATOR BLOCK — a lookup table you read to pick the primary type. It is NOT emitted … -->`. Original text kept below for the record. **`schema-markup-generator/SKILL.md:119-134` — the CORE-EEAT Schema Mapping fence is undecidable from the file.** It carries an item ID and the framework name and is fenced as `markdown`, exactly like the emitted blocks around it. Whether it is emitted output or an operator lookup table cannot be read off the file, and the label it needs differs by answer — if emitted, it is a family-8 hit. Needs a decision before a label can be chosen; a labeller guessing here would encode the guess. |
| 112 | **RULED AND CLOSED 2026-08-17 — three labels, closed list, with a checker that can see it reopened.** `SKELETON` (structure only, nothing paste-ready) · `ILLUSTRATIVE FILL` (filled, but every value invented) · `OPERATOR BLOCK` (addressed to whoever runs the library). Nothing else. `OPERATOR HANDOFF` was a fourth spelling of the third and is **retired** — 4 sites in rank-tracker renamed (4.2.1), including the one at `SKILL.md:137` that quoted the old form as the documented example, which is how a retired synonym comes back. `ILLUSTRATIVE FILL` was in genuine use at 12 sites and answers a question the other two do not, so it joins the ruled tables rather than being folded away. Carried to `references/inter-skill-handoff.md` §3.1 (as a table: label, what it says, when to use) and `CLAUDE.md` Value Rule clause 2. **Enforced, not just written**: `bash scripts/fence-nesting-check.sh --labels` reports any label word off the list — fault-injected by reintroducing `OPERATOR HANDOFF` (2 sites reported), then restored (0). Original finding follows. **There are FOUR in-fence label vocabularies and the two ruled tables name two of them — measured by an instrument that got it wrong.** `CLAUDE.md`'s Value Rule clause 2 and `references/inter-skill-handoff.md` §3.1 enumerate `SKELETON` and `OPERATOR BLOCK`. In the tree there are also `ILLUSTRATIVE FILL` (a *filled* example — `VERSIONS.md:63` records it, `performance-reporter/references/report-templates.md` uses it on all three fences) and `OPERATOR HANDOFF` (`rank-tracker/SKILL.md:209`, inside the client report fence, correctly). **The cost is not hypothetical.** A sweep run 2026-08-17 for unlabelled fences carrying a run handle searched the two ruled labels plus `ILLUSTRATIVE`, and reported `rank-tracker`'s block as a violation — a compliant, correctly-labelled operator block, flagged because its label word was not on the list. A checker cannot verify a rule whose vocabulary is open, and the four labels answer three different questions (structure-only, addressed-to-operator, numbers-are-invented). Needs a closed list, ruled once, in the two tables. Original note follows. **`ILLUSTRATIVE FILL` is a third in-fence label vocabulary that neither ruled table names.** `CLAUDE.md`'s Value Rule clause 2 and `references/inter-skill-handoff.md` §3.1 enumerate `SKELETON` and `OPERATOR BLOCK`. `ILLUSTRATIVE FILL` is the established house form for a *filled* example — `VERSIONS.md:63` records it, and `performance-reporter/references/report-templates.md` uses it on all three fences — but a run reading only the two ruled tables would not know it exists, and the three labels answer three different questions (structure-only, addressed-to-operator, numbers-are-invented). A register gap, not a defect. |

| 113 | **FIXED 2026-08-17 in entity-optimizer 4.2.3 — a third instance of the class finding 61 closed in the two auditors, and the sweep that would have found it was mine to run.** `cross-cutting/entity-optimizer/SKILL.md`'s client report fence ended with an unlabelled `### Cross-Reference` block carrying framework item IDs as **prose referents** — *"Items A07 (Knowledge Graph Presence) and A08 (Entity Consistency) directly overlap"* — plus `CITE I01-I10` and two skill-slug links. Reader Test clause 3 bans an ID as the referent in prose; the row-label carve-out does not reach it. Fixed by the preferred form: lifted out of the client fence entirely, into a labelled operator block. **F9's shape** — a class fixed where it was noticed. The two auditors were fixed under a lane brief scoped to their own files; sweeping the class across the other 18 skills was the coordinator's job and was not done until a scan of all fences carrying a run handle turned this up. |
| 114 | **`content-quality-auditor/SKILL.md` is now at exactly 350 of its 350-line cap.** Two changes today came within one line of hitting it — the Output Validation checkbox (solved by extending an existing line instead of adding one) and the framework gloss (solved by folding a paragraph into a list row). Both solutions were better than what they replaced, so the cap did useful work; but the next addition has no such room, and the repo's own rule says the answer is to move detail to `references/`. Deciding *which* detail is a structural call, not a line edit, and it is owed before the next change to that file. — **RECONCILED 2026-08-18 at `5909552` — OPEN, with the figure corrected: the body is 349, not 350.** The validator counts the body, not the file (384 lines total, 35 of frontmatter). HEAD's own commit says the lane *"landed at 349 of 350 body lines with nothing moved to references/, and it declined to move the one obvious candidate"*. So the structural call the row says is owed still has not been made, and the headroom is one line `[obs:2026-08-18 bash scripts/validate-skill.sh cross-cutting/content-quality-auditor at 5909552 — 'Skill body length OK: 349 lines', 15 passed / 0 warnings / 0 failed]`. Note the validator needs the full path; `validate-skill.sh content-quality-auditor` errors with *SKILL.md not found*. |

| 115 | **The sourced FAQ-restriction claim stands on 30 lines across the library and only 5 carry the source — all 5 in the skill that was asked to fix it.** The content-refresher lane cited its own 5 of 5 and then, applying F9-r11's rule to itself, measured the rest: **25 uncited surfaces outside its scope**, concentrated in `build/schema-markup-generator/SKILL.md` (×5), `references/validation-guide.md` (×5) and `commands/generate-schema.md` (×2), with more in `schema-decision-tree.md`, `schema-templates.md`, `content-structure-templates.md`, `ctr-and-social-reference.md`, `technical-audit-example.md`, `gap-analysis-frameworks.md`, `serp-feature-taxonomy.md` and `analysis-templates.md`. The claim is **true and sourced** — Google Search Central, 2023-08-08 — so this is not a correctness defect; it is that 25 surfaces assert it with nothing a reader can check, in a library whose own rule is that a claim travels with its source. Needs one sweep, not eleven edits. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the sourced fraction is worse than recorded, not better.** Re-measured at HEAD: **26 lines assert the government/health FAQ restriction across 13 files**, and **1** carries a URL or a *Search Central* naming within ±3 lines. Counting whole passages rather than lines, the full source (blog title + 2023-08-08 + URL) sits in `content-refresher` only — `SKILL.md:308`, `ai-overview-recovery.md:65-68`, `refresh-example.md:180-182`, `refresh-templates.md:263-266` and `:534`. Everywhere else the assertion carries a bare date: `schema-markup-generator/SKILL.md` ×6, `validation-guide.md` ×5, `commands/generate-schema.md` ×2, plus `schema-decision-tree.md`, `schema-templates.md`, `content-structure-templates.md`, `ctr-and-social-reference.md`, `technical-audit-example.md`, `gap-analysis-frameworks.md`, `serp-feature-taxonomy.md`, `analysis-templates.md` `[obs:2026-08-18 python census over build/ optimize/ monitor/ research/ cross-cutting/ commands/ references/, evals excluded, pattern 'FAQ' AND government/health within one line, source window ±3 lines at 5909552 — 26 lines, 1 sourced]`. The single sweep the row asks for has not been run. |
| 116 | **FIXED 2026-08-17 — `expectation-carrier-check.sh` had a FOURTH false-positive class its footer did not name: a quoted phrase that WRAPS a line in the skill source could never match.** The corpus is joined with newlines intact while an expectation quotes the phrase as one line, so `**A proxy with` / `no floor is not a filter**` (`gap-analysis-frameworks.md:367-368`) read as uncarried while being carried verbatim. Both multi-line quotes in the new content-gap expectations hit it. Fix is one line — collapse whitespace in `norm()` on both sides — and measured on that suite it removes 2 of 11 candidates, both hand-checked and found to be false positives. The three holes already in the footer (length cap, quote style, behaviour-blindness) are unaffected. **Recorded because the tool's own footer is the thing executors are told to trust**, and it was short by one. |
| 117 | **RULING NEEDED — content-refresher's own eval grades a template shape the skill deliberately no longer has.** `evals.json` e1 quotes `[X]/100 — [P] pts over [n] items ([item IDs])`, but finding 74's shipped fix moved item IDs **out** of the client table and into the operator fence. A run obeying the skill now fails the expectation; a run passing the expectation ships the family-8 violation the skill forbids. This is the F13 shape pointed the other way — not a skill teaching what its evals forbid, but an **eval enforcing what the skill fixed**. The candidate count is 13 before and 13 after the lane's edits, so the lane neither caused nor cleared it, and it correctly refused to change an instrument to suit a diff. Coordinator call: the expectation is rewritten to the shipped shape, or finding 74's fix is wrong. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and row 74's closure is what keeps it live.** The suite still quotes the pre-fix shape: `optimize/content-refresher/evals/evals.json` e1 expectation 6 requires each score to print *"in the template's own shape — `[X]/100 — [P] pts over [n] items ([item IDs])`"* and adds that *"a bare `[X]/100` with no points-and-items tally fails"*. The skill's client table at `SKILL.md:138-145` prints `[X]/100 — [P] pts over [n] items` with no IDs, and `:154` forbids them there in terms. A run obeying the skill still cannot satisfy the expectation as written `[obs:2026-08-18 python read of optimize/content-refresher/evals/evals.json e1 exp 6 and sed of SKILL.md:138-168 at 5909552]`. **This is a step-7 suite finding and outranks any pass rate on this suite.** Coordinator call still owed. |
| 118 | **content-gap: the rule says "on every row that uses the proxy" and the template a run copies gives that sentence no cell.** `SKILL.md:166` and `gap-analysis-frameworks.md:290` bind the non-independence disclosure per-row; `analysis-templates.md:430-436` assigns the *Volume* cell the proxy and its basis, the *Quick Win Score* cell the cancellation note, and stops. Same shape `3d0b592` diagnosed twice — the rule exists and the template gives it nowhere to land. The new expectation was deliberately written to accept the row **or a note the row's reader necessarily meets**, so it cannot fail a run for a placement the skill's own template cannot produce. If the rule is to bind strictly per-row, the template needs the cell first. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** The template now names the cells. `analysis-templates.md:481-497` states *"Where Search Demand was proxied, **four things ride the row itself**"*: the *Volume* cell (proxy and its basis), the *Quick Win Score* cell (*"demand and density legs cancel — read as Relevance + Effort"*), the same *Volume* cell again for a row scoped narrower than the floor's unit, and the *Factor scores* cell for the Density re-gloss. The row's own condition — *"the template needs the cell first"* — is met. Landed in `5909552`, whose message names findings 67/68/79 and not this row `[obs:2026-08-18 sed -n '481,497p' research/content-gap-analysis/references/analysis-templates.md; git log -S'four things ride the row itself' → 5909552]`. Residual precision, not a reopening: the disclosure travels as the *cancellation* note, so a row printing no Quick Win Score carries it only through the Factor-scores re-gloss. |
| 119 | **content-gap e4.6 is weaker than the skill it grades.** It accepts any volume figure "explicitly labeled estimated"; `SKILL.md:166` allows "estimated" only where the estimate has a stated basis, and otherwise requires an explained N/A. e1.6 already carries the strict form. The two are satisfiable together, so this is not an unsatisfiable pair — but **a run could pass e4.6 with the exact thing e1.6 fails**, which makes the suite's verdict depend on which eval happens to catch it. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged.** e4.6 still accepts *"any volume/difficulty figure in the analysis or handoff context is explicitly labeled estimated (no SEO tool is connected)"* with no basis requirement, while e1.6 carries the strict form (*"labeled estimated only where the basis of the estimate is stated… an 'estimated' number with nothing behind it fails"*) and e3.6 carries the Greek strict form. The suite's verdict still depends on which eval catches it `[obs:2026-08-18 python read of research/content-gap-analysis/evals/evals.json — expectations 1.6, 3.6, 4.6 at 5909552]`. |
| 120 | **The class behind 105 was spot-fixed, not swept — named here rather than left implied, per F9-r11.** 105's two behaviours were instrumented in the one suite that shipped them. The underlying class is **"a behaviour the skill states that no expectation grades"**, and no library-wide sweep has been run for it. `expectation-carrier-check.sh` cannot find it: it detects the opposite direction (an expectation with no carrier), and its own footer says it is blind to behaviour in either direction. So the class has **no instrument at all** — unlike the engine-claim class, which at least has a sweep. Closing it needs a dispatched sweep across 20 suites; until then the class stays open with its scope named as it is here. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and row 144's measurements are the current evidence for it.** The class still has no instrument: `scripts/` holds 20 scripts and none asks *does an expectation grade this stated behaviour*. `expectation-carrier-check.sh` still runs the opposite direction and says so in its own footer. Live instances measured this pass: 0 expectations mention the seven-field action contract across all 7 skills that carried it first, 0 mention the ownership screen across 4 skills, 0 mention `k of N` in alert-manager, and rank-tracker's suite has 0 prompt-level expectations `[obs:2026-08-18 ls scripts/; grep -co over each suite's evals.json at 5909552]`. |

| 121 | **PILOT, Phase 4 — the business is reachable through SIX properties and four URL grammars, not the two and three the brief assumed.** Found in search results: the main domain in three grammars (`/el-gr/…`, `/en/…`, and root-level `/faq/…` and `/locator/?product=`), the bare host with `.html` suffixes, `sanihellas.wordpress.com`, and **four brand microsites on separate domains** — `noboadvantage.gr`, `kullhaus.gr`, `meaco.gr`, `atlantic-heating.gr`. The microsites are deliberate, current and well-titled; the lane recommends **nothing** for them, which is the right call and worth recording, because a sweep that finds six properties is under pressure to call all six a problem. The `/en/` locale is asymmetric with `el-gr` — language-only against language-plus-region. |
| 122 | **PILOT — five distinct URLs return one identical title, across three URL grammars, both hostnames and both locales.** The title `SANI HELLAS αντιπρόσωποι Kullhaus, Meaco, Nobo, Atlantic` is served at `/el-gr/`, `/el-gr/contact/`, `/thermansi.html`, `sanihellas.gr/thermansi/thermopompoi.html` and `/en/nobo-convectors/`. **The cause was deliberately not asserted.** Four causes produce exactly this shape — legacy redirects, a catch-all serving homepage content, soft-404s, or an undifferentiated site-wide title template — and the lane weighted the fourth Likely on defensible evidence: two of the five are current in-structure pages, not legacy URLs, so at least part of it is title templating independent of routing. Two owner-run checks separate them in about two minutes. Also found alongside: indexed faceted URLs (one titled `9 - Sani Hellas`), an indexed commercial page titled only `sani`, three transliterations of αφυγραντήρες in slugs, and percent-encoded U+0301 combining accents in blog slugs. |
| 123 | **PILOT — the Meaco official-representative claim is contested in what search systems return, and this is the highest-value commercial finding in the pilot.** Three of four brand-plus-category associations resolve cleanly (Kullhaus, Nobo, Atlantic). Meaco does not: a generated summary asserted that a competing Greek retailer *"is recognized as an official representative/distributor of Meaco in Greece"*, with that retailer's Meaco product pages in the same result set. **Reproduced independently by the coordinator** on a differently-worded Greek query, which returned both parties described as official. What is established: a competitor sells Meaco, appears in the same results, and a search system produced prose crediting it with official status. What is **not** established, and was correctly left unresolved: who actually holds the appointment — that is contractual, no search result settles it, and the client knows it while we do not. The finding is not that anyone is lying; it is that **the claim is contested in AI output and this business is not winning it outright**, for a business whose commercial position rests on being the official channel. |
| 124 | **PILOT — the legacy WordPress property publishes contradicted business facts.** It carries **Πραξιτέλους 9, Αθήνα** while four other sources agree on **Κωστή Παλαμά 5-7, Νέα Χαλκηδόνα**, and foregrounds a brand absent from the current portfolio. The dilution here is **contradicted facts, not stolen rankings** — which is the more actionable framing, because it is a local/entity consistency problem with a concrete fix rather than a ranking-cannibalisation theory that would need evidence nobody has. |
| 125 | **PILOT — a measurement limit worth more than the findings it stopped: this search tool returns NO result counts at all.** The brief expected `site:` counts to be caveated as estimates; the honest position is one step further back, so **no index figure is reported anywhere in the assessment** and the check that would supply one is named instead. The lane also showed that **`site:` absence is not evidence of non-indexation here**: `site:sanihellas.gr faq OR locator OR en` returned zero on-domain results and the tool's own summary proposed the pages might not be indexed — while the lane had **already seen all three returned in earlier searches**. It withheld a health score for the same reason: nothing the scoring rubric scores was measurable without robots.txt, sitemap, canonicals, headers or CWV, and the rule says withhold entirely rather than score one sub-table. Swept afterwards: **no skill instructs an operator to use a `site:` result count, and none treats a `site:` miss as evidence of deindexation** — so this is a limit of the environment, not a defect in the library. |
| 126 | **PILOT, and it is good news: blog post 17851 ranks FIRST for «καλύτερος αφυγραντήρας για σπίτι 2026»**, ahead of the affiliate/review cluster (Testado, DropFix, best10, parathiro) that otherwise owns these queries. A separate AI answer named **Kullhaus alpha Q13L/Q20L and DDQ10L by model** while citing those same affiliate sites as its sources. Two things follow: the blog is working and **must be protected through any URL cleanup**, and the products are already surfacing in AI answers while the citation goes to intermediaries — which is precisely the gap GEO work closes. |

| 127 | **PR #9 — BOTH MERGE CONDITIONS FAIL. Reported to Sani; no merge, and conditions-met would not have been an authorization either.** **Sani ruled on 2026-08-17 that PR #9 is not to be merged at all (`SETTLED-RULINGS.md` M1), so this row is now the record of why, not an open question about whether.** (a) *Blind re-runs clean* — **NOT MET**, and not because a run came back dirty. **COUNT CORRECTED 2026-08-17, and the correction is this register's own F11 class landing in the register the F11 guard exists to keep clean.** The sentence stood here in the present tense: *"20 of 20 skills' latest blind record grades a version the tree has moved past. Zero are current."* It was true when written at `e4a7bfc`; **eleven blind records landed after it**, and it was left standing. A bare present-tense count in an append-only register falsifies itself the moment the thing it counts moves — which is exactly how this happened, so the replacement below carries its own as-of date and its own method. **Recount, as of 2026-08-17 at `fba166c`, method stated so it can be re-run: a record is *current* when the version it says it graded equals the skill's `metadata.version` at HEAD **and** `git diff <the record's own `repo_head_at_open`>..HEAD` over that skill's directory is empty** `[obs:2026-08-17 per-skill measurement over docs/loop/eval-baselines/blind-2026-08-17/*.json — 9 current, 2 stale, 9 skills with no record of that date]`. **9 of 20 current** (alert-manager · competitor-analysis · content-gap-analysis · domain-authority-auditor · geo-content-optimizer · internal-linking-optimizer · keyword-research · performance-reporter · rank-tracker); **2 stale by one version** (backlink-analyzer 4.2.0 → 4.3.0, 3 files moved by `fba166c`; technical-seo-checker 4.5.0 → 4.5.1, 2 files moved by `9e5cd43` **during its own grading**); **9 with no 2026-08-17 record at all** (content-quality-auditor · content-refresher · entity-optimizer · memory-management · meta-tags-optimizer · on-page-seo-auditor · schema-markup-generator · seo-content-writer · serp-analysis). **"9 of 20" is the flattering number and must not travel alone: three of the nine carry no usable clean result**, each by its own record's own words — `content-gap-analysis.json` is PROVISIONAL, its grader killed by a spend limit before reporting, its `grader_termination_notice` forbidding its rate being cited as a graded baseline; `rank-tracker.json` records `comparison_performed: false` and names e2.1 and e3.6 as the hotspots someone else must diff; `performance-reporter.json` records the comparison as `NOT PERFORMED — the required input was out of scope`. **So the honest figure is 6 of 20 with a current, author-confirmed, regression-compared record** — alert-manager, competitor-analysis, domain-authority-auditor, geo-content-optimizer, internal-linking-optimizer, keyword-research. Four further graders were mid-run when this recount was taken (content-quality-auditor, entity-optimizer, on-page-seo-auditor, seo-content-writer), so both numbers move again the same day; re-run the method rather than quoting either. The three the register closed on 2026-08-13 have all moved since (geo 4.4.2→4.4.11, alert-manager 4.3.2→4.3.4, content-refresher 4.3.2→4.3.7). Worst spread: seo-content-writer's record tests 4.2.4 against a shipping 4.6.0. Six suites' *instruments* also changed after the commit those runs executed from — two by expectation count, **four rewritten at constant count**, which is finding C4's shape at wave scale. So the records grade a question no longer asked, on a version no longer shipping. (b) *Independent review clean* — **NOT ASSESSABLE**: 229 commits, 320 files, 38,503 insertions, all 20 SKILL.md and all 20 eval suites touched, and — decisively — **four of the rulebooks a reviewer must check against do not exist on `main` at all**, so a reviewer would be checking the diff against rules that arrived in the same diff. Mode A's **BLOCK stands unwithdrawn**: it was answered, never withdrawn, and a verdict belongs to the reviewer, not the author of the fix. **11 commits touching 10 of 20 SKILL.md files have landed since the last answered round, unreviewed.** — **RECONCILED 2026-08-18 at `5909552` — OPEN as a record, and the recount is stale again in the direction the row predicted for itself.** Re-run of the row's own stated method at HEAD: **0 of 20 current**. Every one of the twenty `blind-2026-08-17` records now names a version the skill has moved past — alert-manager 4.4.0→4.6.0, backlink-analyzer 4.2.0→4.4.0, competitor-analysis 4.2.1→4.3.0, domain-authority-auditor 4.4.0→4.6.1, internal-linking-optimizer 4.5.0→4.6.0, keyword-research 4.5.0→4.6.0, meta-tags-optimizer 4.2.2→4.3.0, on-page-seo-auditor 4.4.0→4.5.1, performance-reporter 4.5.0→4.6.1, rank-tracker 4.3.0→4.4.0, schema-markup-generator 4.2.10→4.3.0, seo-content-writer 4.7.0→4.8.0, serp-analysis 4.3.7→4.4.0, technical-seo-checker 4.5.0→4.6.2, and six more whose SKILL.md carries no top-level `version` for the reader to compare `[obs:2026-08-18 python over docs/loop/eval-baselines/blind-2026-08-17/*.json vs each suite_path/SKILL.md at 5909552 — current 0 / stale 20]`. The row is a record of why PR #9 is not merged (ruling M1), so this is the count moving, not the verdict. |
| 128 | **The pre-push gate has never evaluated the diff PR #9 would merge, and does not pass on it.** Run as wired the gate passes — and says so itself: *"PASSED — but NOTHING WAS OUTGOING… this is not evidence about any commit; it is the absence of a subject."* Three of five legs scope to `@{upstream}..HEAD`, which is empty on a fully-pushed branch. Re-run against `origin/main` as the script's own text instructs: **claims-gate FAILS (25 failed, 82 warnings), commit-scope-check FAILS (13 failed)**, register-lock passes 229/229. **Both the alarming reading and the reassuring one are wrong**: seven of the 25 were hand-adjudicated and are dominated by false positives — `ADVERSARIAL-LAYER.md:49` is the lexicon's own *definition* matching itself, `:40-41` quotes a withdrawn overclaim with its correction named, and `WATCH-ITEMS.md:261` carries *"Historical status, superseded"* two lines up, a different spelling of the gate's accepted marker, which is finding 112's open-vocabulary defect in a second place. So "25 defects" is false and "the gate passes" is false. The true statement: **the register-integrity gate does not pass on the merge diff, nobody has ever run it against that base, and unlike `engine-claim-sweep.sh` it ships no adjudication list** — so its output is not yet actionable. Two scope failures are real: `67ecad7` is F14's founding `git add -A` signature, and `ac7436b` changes gate code without naming it. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and one half is closed.** The *"ships no adjudication list"* half is fixed: `scripts/claims-gate-allowlist.txt` exists at 28 lines. The scope half stands — `pre-push-gate.sh:33` still defaults `BASE` to `origin/main` while three legs scope to `@{upstream}..HEAD` (`:171-178`), so the empty-subject reading is still reachable `[obs:2026-08-18 wc -l scripts/claims-gate-allowlist.txt → 28; grep -n '@{upstream}' scripts/pre-push-gate.sh at 5909552]`. **Not re-run against `origin/main` by this pass** — the row's 25-failed / 13-failed figures are therefore unverified at HEAD and should not be quoted as current. PR #9 is ruled unmergeable (M1), which makes the rest of the row a record. |
| 129 | **The B1 closure number was measured on an instrument that no longer ships — and a 2×2 separates the two causes cleanly.** `OPEN-FINDINGS.md:227` records *"raw 173 → adjudicated → residual 46"* from a `git archive`, described as five families. That number **reproduces exactly** and is honest. But the shipping sweep has seven families, and the controlled comparison is: old script × old tree **46**; old script × HEAD tree **46** — *content added zero residual*; new script × old tree 66; new script × HEAD tree **60**, which is what ships. So **14 residual lines at HEAD were never inside the adjudicated 46** (P6 +7, adjudication-logic changes +7, P7 **+0** — reproducing finding 102's own measurement independently). This is finding 86's class applied to the register's headline sweep number: an instrument moved and no record said so. **The closure's direction survives contest** — the branch's content added no engine-claim residual — but the number needs its configuration stated beside it. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the gap is now larger than the row measured.** `OPEN-FINDINGS.md:227` still records *"raw 173 → adjudicated → residual 46"*. The shipping sweep at HEAD returns **raw 289 → residual 142** `[obs:2026-08-18 bash scripts/engine-claim-sweep.sh at 5909552 — '######## raw 289 → adjudicated → RESIDUAL: 142 ########']`. The row's point holds unchanged: the register's headline number needs its instrument configuration and its date beside it, or a reader compares two different instruments. |
| 130 | **The adversarial layer has now failed to obtain an independent second lane on two consecutive firings, and it has no enforcement mechanism.** `ADVERSARIAL-LAYER.md` §4 rule 2 requires the contrastive lane to run on a **different Claude tier**, as a **plain** subagent, without the Mode A checklist in context. Both firings — 2026-08-13 and 2026-08-17 — were the same agent, same tier, same context, and both **declared their own non-independence rather than concealing it**, which is the behaviour we want and is still not a second lane. A rule with no mechanism is a preference. Either the layer gets a dispatch path that can satisfy it, or §4 rule 2 should say plainly that the lane is self-contrast and be read accordingly. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged.** `docs/loop/ADVERSARIAL-LAYER.md` §4 rule 2 still reads *"The second lane runs on a DIFFERENT Claude tier from the primary Mode A reviewer's (subagent model override), both named explicitly at launch"* (`:232-236`), with no dispatch path and no enforcement leg in any gate; §4 rule 5 still states the standing limitation without conceding the lane is self-contrast. Neither of the two resolutions the row offers has been taken `[obs:2026-08-18 sed -n '226,250p' docs/loop/ADVERSARIAL-LAYER.md at 5909552]`. |
| 131 | **`expectation-carrier-check.sh` reports 184 candidate phrases demanded by a suite and absent from its skill — a number nobody has looked at.** That is the whole-tree figure at HEAD. Its own footer limits what it means (uncarried *vocabulary*, not uncarried *behaviour*, and the fourth false-positive class was only closed today), so 184 is an upper bound and some fraction are wrapped-line or paraphrase artefacts. But it is the same class as finding 117 — an expectation grading something the skill never states — and 117 was found by reading, not by this tool. **The number needs triage before it can be quoted either way.** — **RECONCILED 2026-08-18 at `5909552` — OPEN, number now 188.** The whole-tree figure at HEAD is **188** candidate phrases, not 184, and the footer's stated limits are unchanged (uncarried vocabulary, not uncarried behaviour; coverage 1 of 3 known instances) `[obs:2026-08-18 bash scripts/expectation-carrier-check.sh at 5909552 — '188 candidate phrase(s) demanded by a suite and absent from its skill's own text']`. No triage has been done, so the number still cannot be quoted in either direction. |

| 132 | **RULING NEEDED — `keyword-research`'s Greek module instructs an assertion that anti-slop family 9 forbids, and the placement rule is *derived from* the banned claim.** `research/keyword-research/references/greek-keyword-coverage.md:20` ("Diacritic normalization in search matching is reliable — no separate content needed"), `:42` ("adds no ranking benefit search engines don't already provide by normalizing accented and unaccented forms as equivalent") and `SKILL.md:185` state engine behaviour as fact with no primary source. Family 9 carries ruling R3 amendment 9a into Greek and bans exactly that shape on a client-read Greek surface. The bite is structural, not cosmetic: form (b) gets no placement **because** the engine normalises, so a family-9-compliant Greek deliverable cannot state the reason for its own central instruction. A pilot lane hit it while writing real Greek, rewrote its draft as a declared working model plus a client-runnable check, and reported that the wording was its own invention rather than a carried rule. Two resolutions and only Sani or a sourced primary settles which: a sourced carve-out for normalisation, or the Greek module restates these three lines as working assumptions. — **RECONCILED 2026-08-18 at `5909552` — OPEN, all three lines standing at their stated positions.** `greek-keyword-coverage.md:20` still reads *"Diacritic normalization in search matching is reliable — no separate content needed"*; `:42` still reads *"adds no ranking benefit search engines don't already provide by normalizing accented and unaccented forms as equivalent"*; the SKILL.md line has moved from `:185` to **`:165`** and still reads *"Diacritic normalization (a ↔ b) is reliable in search matching"* `[obs:2026-08-18 sed -n '18,22p;40,44p' research/keyword-research/references/greek-keyword-coverage.md; grep -n normaliz research/keyword-research/SKILL.md at 5909552]`. Neither resolution has been taken. Ruling still owed. |
| 133 | **FIXED 2026-08-17 — family 9's RANK step was catching half its own family, and the escaping half is the half a keyword deliverable writes.** Found by finding 132's lane and reproduced independently by the coordinator on six constructed variants: NET 6 of 6, RANK **3 of 6**. The three escapes were all *normalisation* claims — «ταιριάζει», «κανονικοποιούν», «αγνοεί» — none of them in the original eleven disposition stems, which covered the founding shapes («προτιμά», «παραθέτει», «εμπιστεύονται») and nothing else. Five stems added (`ταιριάζ`, `κανονικοποι`, `αγνο`, `κατατάσσ`, `φιλτράρ`) taking it to **6 of 6**; noise cost over the shipping skill tree is `ταιριάζ` 1 file and **0** for the other four [obs:2026-08-17 grep -rFil over research build optimize monitor cross-cutting references, LC_ALL=C.UTF-8]. This is §6's amendment requirement doing exactly what it was written for, one family later. |
| 134 | **`serp-analysis` has no defined behaviour for "no SERP data at all" — it asks and stops.** Its only manual-tier instruction is *"Ask the user to provide … SERP screenshots"* (`SKILL.md:107-113`). `keyword-research:120` carries the opposite and explicit rule (*"proceed immediately and list the stated assumptions … Ask clarifying questions only when genuinely blocked"*). With no equivalent, an autonomous run either stalls or improvises, and the pilot lane improvised a capture protocol that would differ every run. Note the Step-7 zero-factor rule added 2026-08-17 covers the **score** ("emit no number and no band") and behaved exactly as written — nothing covers the **report**. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** `serp-analysis/SKILL.md`'s manual tier still ends *"Proceed with the full analysis using provided data"* over a five-item *Ask the user to provide* list (`:107-115`), and no *nothing arrived* behaviour is stated. The only related line is `:200`, which governs the **figure** (*"the figure stays out"*) and not the report — the same split the row already excluded `[obs:2026-08-18 sed -n '105,116p' and grep -n on research/serp-analysis/SKILL.md at 5909552]`. |
| 135 | **`greek-shopping-surfaces.md` prints the numbers it forbids, in the copyable place.** Surface Map cells at `:13-14` carry *"~40.8M visits/mo"* and *"~6.9M visits/mo"*; the prohibition sits at `:17` in a bullet **below** the table, and `serp-analysis/SKILL.md:180` states those magnitudes are *"not client-quotable"*. This is the Value Rule's own founding mechanism — a model copies the fence, not the heading above it — applied to a table instead of a fence. Fix shape: mark the cells in-cell ("vendor estimate — not client-quotable"), not only underneath. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL.** The cells gained an in-cell **provenance** marker — `:13` now reads *"~40.8M visits/mo (vendor data — see [VERIFY] bullet below)"* and `:14` *"(same vendor snapshot)"* — but not the in-cell **prohibition** the row's fix shape named. *"not client-quotable"* still lives only at `research/serp-analysis/SKILL.md:206`, outside the copyable table, which is the founding mechanism the row cited `[obs:2026-08-18 sed -n '11,20p' research/serp-analysis/references/greek-shopping-surfaces.md; grep -n 'client-quotable' across both files at 5909552 — 1 hit, in SKILL.md]`. |
| 136 | **`keyword-prioritization-framework.md`'s zero-data path is near-non-discriminating and the file does not say so.** With the prescribed renormalisation (0.667/0.333), Business Relevance 5 contributes 3.335 before intent is read, so any core-offering keyword with intent ≥ 2 lands P0 (`0.667×5 + 0.333×2 = 4.001`). A relevance-5 keyword can only take five values — 3.7, 4.0, 4.3, 4.7, 5.0 — four of which are one band. The file's own worked example at `:56` (Relevance 5 / Intent 4 → 4.667 → P0) demonstrates it without noticing. The arithmetic is right; the instrument is blunt in exactly the state the section exists for, and the pilot lane had to write that caveat into a client deliverable itself. Same family as finding 34, one section over. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** `keyword-prioritization-framework.md:74` still carries the worked case *"Relevance 5, Intent 4 → `0.667×5 + 0.333×4 = 3.335 + 1.332 = 4.667` → rounded **4.7 → P0**"* with the renormalised weights at `:71`, and the file still says nothing about the instrument being near-non-discriminating on that path — a grep for `blunt` or `non-discriminating` returns nothing `[obs:2026-08-18 sed -n '50,60p' and grep -n on research/keyword-research/references/keyword-prioritization-framework.md at 5909552]`. |
| 137 | **RULING NEEDED — `competitor-analysis`'s Output Validation contains an item its own permitted state cannot satisfy.** `SKILL.md:285` requires *"Competitor strengths backed by measurable evidence (metrics, rankings)"*, while the manual-tier branch explicitly permits proceeding with no export and `:290` forbids any hard metric the source data lacks. In a zero-data run every strength must be Πιθανό/Υπόθεση with a named check — correct behaviour that fails the checklist item as written. Same unsatisfiable-pair shape as the row-label ruling of 2026-08-13, and it wants the same treatment: a scoped carve-out stated in the item, not a silent reword by whoever hits it. — **RECONCILED 2026-08-18 at `5909552` — OPEN; line moved `:285` → `:322`.** `competitor-analysis/SKILL.md:322` still reads *"- [ ] Competitor strengths backed by measurable evidence (metrics, rankings)"* with no carve-out, while `:329` still requires the Confirmed/Likely/Hypothesis (Επιβεβαιωμένο/Πιθανό/Υπόθεση) labelling that a zero-data run must use. The unsatisfiable pair is intact `[obs:2026-08-18 grep -n on research/competitor-analysis/SKILL.md at 5909552 — lines 322, 329]`. **This is a step-7 suite/skill finding**: it is a stated rule that its own permitted state cannot satisfy. Ruling owed. |
| 138 | **The 4-form Greek expansion rule is unconditional and does not scale, and form (d) has no destination test.** `SKILL.md:170` (*"Expand every Greek keyword to 4 forms — regardless of how it arrived"*) and the checklist at `greek-keyword-coverage.md:127` demand all four forms for every keyword; the pilot's ~37 targets means 148 mostly-mechanical cells. Separately, (d)'s placement at `:48` ("bilingual site sections, EN-language landing pages") is conditional on a surface that may not exist, while production of the form is mandatory. The lane expanded head terms and top modifiers, stated an inheritance rule, and flagged (δ) as a client decision — all three its own inventions, none carried by the skill. This one now interacts with the mandate: form (d) is the library's existing hook for the English-speaking audience, so a scaling rule for it is no longer optional. — **RECONCILED 2026-08-18 at `5909552` — OPEN; lines moved.** `SKILL.md:144` (was `:170`) still reads *"Expand every Greek keyword to 4 forms — regardless of how it arrived"*, and `greek-keyword-coverage.md:50` (was `:48`) still gives (d) a conditional placement — *"bilingual site sections, EN-language landing pages, tourist/expat/international-facing content"* — while production of the form stays mandatory. No scaling rule and no destination test were added `[obs:2026-08-18 grep -n on research/keyword-research/SKILL.md and references/greek-keyword-coverage.md at 5909552]`. |
| 139 | **No stated convention for marking a table as a keyword-list surface.** `SKILL.md:128` confines Greeklish to "keyword-targeting tables only" and family 4 exempts "keyword-export strings on explicitly keyword-list surfaces" — neither says how a surface declares itself explicit. The pilot lane invented a marker line. Without a convention a grader cannot distinguish an exempt table from a family-4 hit, which makes the exemption unenforceable in both directions. — **RECONCILED 2026-08-18 at `5909552` — OPEN; line moved `:128` → `:127`.** `keyword-research/SKILL.md:127` still confines Greeklish to *"keyword-targeting tables only"* and `anti-slop-ruleset.md:184` (family 4) still exempts *"keyword-export strings on explicitly keyword-list surfaces"*. Neither states how a surface declares itself explicit, so the exemption is still unenforceable in both directions `[obs:2026-08-18 grep -n on research/keyword-research/SKILL.md and build/seo-content-writer/references/anti-slop-ruleset.md at 5909552]`. |
| 140 | **Minor — `keyword-research` Step 10 forbids a keyword appearing first in a crosswalk, so with no store cities the local section must be dropped entirely, and the drop-and-name behaviour for that case is stated nowhere in the skill.** The lane borrowed it from the handoff carrier. Cheap fix: state it in Step 10. — **RECONCILED 2026-08-18 at `5909552` — OPEN; line moved to `:277`.** The crosswalk rule still reads *"a keyword target may not appear for the first time in a crosswalk — give it an analysis row (explained N/A allowed) first"*, restated at `:312`. No drop-and-name behaviour for the no-store-cities case is stated in Step 10 or anywhere else in the skill `[obs:2026-08-18 grep -n crosswalk research/keyword-research/SKILL.md at 5909552 — lines 277, 312; grep -ci drop → 3, none of them this case]`. |
| 141 | **A dispatched lane did not inherit the coordinator's web-search capability, and the brief asserted it would.** `docs/loop/PILOT.md` records *"WebSearch is not subject to that refusal. Measured this session"* — true of the coordinator's session; the capability did not travel to the subagent, which had Read/Grep/Glob/Bash/Write only. The lane checked rather than assumed, and reported that `www.google.com`, `html.duckduckgo.com`, `search.brave.com` and the client host all return `curl: (56) CONNECT tunnel failed, response 403` while `api.github.com` returns 200 — so the refusal is host-scoped, not a dead network. Two items of its brief were not executable as written and it said so instead of fabricating a SERP. **The rule this yields**: a capability measured in the coordinator's session is not a capability of the lanes it dispatches, and a brief that asserts one must say where it was measured. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** `docs/loop/PILOT.md:167` still reads *"**WebSearch is not subject to that refusal.** Measured this session"* with no statement of where it was measured or that the capability does not travel to a dispatched lane. The rule the row yields is carried nowhere `[obs:2026-08-18 sed -n '160,180p' docs/loop/PILOT.md at 5909552]`. |
| 142 | **Family 10 landed with a real defect on its first run, which is why it is recorded rather than assumed clean.** `research/competitor-analysis/references/positioning-frameworks.md:32` carried a **worked positioning example offered as a model to imitate** reading *"is the content optimization platform that ensures every article ranks on page one"* — a guaranteed search outcome in copy a client would publish. Fixed the same day, with the benefit clause rewritten to name a capability and the reasoning stated in place so the next editor does not restore it. This is the recurring shape the library has recorded since finding 29: a skill demonstrating in an example the behaviour its own rules forbid, where the rule text is right and the template beside it is what gets copied. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, verified.** `positioning-frameworks.md:32` now reads the capability form (*"…shows exactly which terms and subtopics the current page-one results already cover, because it grades a draft against real-time SERP data…"*) and `:35` keeps the retracted wording quoted in place as the reason, so the next editor cannot restore it blind `[obs:2026-08-18 grep -n on research/competitor-analysis/references/positioning-frameworks.md at 5909552 — lines 32, 35]`. Recorded here as a closure because the row was a record of a same-day fix, not an open item. |

| 143 | **A protocol still in force in a register contradicted a binding reference the hour it landed, and nothing in the repo would have caught it.** `PILOT.md` §3 is the pilot's written AI-citation sampling protocol. The new `references/ai-visibility-measurement.md` governs the same measurement, and §3 disagreed with it on three points that each cost a real finding: it recorded cited **domains** (so "they cited our comparison page instead of the product page" is unrecordable), it took **one capture** per query × engine (so week-to-week movement cannot be told from run-to-run variance), and it collapsed mention/citation/recommendation into **one yes/no** — which is precisely the shape of the pilot's own headline observation, where products were named by model while every citation went to third parties. Under the old columns that logs as "our property cited: n" and the finding vanishes. §3 reconciled the same day and now points at the reference as governing. **The class**: a new binding reference does not sweep the registers for protocols it supersedes, and no gate leg compares them — `validate-tracking` reads versions and links, `claims-gate` reads drafting integrity, neither reads for contradiction. This is finding 120's class (a stated behaviour nothing grades) in the register half of the repo rather than the skill half. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL: the instance is closed, the class is not.** `PILOT.md:322` now reads *"§3 was upgraded 2026-08-17 to match `references/ai-visibility-measurement.md`, which is now the governing specification — read it, not this section"*, with all three disagreements written out as what the old protocol got wrong. **The class stands**: no gate leg compares a register protocol against a binding reference. `scripts/` holds 20 scripts — `validate-tracking`, `claims-gate`, `commit-scope-check`, `register-lock`, `fence-nesting-check`, `expectation-carrier-check`, `citation-divergence-check`, `engine-claim-sweep` and the rest — and none of them reads for contradiction between two documents `[obs:2026-08-18 sed -n '318,335p' docs/loop/PILOT.md; ls scripts/ at 5909552]`. |

| 144 | **THE LARGEST DEBT THIS WAVE CREATES: 15 of 20 skills changed and not one eval suite was re-run.** Every lane reported its suite untouched and unrun, which was the correct scope discipline and leaves the debt whole. Worse than the re-run gap is the **carrier gap in the other direction**: the wave added gradable behaviours that no expectation grades — `rank-tracker`'s entire prompt-level half (its suite has no prompt-level expectation at all), `geo-content-optimizer`'s new requirement that each change name which of the three jobs it serves, `alert-manager`'s `k of N` rule, the seven-field contract in four auditors, and the ownership screen in three research skills. This is finding 120's class arriving at wave scale, and `expectation-carrier-check.sh` cannot see it by construction — it detects an expectation with no carrier, not a carrier with no expectation. Every lane that changed a template preserved verbatim the strings its existing expectations quote, so the *existing* suites should not regress; nothing establishes that, because nothing was run. A Mode B pass across all 15 is owed before merge, and the suites need extending before that pass means anything. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and the two halves went opposite ways.** **The re-run half is CLOSED as written**: `docs/loop/eval-baselines/blind-2026-08-17/` holds **20 records, one per suite**. But the debt regenerated at a higher version — **0 of 20** records now grades the shipping version (see row 127's re-measurement). **The carrier half is fully OPEN and none of the five named gaps closed**: `rank-tracker`'s suite has **0** prompt-level expectations (the 5 `prompt` hits in its `evals.json` are the schema's own `"prompt"` key, one per eval); `alert-manager` has **0** expectations mentioning `k of N`; the seven-field action contract is mentioned by **0** expectations across all seven skills that carry it (content-quality-auditor, domain-authority-auditor, backlink-analyzer, performance-reporter, on-page-seo-auditor, technical-seo-checker, entity-optimizer); the ownership screen by **0** across content-gap-analysis, keyword-research, internal-linking-optimizer and geo-content-optimizer `[obs:2026-08-18 python over each suite's evals.json at 5909552 — counts as stated]`. |
| 145 | **RULING NEEDED — an unsourced benchmark now sits in the same file as rows written explicitly without one.** `monitor/performance-reporter/references/kpi-definitions.md` carries a pre-existing **AI Citation Rate** with `Good Range: >20%` and `Warning: <5%`, no source. The new prompt-level rows in the same file state that no published benchmark exists and none is supplied from memory. The lane resolved only the naming collision (different populations, kept separate, never blended) and deliberately did not touch the numbers, because the same figures feed `alert-manager`'s AI-citation-rate ladder, which is **row 7 of the open threshold decisions already awaiting Sani**. Deleting or re-banding them is a threshold decision, not an edit. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged.** `monitor/performance-reporter/references/kpi-definitions.md:289-296` still carries **AI Citation Rate** with `Good Range \| >20% of monitored queries`, `Warning \| <5% or declining trend` and `Data Source \| ~~AI monitor`, with no source, in the same file as rows that state no published benchmark exists `[obs:2026-08-18 sed -n '286,300p' monitor/performance-reporter/references/kpi-definitions.md at 5909552]`. Still a threshold decision, still tied to `alert-threshold-guide.md`'s open row 7. |
| 146 | **Register drift created by this wave and left visible rather than papered over.** Adding four prompt-level rows moved `alert-threshold-guide.md`'s open-decisions count from nine to thirteen, per that table's own "the count is the table" rule. Finding 65 and `VERSIONS.md:134` still quote 8 and 5. The guide now carries a sentence saying a register quoting nine is superseded rather than in disagreement — which is a stopgap, not a fix; the two registers still need reconciling to one number. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL.** The guide's own half is correct and self-consistent: `alert-threshold-guide.md:495` heads *"Open threshold decisions — **thirteen rows**"* over a table numbering 1, 2, 3-6, 7, 8, 9, 10, 11, 12, 13, with the *"The count is the table"* rule and the superseded-count sentence at `:501-506`. **The reconciliation has not happened**: finding 65 in this file still reasons from **9** and **8**, and the `VERSIONS.md:134` pointer the row cites **no longer resolves** — line 134 is blank at HEAD and no count of 5 survives anywhere in `VERSIONS.md` `[obs:2026-08-18 sed -n '495,520p' monitor/alert-manager/references/alert-threshold-guide.md; awk 'NR==134' VERSIONS.md → empty; grep -n 'five open\|five rows\|5 rows' VERSIONS.md → no match, at 5909552]`. That dead pointer is itself a live instance of row 165's class. |
| 147 | **Four unlabelled template fences in the two Optimize skills, same class as finding 60.** `on-page-seo-auditor/references/audit-templates.md` (Step 11) and `audit-example.md`, `technical-seo-checker/references/technical-audit-templates.md` (Step 9) and `technical-audit-example.md` — each is a ```` ```markdown ```` fence full of bracket tokens with **no in-fence `<!-- SKELETON … -->` label**, while their own Step 10 operator siblings carry one and both cross-cutting auditors label theirs. Found by the lane whose additions inherit the gap without creating it, and correctly reported rather than fixed outside its brief. Sweep these with 60 and 63 in one pass. — **RECONCILED 2026-08-18 at `5909552` — OPEN, all four sites.** No in-fence label exists in any of them: `audit-templates.md`'s Step 11 fence opens at `:365`, `technical-audit-templates.md`'s Step 9 fence at `:372`, `audit-example.md` at `:13`, `technical-audit-example.md` at `:18`. The Step 10 operator siblings still carry theirs (`audit-templates.md:304`, `:342`) `[obs:2026-08-18 grep -n 'SKELETON\|ILLUSTRATIVE FILL\|OPERATOR BLOCK' and grep -n '^```' over all four files at 5909552 — 2 hits, both OPERATOR BLOCK, both in audit-templates.md]`. **One correction to the row**: the two `*-example.md` fences are not *"full of bracket tokens"* — they carry invented values (`example.com`, `cloudhosting.example`, dated 2025-01-15 / 2024-09-15), so the label the closed list of three gives them is `ILLUSTRATIVE FILL`, not `SKELETON`. The defect (no in-fence label) is the same; the prescribed label is not. |
| 148 | **`cross-cutting/domain-authority-auditor/SKILL.md:134`** — a repo path (`references/cite-domain-rating.md`) inside the Step 1 **client** skeleton fence. Pre-existing finding-80 class (a run handle in client prose), untouched by the lane that found it. Its sibling was fixed in this wave: `technical-seo-checker/references/technical-audit-example.md:146` carried *"ruling R3 amendment 9a"* inside the client report fence, and the substantive statement was kept while the handle went. — **RECONCILED 2026-08-18 at `5909552` — OPEN, at the same line.** `cross-cutting/domain-authority-auditor/SKILL.md:134` still carries `references/cite-domain-rating.md` inside the Step 1 fence, which opens at `:122` with `<!-- SKELETON … -->` and closes at `:143` `[obs:2026-08-18 git show 5909552:cross-cutting/domain-authority-auditor/SKILL.md \| grep -n cite-domain-rating → :134; fence bounds :122-:143]`. **In-flight caution**: that file was under uncommitted edit by a parallel lane during this pass (`git status M`, version 4.6.1 → 4.6.2 read minutes apart), so this verdict is pinned to `5909552` and not to the working tree. |
| 149 | **`domain-authority-auditor`'s eval expectations and its own template disagree on the Top-5 field labels.** The suite grades "Finding / Evidence / Impact / Fix"; the template says "Current / Potential gain / Evidence / Confidence / Action". The lane preserved the template's labels rather than resolving it in either direction, which is right — resolving it changes either a shipped template or a graded expectation, and that is a call with a reviewer in it. Same shape as finding 117. — **RECONCILED 2026-08-18 at `5909552` — OPEN.** The suite still grades *"the Top 5 improvements each carry Finding/Evidence/Impact/Fix"* (`evals/evals.json`, e1 expectation 6 and the e1 `expected_output`), while `SKILL.md:284-291` still prints *Name / Current / Potential gain / Evidence / Confidence / Action / Owner / Effort / Depends on / Done when / Risk*. `ade5457` added a partial mapping for two of the four — `SKILL.md:238`, *"The **Action** line *is* the action field and the **Potential gain** line *is* the expected-impact field"* — but *Finding* and *Fix* still have no named counterpart `[obs:2026-08-18 python read of cross-cutting/domain-authority-auditor/evals/evals.json; sed -n '281,292p' SKILL.md at 5909552]`. Same shape as 117, and unresolved for the same reason. |
| 150 | **Three more R3-9a engine-disposition claims, named by the lane that fixed two others and correctly declined these.** `monitor/backlink-analyzer/references/link-quality-rubric.md:96` (*"Footer and sitewide links are devalued by search engines"* — sitting 12 lines below a sibling claim the library already `[VERIFY]`-tagged for exactly this), and `cross-cutting/entity-optimizer/references/knowledge-graph-guide.md:337`, `:386` (*"tells search engines 'this is the same entity'"*, *"search engines can confidently map to Knowledge Graph entries"* — where the same skill's SKILL.md tip 2 has already withdrawn and `[VERIFY]`-tagged the identical mechanic, so skill and reference currently disagree) and `:467` (an unsourced `Timeline: 1-3 months`). One R3-9a sweep, not four spot fixes. — **RECONCILED 2026-08-18 at `5909552` — CLOSED, all four claims.** `link-quality-rubric.md:111-116` now carries a `[VERIFY — 2026-08-17]` block withdrawing *"Footer and sitewide links are devalued by search engines"* and stating *"The 15% weight does not rest on it"*. `knowledge-graph-guide.md:384` withdraws the same-entity claim as *"an asserted engine mechanic"*; `:441-442` withdraws *"a consistent entity graph that search engines can confidently map to Knowledge Graph entries"* citing R3 amendment 9a; `:523` replaces the unsourced `1-3 months` with *"**unknown, and do not state one** — the '1-3 months' here had no source"* `[obs:2026-08-18 sed -n '105,125p' monitor/backlink-analyzer/references/link-quality-rubric.md; grep -n on cross-cutting/entity-optimizer/references/knowledge-graph-guide.md at 5909552 — lines 384, 441-442, 523]`. The one R3-9a sweep the row asked for was run (`794724d`, *R3-9a sweep — ten breaches, six of which no list had*). |
| 151 | **A document whose subject is stating N beside a figure got its own N wrong.** `prompt-set-v1`'s header claimed 40 prompts (26 Greek, 14 English) over tables holding 44, so the capture totals derived from it — 360, and a 153-capture minimum subset — were both wrong. Caught by the Greek editor pass, not by the author, and not by any check: **no instrument in this repo counts a table and compares it to the count asserted above it**, which is the cheapest possible verification and would have caught this in one line. The corrected file states counts per cluster so the total is checkable rather than asserted. Worth a small guard: an asserted count adjacent to an enumerable table is a greppable shape. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL: the instance is closed, the guard is not built.** `prompt-set-v1-2026-08-17.md:30-36` now states counts per cluster (A 24, B 18, C 8 = **50** rate-feeding, plus 2 verification rows excluded from every rate), and the file holds **52** ID-prefixed prompt rows, which is 50 + 2 `[obs:2026-08-18 sed -n '26,50p' docs/loop/pilot/prompt-set-v1-2026-08-17.md; grep -oE '^\| *[A-Z]+[0-9]+' \| wc -l → 52 at 5909552]`. The row's own proposal is unbuilt: `scripts/` holds 20 scripts and none counts a table and compares it to the count asserted above it, so the stated absence (*"no instrument in this repo counts a table…"*) is still true. |
| 152 | **Process, recorded because it was disclosed rather than concealed: a lane ran `git stash` / `git stash pop` during a six-lane wave.** That stashes the **entire working tree**, including five other lanes' in-flight, uncommitted work — a whole-wave data-loss hazard taken to get an unmodified-tree reading of one advisory script. The lane disclosed it unprompted, said it was a mistake and would not repeat it, and verified afterwards. The coordinator re-verified independently rather than accepting the report: all previously-committed files byte-identical to HEAD, all four lane-authored new reference files present at 201/175/155/153 lines, stash list empty, zero conflict markers. **No content was lost.** The rule this yields belongs in the lane brief, not in a lane's judgement: **no `git stash`, `git checkout --`, `git restore` or `git clean` in a multi-lane wave** — a lane needing a clean-tree reading asks the coordinator, who can serve it from `git show HEAD:path` without touching anyone's working tree. — **RECONCILED 2026-08-18 at `5909552` — OPEN as an uncarried rule (F17 shape).** The rule the row yields — *no `git stash`, `git checkout --`, `git restore` or `git clean` in a multi-lane wave* — exists **only in this row**. It is in no lane brief carrier, no `docs/loop/` register and no gate: the sole other repo mentions are `commit-scope-check.sh:89` (a fixture harness using `git clean` outside the repo) and `git-push-guard.sh:27-31` (arguing `git stash push` must *not* be blocked as a push) `[obs:2026-08-18 grep -rn 'git stash\|git clean\|git restore' docs/loop/*.md .claude/ scripts/*.sh at 5909552]`. **Live corroboration from this pass**: five files were modified in the working tree by parallel lanes while this reconciliation ran, so the hazard the row names was present again today. |

*Rows 153–167 opened 2026-08-17 from the blind grading wave of that date (11 records under `docs/loop/eval-baselines/blind-2026-08-17/`) plus the register pass that followed it. Every count below was re-measured against the tree at `fba166c` rather than carried from a record's prose; where a record is the only source, the row says so.*

| 153 | **A shipped scoring instrument states a formula that is wrong in the understating direction, and four of its own eval expectations now encode the error — so the suite would defend it.** `domain-authority-auditor` prices every Top-5 improvement as *recoverable points × dimension weight*. The skill's own definitions make dimension score = points ÷ (10 × s) × 100 where **s is the number of *scored* items**, so a recovered raw point moves the CITE Score by **(10 ÷ s) × weight** — the stated formula is that expression with the (10 ÷ s) factor silently set to 1. It is exact only when s = 10 and understates by 10/s otherwise. **Measured on this run's own fixtures: a Partial in a six-item Citation dimension is worth 2.0833 weighted points and the formula prints 1.2500 — a 40% understatement**; the same eval's five Top-5 gains sum to 11.0833 under the correct method, which closes exactly against the report's own 74.1389 → 85.2222 projection, and to 9.75 under the stated one, which does not. **The file contradicts itself**: §6's worked case refuses the shortcut and recomputes both endpoints, getting 0.83 where §5's formula predicts 2.50. Five statements carry the wrong form — `references/score-arithmetic.md` §5 and §8 rule 5, `SKILL.md` :227 and :281, `references/example-report.md`:59 — and every worked example in the skill scores all 40 items, which is why it has never surfaced. **Consequence, not theoretical: the Top-5 sort order inverts** — the 2.083 item ranks second and the naive 1.25 ranks below four Identity Partials — so the skill's headline promise of impact-sorted improvements fails on its own arithmetic precisely in the thin-data reports the N/A machinery exists to serve. **Scope measured, not assumed**: `recoverable points` returns five hits repo-wide, all in this skill; `content-quality-auditor` runs the identical rescale with no gain shortcut written down, so it has the exposure without the wrong formula. **The suite is the half that would have defended the defect**: four expectations — e1.7, e2.6, e3.6, e4.7 — encoded the wrong formula verbatim (*"…times its dimension's stated weight"*), so a report doing the arithmetic correctly could be graded as failing. e3.6 was additionally **self-contradictory**: the same expectation requires every aggregate to recompute from the report's own tables and every combined-gain claim to equal the sum of its parts, which a report obeying its own gain clause cannot do whenever a dimension is rescaled — two clauses of one expectation, jointly unsatisfiable. Neither the grader nor this register pass edited the suite. **STATE-OF-TREE CAVEAT, and it is the reason this row is dated rather than present-tense: everything above was measured against `fba166c`, and a concurrent skills lane repaired both halves in the working tree while this row was being written** — `score-arithmetic.md` §5 now reads `× (10 ÷ n) ×`, and all four expectations now read *"times 10 divided by that dimension's scored-item count"* `[obs:2026-08-17 uncommitted working tree, git status M on SKILL.md, score-arithmetic.md, example-report.md and evals/evals.json]`. **Nothing here is closed by that**: the repair is uncommitted, unreviewed and unexercised by any blind run, and F13-r3's standing rule is that a carrier written in response to a ledgered failure is unproven until a blind run exercises it. Re-check at HEAD before acting, and close this row only on a blind result. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL, and it closes on the row's own stated condition, which is unmet.** The carrier repair the row saw uncommitted is now **committed**, in `e06073a`: `score-arithmetic.md` §5 `:110-127` and §8 rule 5 `:228` carry `recoverable points × (10 ÷ n) × weight` with the understatement table; `SKILL.md:283` and `:286` show all three factors; `example-report.md:59` and its five Top-5 entries print `× (10 ÷ 10 scored) ×`; and all four expectations (e1, e2, e3, e4) now read *"times 10 divided by that dimension's scored-item count times its dimension's stated weight"*, with e3's self-contradiction resolved in the expectation's own text (*"which is why the two clauses are one requirement here"*). **Not closed**: the row says *close this row only on a blind result*, and no blind run has exercised it — the newest record is `blind-2026-08-17/domain-authority-auditor.json` at **4.4.0** against a shipping **4.6.1** `[obs:2026-08-18 git log -S on score-arithmetic.md and evals.json → e06073a; python read of the four expectations; skill_version_under_test 4.4.0 vs SKILL.md version 4.6.1 at 5909552]`. F13-r3's rule governs. |
| 154 | **REGRESSION, do-not-merge class — `alert-manager`, three expectations, each of which had held across two prior blind runs and two skill versions.** From `blind-2026-08-17/alert-manager.json`, comparing against `blind-2026-08-13` and `blind-2026-08-11`; all three expectation texts are **byte-identical across all three records** (27 of 29 are, and neither of the two rewritten ones is on this list), so each move is attributable to the deliverable or to the skill and never to rewording. **e2.6** — the prior run carried both missing Criticals; this one adds the security and manual-action rows and no site-down/5xx row anywhere, on a scripted scan of eight terms returning zero, in a deliverable that itself names the 08/07 hosting outage as the log's only true positive. Carrier unchanged and present at `alert-configuration-templates.md`:165. **e3.1** — the prior run called the «άμμος γάτας» reading probable; this one asserts it as fact four times in bold and negates the fixture's own definition of «—» one clause after quoting it. This is the escalation of a defect the 08-13 record logged *outside* the graded set as D6, which is the argument for logging those at all. **e4.4** — the prior run satisfied the citation-loss response path inside a labelled operator block; this one has no operator block and zero hits for `content-refresher`, `recovery playbook`, `AI Overview recovery` or `refresh`, while the rule is carried three times over in the skill. **Suite headline is 24/29 = 0.8276 for the third consecutive run across three skill versions while six expectations moved underneath** — for this suite the rate is measuring nothing and only the per-expectation diff carries information. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and no tree evidence can close it.** `blind-2026-08-17/alert-manager.json` is still the newest record for this suite, and in it **e2.6 = FAIL and e3.1 = FAIL** — the same record the row was written from. The skill has moved 4.4.0 → 4.6.0 with no re-run, so the regression is neither confirmed nor cleared `[obs:2026-08-18 python read of docs/loop/eval-baselines/blind-2026-08-17/alert-manager.json — e2.6 FAIL, e3.1 FAIL; skill_version_under_test 4.4.0 vs SKILL.md 4.6.0 at 5909552]`. |
| 155 | **REGRESSION, do-not-merge class — `keyword-research`, two expectations, every confounder eliminated by diff.** From `blind-2026-08-17/keyword-research.json` against `blind-2026-08-10b/keyword.json` (the only prior blind record for this suite). **e3.3** — byte-identical expectation, byte-identical prompt, unchanged fixture, and `git show 5d51741:research/keyword-research/SKILL.md` diffs **empty** against HEAD over the whole of Step 4. The prior deliverable produced the 4-form crosswalk table and its grader quoted the header; this one produces zero occurrences of `EN equivalent`, no four-form header, and no Greeklish or EN form for three of the four Greek demands. Two blind runs, one artefact, one absence, identical instructions. **e4.1** — byte-identical expectation and prompt; the prior run labelled blended queries with compound labels built from the skill's own four categories, this one writes `Mixed — job name…` and `Mixed — a download…`, neither naming any of the four. **Recorded because the grader overturned itself**: its first draft filed e4.1 as an instrument defect on the strength of `keyword-intent-taxonomy.md`:324 printing *"Mixed -- check SERP"* in a Correct-Classification column; the baseline read settled it, because a blind executor found the compliant path on the same reference and the same text. The :324 ambiguity is real, mitigates, and does not excuse. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unproven either way.** `blind-2026-08-17/keyword-research.json` is still the newest record; the skill has moved 4.5.0 → 4.6.0 (`d1d4a24`/`6316edf` carried the action contract into it) with no re-run. A carrier change without a re-run cannot close a regression `[obs:2026-08-18 blind-2026-08-17/keyword-research.json skill_version_under_test 4.5.0 vs research/keyword-research/SKILL.md 4.6.0 at 5909552]`. |
| 156 | **REGRESSION, do-not-merge class — `internal-linking-optimizer` e4.4, deliverable-attributable, no skill-side cause found.** From `blind-2026-08-17/internal-linking-optimizer.json` against `blind-2026-08-10b/linking.json`; same method both runs (blind executor, informed split grader), wording **byte-identical** — extracted from `git show da16893:…evals.json` and from HEAD and compared directly. The prior deliverable put the orphan fix first; this one fixes the 404 in Phase 1 and fixes the orphan in **no** phase, on the stated ground that an anchor brief adds no links (*"New links added: **0**"*). The priority order still stands at `link-architecture-patterns.md`:366 and the orphan-fix phase is still the first phase of `linking-templates.md`'s Step 7 template at :211, both unchanged in substance across 4.0.2 → 4.5.0, and **nothing added at 4.1.0–4.5.0 tells a writer that an anchor-scoped brief must not add links**. The same executor did the opposite in e1 and e2 of the same run, fixing the identical orphan with two named inbound links. **Do not quote the 0.9286 → 0.9333 headline**: the denominators are different question sets. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unproven either way.** Newest record is still `blind-2026-08-17/internal-linking-optimizer.json`; skill moved 4.5.0 → 4.6.0 with no re-run `[obs:2026-08-18 record skill_version_under_test 4.5.0 vs optimize/internal-linking-optimizer/SKILL.md 4.6.0 at 5909552]`. See row 167 — this suite is also the one whose `e1.6` id was reused, so an id-only comparison of its records mis-maps. |
| 157 | **REGRESSION, do-not-merge class — `backlink-analyzer` e1.6, and the suite has four failures that survived two independent cold runs.** From `blind-2026-08-17/backlink-analyzer.json` against `blind-2026-08-10/backlink.json`; e1.6 is byte-identical across both (25 of 28 are; the three that moved are marked VOID and none is on this list), so the move is deliverable-attributable. The prior run's e1.6 defects were two loose superlatives, correctly unscored; this deliverable asserts three things about the fixture that the fixture contradicts, in one three-line passage at `e1.md`:247-248. **The larger finding is beside it**: e2.1, e4.3, e5.1 and e5.3 failed in **both** runs, under two independent blind executors seven days apart, on byte-identical text, with the skill unchanged in the ways they turn on — and two of them failed the *same way* both times (e2.1 left exactly one flagged domain carrying the worksheet's own description in place of a named rubric element; e5.1 omitted the 68-of-74 backlink quantification both times while getting the 10-domain count). A defect that survives two independent cold runs is a carrier problem or an expectation problem, not executor variance. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unproven either way.** Newest record is still `blind-2026-08-17/backlink-analyzer.json`; skill moved 4.2.0 → 4.4.0 (`fba166c`, `6c14e35`) with no re-run `[obs:2026-08-18 record skill_version_under_test 4.2.0 vs monitor/backlink-analyzer/SKILL.md 4.4.0 at 5909552]`. |
| 158 | **REGRESSION, do-not-merge class — `geo-content-optimizer` e2.1, and the record states its own mitigation rather than hiding it.** From `blind-2026-08-17/geo-content-optimizer.json` against `blind-2026-08-13/geo.json`; e2.1 is not in commit `3d0b592`'s diff, so the expectation text is byte-identical and the move is attributable to the deliverable or the skill. The 08-13 page opened on a term-initial definition at word 1; this one opens on the fixture's own «Στην κατηγορία …» frame with the definition displaced to roughly word 118 under its own H2 — the prior run put the fixture frame *second*, this one puts it *first*. **Honest qualification, from the record**: the 08-13 record called e2.1 *"THE WEAKEST PASS IN THIS SUITE"* and said a strict reader could have graded it the other way, so part of the gap is grader strictness on an expectation both records flag as ambiguous. What is not strictness is the position of the definition, which differs in fact. **The counterweight belongs in the same row**: the four expectations added from the 08-13 record's own out-of-set defects paid on their first run — e3.8 caught defect D2, and the two operator-block expectations record a real repair (the dangling cross-reference was in four of five deliverables at 08-13 and in zero here). — **RECONCILED 2026-08-18 at `5909552` — OPEN, unproven either way.** Newest record is still `blind-2026-08-17/geo-content-optimizer.json` (29/2/33 = 87.88%); the skill has moved on and no re-run exists `[obs:2026-08-18 python read of docs/loop/eval-baselines/blind-2026-08-17/geo-content-optimizer.json summary at 5909552]`. |
| 159 | **Three effort-band vocabularies across the seven skills that carry the action contract, and the carrier supplies the one its own consumers are forbidden to use.** `references/action-output-contract.md` §1 row 5 prescribes **`S / M / L`**. Measured across the 20 skills at `fba166c`: `on-page-seo-auditor` and `technical-seo-checker` use `S / M / L`; `content-quality-auditor` and `domain-authority-auditor` use **`Quick / Medium / Strategic`** — and with *different* band definitions from each other (<30 min / 1-2 h / needs planning versus <1 week / 1-4 weeks / 1-3 months); `entity-optimizer` uses **`Low/Medium/High`**; `backlink-analyzer` and `performance-reporter` name the field and fix no vocabulary. **The bind is stated in the skill itself**: `domain-authority-auditor/SKILL.md`:236 says *"no second vocabulary is invented"*, :352 says *"the Quick/Medium/Strategic effort bands are the only priority and effort vocabularies used"*, and :374 names `action-output-contract.md` as the authority for this field — so an executor that follows the routing writes `S / M / L` and breaches :352, and one that follows :236 departs from its named carrier. All five of that skill's deliverables followed the SKILL.md body (`S / M / L` occurs zero times in them). **Nothing grades it**: no expectation in that suite mentions effort bands, so the suite is blind to which vocabulary ships. Needs a ruling on which vocabulary is canonical, not five spot fixes. Re-measured against the working tree while writing this row: **the conflict is unchanged** — the carrier still prescribes `S / M / L` at `references/action-output-contract.md`:30 and the two auditors still mandate `Quick / Medium / Strategic`. Line numbers inside `domain-authority-auditor/SKILL.md` are as at `fba166c` and that file is moving; grep the quoted sentences rather than the numbers. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and the wave widened it rather than settling it.** The carrier still prescribes `S / M / L` (`references/action-output-contract.md:30`) and the two auditors still mandate `Quick / Medium / Strategic` and still forbid a second vocabulary (`domain-authority-auditor/SKILL.md:238` *"no second vocabulary is invented"*, `:354` *"the Quick/Medium/Strategic effort bands are the only priority and effort vocabularies used"*; `content-quality-auditor/SKILL.md:344` the same). Re-measured across all 20 skills at HEAD: **17 on `S / M / L`, 2 auditors on `Quick / Medium / Strategic`, `entity-optimizer` on `Low/Medium/High`, `alert-manager` and `backlink-analyzer` fixing none** — and **`content-gap-analysis` now carries BOTH `Low/Medium/High` and `S / M / L`**, an internal conflict that did not exist when the row was written `[obs:2026-08-18 grep -oh over each skill's SKILL.md + references/*.md at 5909552]`. Ruling still owed. **In-flight caution**: `references/action-output-contract.md` was under uncommitted edit by a parallel lane during this pass, and its working-tree `:30` already reads differently (*"A band on this deliverable's own scale… `S / M / L` where the skill states no scale of its own (§8)"*), so re-check before ruling. |
| 160 | **Two orderings for the same list, one of which the other's skill explicitly forbids.** `references/action-output-contract.md`:109 states *"Actions are ordered by **expected impact ÷ effort**"*. `cross-cutting/entity-optimizer/SKILL.md`:243 states the opposite shape — *"impact on entity recognition first, then ascending effort … Effort breaks ties; it never outranks impact, and it is never multiplied by it"* — which is lexicographic, not a ratio, and which forbids exactly the reordering a ratio produces (a Medium-impact/Low-effort action rising above a High-impact/High-effort one). **The literal words do not collide** — the carrier divides where entity-optimizer bans multiplying — **and the produced orders do**, so a reader reconciling the two files gets no signal that they disagree. Measured: `expected impact ÷ effort` in the carrier and five skills, `weighted gain ÷ effort` in the two auditors (a defensible specialisation — gain *is* their impact), lexicographic in `entity-optimizer` alone. — **RECONCILED 2026-08-18 at `5909552` — OPEN; the entity-optimizer line moved `:243` → `:247`.** `references/action-output-contract.md:109` still reads *"Actions are ordered by **expected impact ÷ effort**"* and `cross-cutting/entity-optimizer/SKILL.md:247` still reads *"Effort breaks ties; it never outranks impact, and it is never multiplied by it"* `[obs:2026-08-18 git show 5909552:references/action-output-contract.md \| grep -n 'expected impact ÷ effort' → :109; grep -n 'never multiplied by it' cross-cutting/entity-optimizer/SKILL.md → :247]`. Same in-flight caution as row 159: the carrier is under uncommitted edit and its ordering line has already moved to `:114`. |
| 161 | **The seven-field action contract reached 7 of 20 skills; 13 do not carry it, and 10 of those mention it nowhere at all.** Measured at `fba166c` by grepping every skill tree for the carrier's path and for its field vocabulary (`risk-if-done-wrong`, `owner-role`, `acceptance criterion`). **Carried in the SKILL.md body (7)**: content-quality-auditor · domain-authority-auditor · entity-optimizer · backlink-analyzer · performance-reporter · on-page-seo-auditor · technical-seo-checker. **Pointer only, from a reference file, with no rule in the SKILL.md body (3)**: content-gap-analysis (`ownership-screen.md`:130) · geo-content-optimizer (`ai-visibility-targets.md`:199) · internal-linking-optimizer (`ownership-linking.md`:114). **No mention anywhere (10)**: keyword-research · competitor-analysis · serp-analysis · seo-content-writer · meta-tags-optimizer · schema-markup-generator · content-refresher · rank-tracker · alert-manager · memory-management. Root `CLAUDE.md` states the rule as binding on *"every recommended action"* with no skill carve-out, so 13 skills emit recommendations against a rule they are never shown — ledger **F17**'s exact shape. **Distinct from finding 144**, which records that the contract is *ungraded* where it did land: three graders reported that independently this wave (backlink-analyzer *"a deliverable that shipped bare bullet recommendations would score identically"*; performance-reporter *"8 of 8 behaviours UNGRADED"*; technical-seo-checker *"a full SKILL.md rule block … the suite is blind to whether they do"*). Reach and coverage are two different gaps and both are open. — **RECONCILED 2026-08-18 at `5909552` — CLOSED. The contract reached 20 of 20.** Every skill's `SKILL.md` body now carries the seven-field rule, not just a pointer — verified by reading the rule text in the previously-uncovered ones, e.g. `research/keyword-research/SKILL.md:279-281` and `:323-324`, `cross-cutting/memory-management/SKILL.md:246-248` and `:274`, `monitor/alert-manager/SKILL.md:182` and `:233`, `research/content-gap-analysis/SKILL.md:170` and `:219-229` `[obs:2026-08-18 grep -c action-output-contract over all 20 SKILL.md → 2 or 3 each; grep -n 'risk if done wrong\|acceptance criterion\|Done when' on the four newest at 5909552]`. Landed across `6316edf`/`d1d4a24`, `aa804a8`/`cea7400` and `046b384`/`ade5457`. **Its sibling gap is not closed and is now the whole of the finding**: row 144 — 0 expectations grade any of it. |
| 162 | **A reference file instructs the auditor to put a watch-item handle and a repo path into client prose.** `cross-cutting/domain-authority-auditor/references/greek-eshop-compliance.md`:88 reads *"If the client raises the topic, state that verification against primary sources is pending (W2) and refer them to their accountant/lawyer"*, in a passage that also names `docs/loop/WATCH-ITEMS.md`. A watch-item ID is a run handle and `docs/loop/` is a repo path; Reader Test clause 1 as extended on 2026-08-13 (finding 80) bans both from client prose, and anti-slop family 8 lists `docs/loop/` among its own nets. **The executor got it right and the file did not**: the blind deliverable stated the substance without the handle and recorded the choice in its operator block (*"Neither the watch-item handle nor any repo path appears in the client-facing text"*). **Nothing in the suite grades it** — the covering expectation requires the pending statement and the referral and is silent on the handle — so the next executor may simply follow the reference. The reason it is a finding rather than a typo is that a rule the library states in three places was contradicted in the one place the writer is routed to. **Measured at `fba166c`; a concurrent skills lane rewrote the passage in the working tree during this pass** — the instruction now states the substance in the client's own terms and adds a *Where the watch-item handle may appear* paragraph scoping the handle to operator surfaces `[obs:2026-08-17 uncommitted working tree, git status M on greek-eshop-compliance.md]`. Uncommitted and ungraded, so the row stays open: verify at HEAD, and note that the suite still cannot see this class either way. — **RECONCILED 2026-08-18 at `5909552` — CLOSED.** `greek-eshop-compliance.md` §6 now reads *"If the client raises the topic, the answer is the substance in their own terms and nothing else: verification against primary sources is still pending… their accountant or lawyer is the person to settle it. **No handle, no register name, no repo path in that sentence.**"*, under a *Where the watch-item handle may appear* paragraph scoping `W2` and `docs/loop/WATCH-ITEMS.md` to operator surfaces and citing the Reader Test clause 1. Committed in `e06073a` `[obs:2026-08-18 sed -n '86,98p' cross-cutting/domain-authority-auditor/references/greek-eshop-compliance.md at 5909552]`. **Not a silent fix**: `e06073a`'s subject is about the potential-gain formula, but its body names this defect in full (*"Second defect, same skill: greek-eshop-compliance.md section 6…"*). The row's other clause holds — the suite still cannot see this class either way. |
| 163 | **`geo-content-optimizer`'s screening guard classifies every clean screen as unscreened, because `grep` exits 1 when it works.** `SKILL.md`:302 and `references/geo-score-arithmetic.md`:447 both state *"a screen that exits non-zero has not run, and a screen that has not run is UNSCREENED, never clean"*. Measured at this shell, GNU grep 3.11: match → exit **0**, clean no-match → exit **1**, real error → exit **2** `[obs:2026-08-17 grep -c on a two-line fixture: alpha→0, gamma→1; grep on a missing file→2]`. A screen that finds nothing — the outcome the guard exists to certify — exits 1, so the rule as written cannot distinguish a clean screen from a broken one, which is the exact distinction it was added to make. All five deliverables of the blind run recorded exit 1 beside *"screened, nothing surfaced"*, reporting honestly in §9's own prescribed words while quietly declining to apply :302's literal rule; one correctly recorded a genuine exit-2 abort as UNSCREENED and re-ran it in Python. **Repair is one clause** — *"exit status 2 or above"*, or *"any status other than 0 or 1"* — and §9 already knows it, since its own worked example names status 2 for the collation abort. The defect is that the rule generalised from that example to "non-zero". **Adjacent to finding 81 and not covered by its fix**: 81 corrected §9's *locale* claim and left this clause untouched. No expectation covers it. — **RECONCILED 2026-08-18 at `5909552` — OPEN at HEAD; line moved `:302` → `:319`.** `build/geo-content-optimizer/SKILL.md:319` still reads *"a screen that exits non-zero has not run, and a screen that has not run is UNSCREENED, never clean"*, restated at `:344`, and `geo-score-arithmetic.md:447` still carries the same sentence `[obs:2026-08-18 git show 5909552:build/geo-content-optimizer/SKILL.md and :references/geo-score-arithmetic.md — lines 319, 344, 447]`. **In flight**: a parallel lane was editing both files uncommitted during this pass, and the working-tree text at `geo-score-arithmetic.md:450` already reads *"…that exits non-zero has not run*, and sent every non-zero status to UNSCREENED. But `grep`…"* — i.e. the repair appears to be in progress. **Do not close this row from that**: it is uncommitted, unreviewed and covered by no expectation, exactly as the row records. |
| 164 | **The ledger has a duplicate F13 recurrence number, and it was found by the pass that had to pick the next one.** `FAILURE-LEDGER.md` carries **two** entries labelled *F13 Recurrence 3* — the 2026-08-10 informed-versus-blind carrier entry and the 2026-08-17 lift-exclusion entry, the latter recording *"Recurrence: F13 → 3"* and a `FLIP: F13-r3 -- none` trailer. Recurrences 3, 4 and 5 were already taken (2026-08-10 ×2, 2026-08-11), so the 2026-08-17 entry is the **sixth** wearing the third's number. Consequence beyond tidiness: `F13-r3` is cited elsewhere in the ledger and now resolves to two different mechanisms, and the ledger's own rule 3 makes the recurrence count a quarterly loop-KPI — a count that double-books a slot understates itself. **Not renumbered by this pass**, because ledger rule 4 makes entries append-only and the colliding entry's FLIP trailer is already in git history; the 2026-08-17 recurrence added by this pass is numbered **7** and leaves label 6 unused with the reason written into the entry, rather than labelling a seventh instance "6". A human decides whether to renumber or to leave the gap. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged.** `docs/loop/FAILURE-LEDGER.md:644` still heads *"F13 Recurrence 3 — 2026-08-10 · a carrier that passes informed and fails blind…"* while the 2026-08-17 lift-exclusion entry at `:1988` still records *"- **Recurrence**: F13 → 3."* with its `FLIP: F13-r3 -- none` trailer at `:1990`. The pass-added entry is still numbered 7 with label 6 deliberately unused (`:2251`) `[obs:2026-08-18 grep -n 'F13 Recurrence\|F13 → ' docs/loop/FAILURE-LEDGER.md at 5909552 — collision at :644 and :1988]`. **In flight**: `FAILURE-LEDGER.md` was under uncommitted edit by a parallel lane during this pass; verdict pinned to `5909552`. A human still decides renumber-or-gap. **RESOLVED 2026-08-18, and the pointers in the note above are superseded by the very fix they describe.** The reconciling lane wrote them against `5909552`; the renumber then grew `FAILURE-LEDGER.md` by 43 lines and moved every one. At HEAD the moved entry is **F13 Recurrence 6** — heading `FAILURE-LEDGER.md:1971`, counter `:2033` (`F13 → 6`), trailer `:2035` — with prior claim left to the 2026-08-10 carrier entry at `:644`, which keeps r3. Label 6 was already reserved for it in prose by the Recurrence 7 entry. **Four citations were deliberately NOT updated and are named in the ledger**: one commit trailer in immutable history, and three frozen 2026-08-17 blind records that cite `F13-r3` for the moved entry's mechanism — editing a graded run record to fit a later renumber would be rewriting evidence. `docs/loop/KPI.md`'s 2026-08-17 row was updated (`F13 r3/r7` → `r6/r7`) `[obs:2026-08-18 post-renumber census over every F-entry — no gaps, no duplicates; 51 citations read in context, 3 reclassified on a second read]`. **This row's own note is why bare line pointers are row 165's subject: two lanes, one file, both correct alone.** |
| 165 | **A bare line-number pointer in the live registers is more often wrong than right — measured, not suspected.** The 35-pointer census check (g) has reported for a week was worked through on 2026-08-17: 27 anchor-tagged, 8 refused and referred. **Of the 27, only 12 were correct as written**: 14 needed the line number refreshed because the target had drifted, and 1 named a path that does not exist at the repo root and could never have resolved. Largest drift **+153 lines** (`kpi-definitions.md` CWV thresholds, :300-306 → :453-455, where the old span had landed on an unrelated AI-citation block); next **+78 twice**, on PIPELINE's stage-3 `CLAUDE.md` pair — the **second** silent drift of that same pair, the first being the case that motivated extending check (g) beyond `VERSIONS.md` in the first place. **One target moved during the pass itself**: a settled-ruling pointer read at :259 was at :258 by the time the check ran, moved by a concurrently-running skills lane. The census counted only whether a token was present, never whether the number was, so it never reported this. Detail and the eight refusals are recorded in `GATED-ITEMS.md` under the check (g) scope-extension bullet. — **RECONCILED 2026-08-18 at `5909552` — PARTIAL: the anchored half is healthy, the referred half is untouched.** All **37** anchor-tagged pointers verify at HEAD and **0** have drifted, including the eight re-anchored by `a4ea447`. The **8 refusals stand un-anchored** and are named: `docs/loop/SETTLED-RULINGS.md:141` and `docs/loop/GATED-ITEMS.md:263, 264, 268, 290, 630, 767, 773` `[obs:2026-08-18 bash scripts/reanchor-pointers.sh at 5909552 → 'intact: 37 \| would re-anchor: 0 \| refused: 0'; bash scripts/validate-tracking.sh → '(g) 8 un-anchored file:line pointer(s)' listing those lines, and 'all 37 anchor-tagged … verified']`. The measured ratio (12 of 27 correct as written) stands as a historical finding about the class, and row 146 supplies a fresh instance of it: the `VERSIONS.md:134` pointer inside this register no longer resolves. |
| 166 | **The blind corpus encodes an editor-pending verdict two different ways, and a mechanical differ reports a verdict move that did not happen.** The 2026-08-11 and 2026-08-13 alert-manager records encode the e3.5 editor slot as `passed: false` in their per-expectation arrays while their prose and their summaries treat it as editor-pending and count it in neither passed nor failed; the 2026-08-17 record encodes it as `verdict: EDITOR-PENDING` with `passed: null`, matching what all three summaries already do. A differ reading only the `passed` field therefore reports e3.5 as having moved FAIL → not-FAIL between 08-13 and 08-17. **It did not move** — it has been editor-pending in every record, awaiting the same binding-editor report. This is ledger **F16**'s two-schemas-in-one-corpus problem one level down, at expectation granularity rather than summary granularity, and it needs a coordinator decision on which encoding the corpus standardises before any cross-record diff is automated. — **RECONCILED 2026-08-18 at `5909552` — OPEN, unchanged, and re-verified rather than assumed.** The three records still encode the alert-manager e3.5 editor slot two ways: `blind-2026-08-11/alertmanager.json` and `blind-2026-08-13/alertmanager.json` carry `{'id': 'e3.5', 'passed': False}`, while `blind-2026-08-17/alert-manager.json` carries `{'id': 'e3.5', 'verdict': 'EDITOR-PENDING', 'passed': None}` `[obs:2026-08-18 python walk over the three records at 5909552]`. **Corroborated one level up**: `scripts/eval-corpus-report.sh` now refuses a single pooled figure for the same reason — *"MIXED EDITOR-SLOT CONVENTION"*, 32 suites uncounted against 15 counted, 0.8993 vs 0.9221. The coordinator decision the row asks for has not been taken. **CLOSING RE-CHECK, same day**: a parallel lane began normalising the corpus **while this pass ran** — `blind-2026-08-11/alertmanager.json`, `blind-2026-08-13/alertmanager.json` and `blind-2026-08-17/geo-content-optimizer.json` all move `"passed": false` (+ `"status"`) → `"passed": null` + `"verdict": "EDITOR-PENDING"`, and `INSTRUMENT-CHANGES.md` gains *"## 2026-08-18 — the editor-pending slot was encoded two ways; normalised. NOT an instrument change"* `[obs:2026-08-18 git diff -U0 on those three records and on docs/loop/eval-baselines/INSTRUMENT-CHANGES.md, uncommitted working tree over 5909552]`. **The verdict above stands at HEAD and this note does not close it**: the work is uncommitted and unreviewed. Verify at the next commit before closing. **CLOSED 2026-08-18 — the note above says OPEN because the work was uncommitted when it was written; it is in this commit.** Canonical encoding is `verdict: "EDITOR-PENDING"` with `passed: null`, settled by the corpus against itself rather than by preference: all ten records already carry the `editor_slot_convention` object F16-r1(d) mandates, and one names the field outright. **10 records, 13 entries normalised** — well past the two this row named. **No summary moved**, asserted rather than claimed: every `summary`/`totals` object deep-diffed against HEAD (0 changed), a per-file guard refused any change whose leaf was not `passed`/`status`/`verdict`, and `eval-corpus-report.sh` is byte-identical before and after `[obs:2026-08-18 eval-corpus-report.sh — 47 records, as-recorded 1259/1400, slot-counted 1291/1400, identical pre and post]`. **A THIRD shape is reported and NOT normalised**: two 2026-08-10b records use `passed: true` + `editor_pending: true` and disagree about whether it counts in `summary.passed`, leaving one record whose array does not reconcile with its own summary. Changing it is a re-grade worth ±3.5 points, so it stays open as F16-r1(d)'s unresolved question. |
| 167 | **An expectation id was reused for a different question, so comparing two records of the same suite by id alone mis-maps it.** In `internal-linking-optimizer`'s prior tree `e1.6` was the no-fabrication expectation; at HEAD `e1.6` is the suggestion-payload expectation and the no-fabrication question is the new `e1.7` (text similarity of old-e1.6 to new-e1.6: **0.04**; to e1.7: near-identical, differing only by replacing a `~~web crawler data, ~~analytics` enumeration with *"stated in the report's own words"*). Suite grew 28 → 30. The grader caught it and said so in terms — *"ANY READER COMPARING THE TWO RECORDS BY ID ALONE WILL MIS-MAP THIS ONE"* — which is the only reason it is known. **The general defect: expectation ids are positional, so an insertion silently renumbers every id after it and no record states which question an id held.** Nothing in the repo prevents this and nothing detects it; the neighbouring suites are unaudited for the same shape. — **RECONCILED 2026-08-18 at `5909552` — OPEN, and nothing records the remap.** At HEAD `internal-linking-optimizer` e1.6 is the *Suggestion payload* expectation and e1.7 is the *No fabrication (ledger F3 guard)* expectation, with the suite at 30 expectations across 5 evals — exactly the state the row describes. `docs/loop/eval-baselines/INSTRUMENT-CHANGES.md` (127 lines) carries **no** entry for this suite and no record of the id remap; its only `e1.7` mention is content-gap-analysis's append `[obs:2026-08-18 python read of optimize/internal-linking-optimizer/evals/evals.json → 7/7/6/6/4 = 30; grep -n 'internal-linking\|e1.6\|e1.7' docs/loop/eval-baselines/INSTRUMENT-CHANGES.md at 5909552]`. The general defect — positional ids, nothing preventing or detecting a renumber — is unaddressed. **CLOSING RE-CHECK, same day**: a parallel lane added, uncommitted, an `INSTRUMENT-CHANGES.md` entry headed *"## optimize/internal-linking-optimizer — the id `e1.6` was reused for a different question (2026-08-10)"*, carrying a DIRECTION paragraph, the records on either side, and an **Owed** clause that keeps the general defect open (*"expectation ids are positional, an insertion…"*) `[obs:2026-08-18 git diff -U0 docs/loop/eval-baselines/INSTRUMENT-CHANGES.md, uncommitted working tree over 5909552]`. **That falsifies the sentence above — but only in the working tree.** The verdict is pinned to HEAD; re-check at the next commit, and note the lane's own Owed clause keeps the class half open even then. **CLOSED 2026-08-18 by documentation, not renumbering — the note above predates the entry.** The id is `e1.6`, changed at `ebc1ca1`; similarity to its former question is 0.04, and the old question now lives at `e1.7`. **Renumbering was refused**: three records cite `e1.6` by id, so a renumber moves the harm one step later. An `INSTRUMENT-CHANGES.md` section now carries it with the DIRECTION, which is the part a reader gets wrong — comparing `e1.6` by id reads PASS → PASS and looks stable, while the false movement lands one row up at `e1.5` (FAIL → PASS). **Two corrections to this row's own framing**: a third record grades the old instrument and is the most exposed, recording verdicts as positional arrays with no ids at all; and the 08-10b record provably graded the 28-expectation instrument, committed 16m43s before the split. |

*Rows 168–178 are the eleven grader-attributed regressions of the 2026-08-17 blind wave that had reached no register — `on-page-seo-auditor` 4, `content-quality-auditor` 2, `content-refresher` 2, `serp-analysis` 2, `schema-markup-generator` 1. The other eight are rows 154–158. Nineteen were attributed across ten of the eighteen compared suites; eight had been swept, and the eleven below arrived after the sweep. Each row is written from its own record under `docs/loop/eval-baselines/blind-2026-08-17/` rather than from a summary count.*

***The evidentiary grade in each row is load-bearing, not decoration.*** *Two grades, and the test between them is one question: is the prompt leg attached to the failing expectation, or asserted across the whole suite?* **Grade A** — *the record states, for that expectation itself, that its expectation text is byte-identical, that its own prompt is identical, and that the depended-on skill region was diffed.* **Grade B** — *the same material confounders eliminated, usually programmatically (prompts, fixtures and every shared expectation text compared index by index), but prompt identity asserted suite-wide (*"all five prompts byte-identical"*) and never attached to the failing item.* **Measured across these eleven: 7 grade A** (on-page-seo-auditor 4, serp-analysis 2, schema-markup-generator 1) **and 4 grade B** (content-quality-auditor 2, content-refresher 2). No row below is void-on-rewording: every one of the eleven sits on expectation text the record establishes as unchanged.*

***A third grade exists, and it is not in these eleven — it is in the eight that were swept first.*** *Applying the identical test to all nineteen:* **9 grade A, 5 grade B, and 5 whose regression block carries no prompt statement of any kind** — `alert-manager`'s three (row 154), `internal-linking-optimizer`'s one (row 156) and `geo-content-optimizer`'s one (row 158). *Those five eliminate expectation-text drift rigorously and never address the prompt as a confounder at all. Nothing here disturbs those rows and none of the five is thereby doubted — the point is that the corpus has three evidentiary tiers rather than two, and the thinnest tier is entirely inside the set that was registered first, which is the opposite of what a reader of rows 154–158 would assume* `[obs:2026-08-17T17:30Z python scan of regressions_vs_baseline across the 20 records for a per-item prompt field versus a suite-level-only prompt statement — 9 per-item, 5 suite-level, 5 neither]`.

| # | Item |
|---|---|
| 168 | **REGRESSION, do-not-merge class — `on-page-seo-auditor` e1.5, and its confounder is named rather than buried.** From `blind-2026-08-17/on-page-seo-auditor.json` against `blind-2026-08-10/onpage.json` (29/30 = 96.67%); like-for-like on the 30 shared expectations the suite went 96.67% → 86.67%, −10.0 points. **Evidentiary grade A.** **What moved**: the prior deliverable proposed replacement alt text for `/uploads/IMG_2847.jpg` and a filename to go with it (`rebase-onpage-out-e1.md`:253); this one declines in terms — *"**This audit cannot draft the text for image 1**: it has no alt, no caption and a camera-generated filename, so nothing in the source says what the photograph shows, and inventing a description would put a false statement on the page"* (`e1.md`:365-368, repeated at :625) — and no replacement alt for image 1 appears anywhere in the file. **The confounder is real and the grader stated it instead of hiding it**: the governing instruction is byte-identical (`audit-templates.md` still carries *"Add alt text to image [X]: \"[suggested alt text]\""*, line 156 then, 237 now), but the *reinforcing demonstration* was removed — `audit-example.md`'s Quick Win 1 lost its concrete model — in the same window the anti-fabrication carriers were tightened. **So the skill deleted the example and sharpened the prohibition, and the executor read the pair as a ban.** The task is satisfiable without fabricating and the baseline proved it: its own e3 proposed the alt *and* said it could not see the photograph in one breath. This run kept the caveat and dropped the proposal — and applied its own standard to image 2 (`e1.md`:368-370) and not to image 1. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `optimize/on-page-seo-auditor/SKILL.md:352` now requires *"every image with a missing, empty or non-descriptive alt carries a drafted replacement string with its confirm-before-publishing condition attached to it — a per-image 'cannot be drafted' is a last resort and names who writes it"*.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 169 | **REGRESSION, do-not-merge class — `on-page-seo-auditor` e3.5, the same defect in Greek in the same run.** Same record and baseline as row 168, **evidentiary grade A** on the same legs. The prior Greek deliverable proposed «Συνεδρία θεραπευτικής άσκησης στο φυσικοθεραπευτήριο Κίνησις στο Περιστέρι» for `DSC0042.jpg` (`rebase-onpage-out-e3.md`:248); this one writes «**Για την πρώτη δεν μπορούμε να προτείνουμε κείμενο**: δεν έχει ούτε alt, ούτε λεζάντα, ούτε περιγραφικό όνομα αρχείου … και μια περιγραφή στην τύχη θα ήταν ψευδής δήλωση μέσα στη σελίδα» (`e3.md`:368-371, repeated at :639) and proposes only for `gymnastirio.jpg`. **Kept as its own row rather than folded into 168 because they are two graded expectations in two evals and each closes separately** — but they are one class with one cause, in two languages, and a fix aimed at either that leaves the deleted demonstration in place will leave the other standing. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** the same carrier as row 168 — `optimize/on-page-seo-auditor/SKILL.md:352`, which is language-independent.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 170 | **REGRESSION, do-not-merge class — `on-page-seo-auditor` e5.1, and the carrier moved the *other* way.** Same record and baseline, **evidentiary grade A**. The clause is *"Evidence quotes at least one stuffed sentence verbatim"*. The prior run carried the stuffed paragraph in full (`rebase-onpage-out-e5.md`:359); this one describes it instead — *"80 words containing \"solar panel cleaning\" nine times"* (`e5.md`:459-462) — and a scripted substring scan of the whole deliverable against the 447-character stuffed paragraph returns a longest verbatim span of five words. **No mitigating confounder, and this is the one shape carrier drift cannot explain**: `SKILL.md` *gained* the quote-discipline rule between the two runs (*"The audit's own Evidence follows the same rule: quote the page verbatim from the HTML or content in front of you, never from memory or reconstruction"*, :136-137, added at `f4ac116`, after the baseline). The instruction was strengthened and compliance fell. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `optimize/on-page-seo-auditor/SKILL.md:352` now requires *"A finding about particular words on the page quotes those words in its Evidence"*, alongside `:136`'s verbatim rule.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 171 | **REGRESSION with the grading call stated in the open — `on-page-seo-auditor` e2.6, a new behaviour that misfired in one audit of three.** Same record and baseline, **evidentiary grade A**. The clause requires every number to trace to the fixture or be marked N/A or estimated. `e2.md`:201-202 prints *"Flesch-Kincaid grade 9.1 (Flesch reading ease 56.9), computed on the 438 words of body prose — 29 sentences"*; ground truth on `service-page-clean-en.md` is 459 visible words, 386 in paragraphs alone, and of five tokenizations tried none yields 438 and no sentence rule yields 29, while the line says *"computed on"*. **The mechanism is not a moved verdict on comparable content**: both 2026-08-10 deliverables declined to compute a readability index at all, this run computes one in all three English audits, and it is exact in e1 (381 words / 24 sentences, dead on) and reconciles in e5 — **only e2's base is wrong, so the new capability is right two times in three**. **The grader recorded that this verdict could defensibly have gone the other way**, so the coordinator can overturn it without re-reading the file: FAIL rests on the clause's headline with a word count and a sentence count inside the parenthetical's own list; PASS rests on reading that parenthetical as scoping the clause to page-element counts, in which case the defect moves to the out-of-set list unchanged. Everything else e2.6 asks for traces exactly, and every fabrication probe returns nothing. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** no carrier addressing the untraceable readability figures was found at HEAD; `8e35e30` reworked band endpoints and a truncating fence in this skill, not this clause. **Carrier NOT found** — this one has no located repair. **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 172 | **REGRESSION, do-not-merge class — `content-quality-auditor` e4.4, one interior word dropped from a Greek evidence quote.** From `blind-2026-08-17/content-quality-auditor.json` against `blind-2026-08-10/quality.json` (28/28 = 100%); like-for-like on the 28 shared expectations, 100% → 92.86%. **Evidentiary grade B** — the material confounders were eliminated programmatically (5/5 prompts equal, 5/5 `files` arrays equal, all 28 shared expectation texts equal index by index, the fixture untouched since 2026-08-09, and the nearest skill surface `SKILL.md`:343 present at 4.2.1 and at 4.5.0) but **prompt identity is asserted suite-wide and never attached to this expectation**, which is one step weaker than rows 168–171 and is the only reason the grade is not A. `e4.md`:141 quotes «δεν θα σταθούμε στις λεπτομέρειες»; the fixture reads «δεν θα σταθούμε **όμως** στις λεπτομέρειες» — one interior word gone, no ellipsis, in an evidence cell — while `e4.md`:137 quotes the same sentence correctly. **The class has a history in this exact suite**: paraphrase-as-quote on Greek fixture evidence cost the 2026-08-09 informed baseline its only two points, the 2026-08-10 blind run closed it by quoting full forms, and it has now recurred at a later skill version. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `cross-cutting/content-quality-auditor/SKILL.md:163` now puts the elision rule inside the item tables: *"**A shortened quote marks the cut with `…`, or it is not presented as a quote at all** — an interior word dropped inside the quotation marks changes what the page is recorded as saying"*, explicitly *"in every language… Greek guillemets « » are quotation marks and carry the identical requirement"*, plus per-row *"copied verbatim, any cut marked …"* at `:155-158`.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 173 | **REGRESSION, do-not-merge class — `content-quality-auditor` e4.6, and it is the same error scored a second time.** Same record and baseline, **evidentiary grade B** on the same legs as row 172. e4.6's third clause (*"no quoted Greek phrase is absent from the fixture"*) fails on the identical `e4.md`:141 elision; its other two clauses pass. **The record flags this against itself**: *"not an independent defect — the suite scores one error twice"*. **Two consequences that pull in opposite directions, and both belong in the register.** For the count: it is a graded expectation whose verdict moved, so it is a regression and is counted as one — which is why the wave's 19 is nineteen moved verdicts and not nineteen distinct defects. For the suite: two expectations sharing one failure condition double-weight a single span in the pass rate, and nothing has actioned that. Fixing the quote closes both rows; the overlap outlives the fix. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** the same carrier as row 172 — and the row's own *"the suite scores one error twice"* caveat is unaffected by it.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 174 | **REGRESSION attributed to the SKILL and not to the run — `content-refresher` e1.1.** From `blind-2026-08-17/content-refresher.json` against `blind-2026-08-13/refresher.json`, with `blind-2026-08-11` and `blind-2026-08-10c` as further points; the expectation held PASS in all three, the 08-13 pass carrying an explicit *"IMPRECISION RECORDED"* on this very clause. **Evidentiary grade B** — 24 of 27 expectations byte-identical, all five prompts and all four fixtures byte-identical, and the three reworded ones compared against the 08-13 record's own `secondary_grading_against_HEAD_suite`, which graded them on exactly this text; the prompt leg is stated suite-wide. **What moved**: clause (b). The prior deliverable asserted the supersession implicitly and named the replacement content; this one refuses — `e1.md`:241-243 *"Nothing on file here establishes either product's current name, so this analysis asserts nothing about them in either direction"* — and the drafted replacement copy (`e1.md`:413-429) names no analytics product at all. **The record puts the cause in the skill and quotes it**: `refresh-templates.md`:298-299 instructs *"Anything not in the register stays open… assert nothing in either direction. The register is the stopping condition, not a licence to correct by memory"*, and the superseded product name occurs zero times in this library outside the fixture. **Do-not-merge on the skill, not on the run** — the fix is a scope clause at :298-299, and the deliverable did what it was told. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `3ce98c9` is the located repair commit for this skill's carriers; no clause-level carrier for e1.1 was confirmed at HEAD by this pass. **Carrier not confirmed at clause level.** **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 175 | **REGRESSION that is the skill obeying a binding rule, and the suite is the artefact that has to change — `content-refresher` e2.6, ledger F13 recurrence 5 unexecuted.** Same record and baseline as row 174, **evidentiary grade B**. The paste-ready opening and FAQ ship as labelled skeletons with bracket slots (`e2.md`:210-235, 284-317) where the 08-13 deliverable shipped both blocks filled — which the fixture cannot support, since it withholds the timing answer and supplies only a transcriber's note that it *"first appears in the fifth paragraph"*. **The history is the finding**: this expectation FAILED the same way at `blind-2026-08-11`, was ruled a suite defect that day (*"The one regression in this suite is the skill behaving correctly"*), and F13-r5's status still reads that e2.6 needs the carve-out or the fixture needs paragraph 5 — a coordinator ruling, suite not edited. The record establishes e2.6 byte-identical from `8fdb629` to HEAD, so **the ruling has not been executed and the untouched expectation has now cost a second run.** It is inside the 19 because a graded verdict moved; it is not a defect in the deliverable, and a fix list that treats it as one will edit a skill that is already right. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** nothing at HEAD changes the suite, which is the artefact the row says has to change; ledger F13 recurrence 5 is still unexecuted. **This is a step-7 suite finding**: the row records the skill obeying a binding rule and the expectation punishing it. **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 176 | **REGRESSION with the lenient reading printed beside it — `serp-analysis` e3.1, the SERP-feature combination diagnosis.** From `blind-2026-08-17/serp-analysis.json` against `blind-2026-08-10/serp.json` (28/29 = 96.55%). **Evidentiary grade A**, and this record states the three legs per expectation more explicitly than any other in the wave: expectation text diffed at `1a1b55d`, prompt unchanged, and the depended-on region diffed — `git diff e688327 HEAD` over `serp-feature-taxonomy.md` touches no line carrying *"Combination"*, *"high-information"*, *"Optimize for both"* or *"AI Overview + Featured"*, so the table is byte-identical to what the passing run read. **What moved**: the prior run named the pairing as a diagnosis and cited the combination table; this one addresses both features in the strategy and never reads the co-occurrence as a signal. **The grader graded it strictly and printed the other reading**: nothing false is said, both features get full strategies, the composition mapping is if anything better than the baseline's, and under a lenient reading of *"read per the table"* it passes and the suite scores 27/29. What is lost is the diagnostic step that tells a reader why both are being worked at once. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `1675e11` is the located repair commit for this skill; no clause-level carrier for e3.1 was confirmed at HEAD by this pass. **Carrier not confirmed at clause level.** **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 177 | **REGRESSION, and the deliverable contradicts its own rule six lines later — `serp-analysis` e4.2.** Same record and baseline, **evidentiary grade A** with the sharpest available third leg: `git diff e688327 HEAD` over `greek-tourism-seasonality.md` is **empty**, and only e4.2's tool clause was reworded while the breached peak-month clause is verbatim identical. **What moved**: the baseline's pass evidence recorded, in those words, *"No month is named as a likely peak anywhere in the deliverable."* This run names August as peak season twice, unsourced and unframed (`e4.md`:226 and :485) — six lines above its own restatement of the rule, *"Measured, not assumed"* (:232), inside the section whose entire subject is not asserting an unmeasured month curve, and it is the exact class that reference's S4 exists to stop. **The alternative reading is stated and refused with a reason**: if *"August is peak season"* counts as common knowledge rather than a seasonality finding it passes and the suite scores 27/29, but the clause is unconditional and the same paragraph is arguing that seasonal shape must be measured per origin market. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `1675e11` likewise; the peak-month clause itself is unchanged. **Carrier not confirmed at clause level.** **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |
| 178 | **REGRESSION on an UNCARRIED expectation — `schema-markup-generator` e1.7, and the baseline passed it on general knowledge.** From `blind-2026-08-17/schema-markup-generator.json` against `blind-2026-08-10b/schema.json` (44/44 = 100%); like-for-like on the 34 byte-identical expectations, 100% → 97.06%. **Evidentiary grade A** — the expectation text is quoted in full and byte-identical, prompt and fixture are stated identical for this expectation, and `git diff da16893..HEAD` over the skill tree shows no telephone-format guidance added **or** removed. The baseline emitted `"telephone": "+30 210 976 5432"` and disclosed the normalisation in prose; this run emits `"telephone": "210 976 5432"` (`e1.md`:36), a whole-file grep for `+30`, `international` and `διεθν` returns zero so it is absent from prose too, and `e1.md`:76 states the choice: *"in the exact form the page prints it"*. **The larger finding is that the expectation has no carrier, and the record says so**: the rule it grades is stated nowhere in the skill — `validation-guide.md`:197 is the whole of it, a table row reading *"telephone \| Recommended \| Phone number"*, and the only country-code signal in the tree is an illustrative slot value that models a form and states no rule — so the baseline pass rested on the executor supplying `+30` from general knowledge, **and on an unsourced engine claim while doing it** (*"Google asks for international dialling format"* has no source in the skill either). F13-r3/r4's signature: a carrier that passes once by luck is not a carrier. Fixing the deliverable does not fix this; writing the rule into the skill does, and whatever reason it gives is subject to R3 amendment 9a. — **RECONCILED 2026-08-18 at `5909552` — OPEN as a regression; carrier state named, verdict unproven.** `build/schema-markup-generator/SKILL.md:292` now states the notation rule the expectation was grading uncarried — *"`telephone` in international dialling format with the country code (`+30 2310 555 000`)"* — restated at `references/validation-guide.md:131`.  **No re-run exists**, so nothing in the tree can close this row: `blind-2026-08-17/` is still the newest record for every suite in it, and **0 of 20** of those records grades the version now shipping (row 127's re-measurement). A carrier landing is not an expectation passing — that is F13-r3's standing rule `[obs:2026-08-18 grep -n on the named carrier file; ls docs/loop/eval-baselines/ — no record dated after blind-2026-08-17, at 5909552]`. |

## C. Process, not defects

| # | Item |
|---|---|
| 76 | Site-access resolution and the crawl dispatch. Superseded 2026-08-11: the account owner created a **"Custom 1"** environment; sessions spawned from here inherit the caller's environment and never saw it. A local machine's own network sidesteps this entirely. |

---

## The merge gate — stated once, precisely

Sani authorised a **conditional** merge on 2026-08-11 (thirteenth verdict entry): merge **if
both** the blind re-runs and an independent review come back clean; fix and do not merge if
anything fails.

**The condition is NOT met.** Four independent reasons, and they are not interchangeable:

1. **Mode A's second pass returned BLOCK** — F11 recurrence and F9 recurrence 4, both in the
   coordinator's own commit. The repairs landed afterwards and **have never been reviewed**.
2. **Two suites carry a contested regression** (content-refresher e2.6, alert-manager e2.5),
   each with both readings stated rather than resolved.
3. **PR #9 is a draft.** It must be marked ready before GitHub will merge it.
4. **The re-runs are evidence about skills that no longer exist** (finding C1, 2026-08-13).
   Sani's condition names *"the blind re-runs"* as one of its two legs. Three of the four blind
   records were taken against versions the tree has since moved past — and this commit moves
   two of them again. **A clean run against a superseded version cannot satisfy a condition
   about the current one**, so the verification leg is unmet no matter how the FIX list
   resolves. This is the reason the merge cannot proceed even on a day when everything else
   clears: it is not a defect to fix but a measurement to retake.

**And the standing constraint above all of these**: the merge needs **Sani's explicit order**.
A condition the coordinator itself judges satisfied is self-authorization, not authorization.
Even with all four reasons above cleared, the correct action is to report that they are cleared
and wait.

**On which gate re-runs**: `docs/loop/ADVERSARIAL-LAYER.md` separates **Mode A** — rule
checklist, holds verdict authority SHIP/FIX/BLOCK/UNDECIDED — from the **contrastive lane**,
which gets no checklist and no verdict. They are different gates with deliberately different
vocabularies, and "the Mode A adversarial review" wrongly merged them in an earlier instruction.
**Both must run.** Mode A because it holds the verdict and its BLOCK stands unanswered; the
contrastive lane because the layer fires it unconditionally on a class (i)+(ii)+(iii) artefact,
which PR #9 is.

**On write access**: GitHub write reaches this repository through the session proxy that has
been pushing all day. A local session holding no token cannot and should not execute the merge —
**it is not a missing capability to work around.** The merge is executed from an authorised
session, or by Sani in the GitHub UI, and only once both gates return clean.
