# AI-citation query list — FROZEN AT §4 LOCK

**State: TEMPLATE, unfilled. DRAFTED 2026-08-12 (R76).** Rows are filled once, from the frozen
keyword set, at lock. After lock: no additions, no removals, no rewording. A later exploratory
list may be kept as a separate appendix and is never merged into the pre-registered metric.

Size: **8–15 `CQ` rows**, each drawn from the fixed keyword set (`PILOT.md` §2b), each optionally
paired with **one** conversational reformulation recorded verbatim.

## Main list

| CQ id | Query text (verbatim) | Base keyword id | Form (keyword / conversational) | Mapped page ID |
|---|---|---|---|---|
| CQ1 | *[at lock]* | | | |
| CQ2 | | | | |
| CQ3 | | | | |
| CQ4 | | | | |
| CQ5 | | | | |
| CQ6 | | | | |
| CQ7 | | | | |
| CQ8 | | | | |

## Controls — both mandatory, both fixed at lock

| Control | Query text (verbatim) | Rationale recorded at lock |
|---|---|---|
| **NEG** — personalisation detector | *[at lock]* | In the cluster's topic; our property has **no page** for it. A citation of us here flags the session. |
| **POS** — surface detector | *[at lock]* | A known Greek competitor should plausibly appear (register names `heatovent.com` in heaters). No competitor and no Greek retailer anywhere in the session flags it. |

## Selection rules, so the list cannot be assembled to flatter us

1. Every `CQ` comes from the frozen keyword set — no query is invented for this list.
2. **At least one Greek inflected pair** appears among the `CQ` rows, so the W10 inflection leg is
   observable on the AI surfaces as well as on the SERP.
3. Queries are not chosen by how well we currently rank for them. Draw them by the same rule that
   built the keyword set (impressions rank after the Peec filter), not by preference.
4. The **NEG** control is chosen before the main list, so it cannot be picked to be safe.
5. Mapped page ID uses the anonymised `P1…Pn` scheme while the repository is public.
