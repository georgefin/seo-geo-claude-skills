Single maintenance action for the SANI HELLAS R&D weekly loop (you are a fresh session; full context is in docs/loop/PIPELINE.md of github.com/georgefin/seo-geo-claude-skills). Europe/Athens returns to summer time on Sunday 2027-03-28 (EET UTC+2 → EEST UTC+3). Call update_trigger with trigger_id=trig_012FupQL9hy8p9FPeBHo9Lee and cron_expression="0 4 * * 6" so the weekly skill-update check keeps firing at 07:00 Athens on Saturdays through summer. Then confirm via list_triggers that its next_run_at is Saturday 2027-04-03 at ~04:0x UTC, and report in your final message: old cron, new cron, next_run_at. Touch NO other trigger. If trig_012FupQL9hy8p9FPeBHo9Lee no longer exists (the routine may have been recreated), locate the single ENABLED weekly routine named like "Herbert Skill Update Check" via list_triggers and apply the same cron change to it, reporting which ID you changed; if there are zero or multiple candidates, change nothing and say so. If update_trigger is blocked or refused, do not retry destructively — state plainly in your final message that the cron flip must be done manually in the claude.ai Routines UI (change 0 5 * * 6 to 0 4 * * 6), so the completion notification tells Sani.