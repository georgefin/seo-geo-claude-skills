# CASE: gap-bare-none-accepted
# ROLE: known-gap
# JOURNAL: journal-live-two-holders.tsv
# COMMIT-AT: -450
# FILE: docs/loop/KPI.md
# EXPECT-EXIT: 0
# EXPECT-MATCH: asserts no holder content — Register-Lock: none
# GAP: this file's own header states "A bare `none` with no reason is NOT accepted".
#      It is accepted. The reason is extracted with `sed 's/^[^-]*--[[:space:]]*//'`,
#      and `[^-]*` cannot reach the `--` because "Register-Lock" contains a hyphen
#      first, so the substitution NEVER fires: `none_reason` is the whole trailer
#      line, which is non-empty and unequal to "none", and the commit passes. The
#      probe's evidence is the output line itself, which prints the untouched trailer
#      where the reason should be.
# WHY NOT FIXED HERE: the repo's own history writes this trailer with an EM DASH
#      ("Register-Lock: none — new directory, no shared register"), so a fix that
#      requires "--" would reject the form actually in use, and a fix that accepts
#      both is a judgement about the contract, not a typo repair. It also changes what
#      a GATED leg accepts while other lanes are mid-push. Reported, not applied.
# ---8<--- commit message
docs(loop): weekly numbers

Register-Lock: none
